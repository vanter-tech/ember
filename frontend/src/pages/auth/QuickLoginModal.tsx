import type { QuickAccessProfile } from '@/store/quickAccessStore'

// Placeholder — PIN entry + password fallback land in EMB-FEAT-09.
export const QuickLoginModal = (props: {
  profile: QuickAccessProfile
  onClose: () => void
}): null => {
  void props
  return null
}
