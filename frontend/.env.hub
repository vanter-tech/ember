# Loaded only by `vite build --mode hub` (pnpm run build:hub). Marks the on-prem Hub bundle:
# the customer subsystem is stripped and the waiter drives the whole table. See src/lib/isHubBuild.ts.
VITE_HUB_BUILD=true
