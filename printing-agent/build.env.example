# printing-agent/build.env.example
# Copy to printing-agent/build.env (gitignored). The agent installer bakes in NO
# secrets or URLs — the backend base URL is delivered by POST /printing/agents/pair
# when the operator redeems a pairing code. This file only exists so the build flow
# mirrors ember-hub's "copy build.env.example" convention.
# (sin variables por ahora)
