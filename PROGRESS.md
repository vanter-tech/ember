# PROGRESS.md — Active Execution State

## Current Execution State
- **Last Completed Task:** EMB-RV-final-review-fix-wave — fixed all 2 Critical + 4 Important findings from the EMB-RV whole-branch review (see EMB-RV bullet below). Backend 686 tests green, frontend `pnpm run build` green. Predecessor: bugfix-refund-modal-review-findings (144).
- **Current Active Task:** none. System health: backend `./mvnw test` green (686), frontend `pnpm run build` green. EMB-RV backlog is now mergeable per the controller's review. Still-owed from before: EMB-CR whole-branch review, manual browser check of EMB-CR/EMB-STAFF/EMB-RV pages (no `claude-in-chrome` tool available this session either — this fix wave was verified via full test suites + hand code-path tracing, see report 145), repo-wide `pnpm run lint` ~15 pre-existing errors, `/landing` follow-ups, no UI trigger for `PATCH /admin/users/{id}/role`.

## Active Context & Recent Decisions
- Monolith root at `ember/`; Java 17 + Spring Boot 3.5.14 / React 19 + TS + pnpm. Product: multi-tenant restaurant platform (collaborative cart, KDS, floor/waiter management, admin analytics). task-3.2 deleted `spring-kafka` — Spring `ApplicationEventPublisher` is the only event bus, do not reintroduce a broker.
- task-3.3: `GlobalExceptionHandler` extends `ResponseEntityExceptionHandler` (mandatory — a bare `Exception.class` catch-all would outrank `DefaultHandlerExceptionResolver` and degrade 400s to 500); RFC 7807 via `problem(status, detail, path)`, 500s log ERROR with a `traceId`, never echo `ex.getMessage()`. task-2.15's **Flyway** (`baseline-on-migrate`, migrations from V2, disabled in tests/H2 `create-drop`): every schema change goes in `db/migration/` AND the entity mapping; `ddl-auto: update` silently can't add `NOT NULL` columns without a default to a non-empty table — check boot logs for `CommandAcceptanceException`, don't trust a clean boot.
- Mongo has no replica set/`MongoTransactionManager` — fail-fast validation-before-mutation instead of `@Transactional`. `Session`/`KitchenOrder` carry `tenantId` (task-2.17, no Mongo `@TenantId`, every finder is tenant-first; route session reads through `SessionService.findById`). Hibernate JPA side uses `DISCRIMINATOR` multi-tenancy (task-2.14): `@TenantId` on `Category`/`MenuItem`/`Bill`/`BillSplit`/`Payment`/`DiningTables`/`RestaurantSettings` (`User` deliberately excluded, looked up by email pre-tenant-bind); isolation repo tests extend `config/AbstractTenantIsolationTest`. `config/MongoTenantBackfill` (task-2.18) and `config/KitchenOrderActiveBackfill` (bugfix, report 52 — retires `KitchenOrder.active` on `SessionClosed`, backs `GET /kitchen/display`'s live-only filter, `GET /kitchen/orders` stays unfiltered) are both idempotent boot-time `ApplicationRunner`s that resolve missing/legacy Mongo fields; that's the pattern to follow for any future Mongo field backfill.
- task-3.4: `config/CorsProperties` (`ember.cors.*`) is the ONE origin policy (tenant subdomains → `allowed-origin-patterns`, never `allowed-origins`); task-3.5's `AuthRateLimiterFilter` buckets `(tenant, clientIp)`, applies servlet-wide via `RateLimitProperties.paths`, not `SecurityConfig`. task-3.1: `.env` (gitignored) holds all secrets, no fallback/fail-fast boot, add new ones to `.env.example` too; old secrets remain in git history, rotation pending. task-4.3's `GET /public/restaurants/{slug}/branding` (permitAll) and the JWT filters are the ONLY places allowed to touch `TenantContextHolder`. task-4.4 made `Restaurant.plan`/`status` load-bearing (403 when `status != ACTIVE`).
- task-3.6–4.2: `SettingsPayload.paymentGateway`/`businessHours`/`billing.taxRules` now fully wired frontend-side (task-5.4–5.8); `GET /kitchen/orders`/`/catalog/items` return `Page<T>` (hand-written in `api.ts`). Remaining task-5.x gaps: `api.ts`'s interceptor only special-cases 401 (not 403); `backend-types.ts` predates task-4.3/4.4 additions. Plan/subscription self-service is real on the backend (`RestaurantAdminController`) but has NO frontend home — belongs on a future customer/billing portal, not the admin app; `api.ts`'s `restaurantAdminService` stays unused until then (report 44) — don't re-add a Plan settings tab. `/admin/analytics` (task-5.17–5.21, `FloatingNav.tsx`'s `BarChart3` link) is now COMPLETE: `SummaryCards`/`SalesChart`/`ProductPerformance`/`TableAnalytics` all consume `analyticsService` in `api.ts`, whose response types are `components['schemas'][...]` aliases (refactor-analytics-types-schema, report 66) — every field on them is OPTIONAL, and this project's build has `strictNullChecks` effectively ON despite no `strict`/`strictNullChecks` key in either tsconfig (verify with `npx tsc -b` if unsure), so every numeric/array field needs a `?? 0`/`?? []` default at the call site, not just at declaration. `SalesChart` was restyled (redesign-analytics-enterprise-ui, report 111) onto a Recharts `AreaChart` (brand-red gradient fill, `type="monotone"`) — `recharts` is now a real dependency, so any future analytics chart should reuse it rather than reintroducing a plain-CSS bar pattern. STILL no shared date-range picker UI anywhere on the page — any future analytics work adding one should make it shared, not per-widget.
- task-5.10/5.11: KDS subscribes to `/topic/kitchen/{tenantId}` (`KitchenLayout` + `websocket.ts`'s `subscribeToKitchen`), just calling `queryClient.invalidateQueries(['kitchenOrders'])` on any message rather than reconciling payloads client-side; backend `KitchenWebSocketListener` broadcasts, `KitchenItemUpdated` gained `tenantId`. No STOMP SUBSCRIBE-destination ACL exists for this topic (or `/topic/session/{id}`) — pre-existing, accepted gap. Connection-state `Badge` reading `useWebsocketStore.isConnected` lives in `OrdersDisplays`, not `KitchenLayout`.
- CUSTOMERS ARE NOT TENANT-BOUND (reports 55–56): `User.restaurantId` is null for them by design (staff keep it, still required); a customer's tenant is only known once they JOIN A TABLE, so login/register mint a JWT with NO `rid` and `jwtAuthFilter` waves tenant-less CUSTOMERs through (staff still gated). `joinSessionCode` looks the code up untenanted (`findByJoinCodeAndStatus`, rejects a code open at 2 restaurants), `joinSession` reads the tenant off the signed QR token; both bind it via `SessionService.bindResolvedTenant` (the one non-filter place allowed to touch `TenantContextHolder`, and it re-checks `ACTIVE` since the filter no longer can) and return `JoinSessionResponse{session,token}` — the client MUST swap to that token (`JoinTableModal`), as menu/items/confirm all need `rid`. `issueTenantScopedToken` refuses to re-scope staff. `/t/:slug` (`TenantLanding`) is still UNREACHABLE — no inbound link anywhere; don't build flows through it without wiring an entry point first.
- task-5.12: `analytics` module (`controller`/`service`/`dto`) owns NO persistence — it only aggregates over `billing`/`session`/`catalog`. `AnalyticsController` carries a CLASS-LEVEL `@PreAuthorize("hasRole('ADMIN')")` so task-5.13–5.16's handlers inherit the gate; tenant always via `TenantContextHolder.requireTenantId()`, never a request param. Analytics repo queries name the tenant explicitly (`where b.tenantId = :tenantId`) ON TOP of the `@TenantId` filter — a context slip here returns plausible numbers instead of throwing. Read shapes are JPQL constructor-expression records living in the OWNING module's repo package (`billing.repository.BillActivityWindow`), not interface projections and not analytics DTOs. Seed endpoint `GET /admin/analytics/range` (billing window for task-5.19's date pickers) — add new routes to `SecurityAuditTest`'s 401 matrix.
- task-5.13 (`GET /admin/analytics/summary?from&to`) fixes the metric semantics task-5.14–5.16 must reuse: revenue = CONFIRMED `Payment` sums (never PENDING, never bill totals); AOV = PAID-`Bill` `sum/count` (2dp HALF_UP); `activeSessions` is a LIVE `countByTenantIdAndStatus(OPEN)` that deliberately IGNORES the window. `from`/`to` are optional and INCLUSIVE, defaulted in the service (`EPOCH_FLOOR` = 1970-01-01, never `LocalDateTime.MIN` — no SQL `timestamp` holds it — and `now()`), echoed back in the response; an inverted window throws `IllegalArgumentException` (409) rather than reading as "no sales". Null aggregates normalise to `0`/`0.00`. task-5.14 (`/sales`) reuses that window helper and those metrics: DB groups only by `year()/month()/day()` (NOT `date_trunc` — spells differently on H2 vs Postgres), week/month/year rolled up in Java by `SalesGranularity` (ISO Monday weeks); series is gap-free (zero-filled) and, with no `from`, starts at the first ACTIVE bucket, not `EPOCH_FLOOR` (which would emit ~20k empty daily buckets); unknown granularity → `IllegalArgumentException` (409), never a silent DAY fallback. task-5.15 (`/products`) is the one analytics read that CROSSES STORES: `BillRepository.findPaidSessionIds` (Postgres) → `SessionRepository.findByTenantIdAndIdIn` (Mongo line items, skip `DRAFT`) → `MenuItemRepository.findByTenantIdAndIdInWithCategory` (`join fetch` category) — same "a sale is a settled bill" rule, but its `totalRevenue` is ITEM money and deliberately ≠ `/summary`'s payment revenue (tax/tips live on the bill). Shares/`cumulativeShare` are computed over the FULL set before `limit` truncates; deleted menu items keep the name they were sold under (null category), renamed ones report the current catalogue name. task-5.16 (`/tables`) ALSO crosses stores: `Bill` has no `tableId` of its own, so `BillRepository.findPaidBillActivity` (sessionId+total+createdAt) joins back to `Session.tableId`/`createdAt` in the service, same as products. `Session` has no closed/settled timestamp, so session duration is approximated as `bill.createdAt - session.createdAt`, in minutes; `activeTableCount`/`averageTurnoverRate` are LIVE like `/summary`'s `activeSessions`, ignoring the window; a deleted table keeps the revenue it earned (null `tableNumber`, same pattern as a deleted menu item).
- EMB-PC backlog (COMPLETE, reports 70–83): `/console/*` platform-operator portal, fully isolated from the tenant app — `PlatformOperator`/`PlatformAuditLog` have no FK to `Restaurant`/`User`, signed with a separate `platform.jwt.secret`, own `SecurityFilterChain` on `/platform/**` (`@Order(1)`) that never touches `TenantContextHolder`; frontend `platformApi.ts`/`usePlatformAuthStore` are a fully separate Axios instance + auth store from the tenant app's. `pnpm run openapi` regen still doesn't cover `platform/**` schemas (hand-typed instead). feature-catalog-pagination (report 68): `GET /catalog/categories` now takes `Pageable`/returns `Page<CategoryResponse>` — read `.content`, not a bare array; `components/PaginationControls.tsx` is the shared Prev/Next control (0-indexed, self-hides at `totalPages <= 1`, SELF-POSITIONED fixed `bottom-8 right-8` floating pill per feature-pagination-floating-style report 69) — reuse for any future paginated admin list.
- EMB-CR backlog (brainstormed 2026-08-16, spec+plan under `docs/superpowers/`): single shared till per tenant (`cash_shifts` partial unique index on `tenant_id WHERE status='OPEN'`), blind arqueo (close endpoint accepts only `countedCash`; server reveals `expected`/`variance` in the response), no separate Z-report entity — `CLOSED` shift rows are write-once and "Corte Diario" is a derived rollup query over them. `openedBy`/`closedBy`/`createdBy`/`processedBy` are plain `varchar` `users.id` columns, NOT JPA `@ManyToOne` — avoids `LazyInitializationException` from `User.restaurantId`'s LAZY association (`open-in-view` is false). `CashShiftService.openShift` takes `tenantId` as an explicit param (matches every other tenant-aware service method), not `TenantContextHolder.requireTenantId()` internally as the plan's literal text had it — a plan-text gap the implementer corrected, confirmed by a Mockito strict-stubbing failure. `PaymentService.registerPhysicalPayment` now hard-requires an `OPEN` shift (gained `CashShiftRepository`/`UserRepository` deps, stamps `cashShiftId`/`processedBy`); DIGITAL payments unaffected. Deliberately NO new frontend WebSocket subscription at the time: `websocket.ts`'s single `currentSubscription` slot was already claimed by `subscribeToWaiter` for the whole `/waiter/*` subtree, so a `subscribeToCashRegister` would have silently broken floor-dashboard real-time updates — the backend still broadcasts to `/topic/cash-register/{tenantId}`, unconsumed. The shared-slot design was fixed by bugfix-waiter-tables-realtime-orders (report 123), which added an independent `waiterSessionSubscription` slot + `subscribeToWaiterSession`/`unsubscribeFromWaiterSession` — that's now the pattern for any future waiter-subtree subscription that must coexist with the tenant-wide one (e.g. a future `subscribeToCashRegister` should get its own slot too, not reuse `currentSubscription`). **Process note:** both halves were meant to run under `subagent-driven-development` via two parallel fork subagents, but forks cannot spawn further subagents ("Fork is not available inside a forked worker") — both fell back to implementing directly with self-review only, no per-task independent reviewer, hence the whole-branch review dispatched afterward.
- EMB-LP backlog (COMPLETE, reports 84–101): `ember/landing/` marketing site — FULLY STANDALONE Astro 7.2.2 package (own lockfile/workspace, zero coupling to `frontend`, static-build only, deployed separately), `site: 'https://ember.vanter.com'`. Placeholder brand art (`og-image.png`/`apple-touch-icon.png`/hero screenshot) still needs swapping for real assets before launch. `astro build` is its own verification command, not `pnpm run lint`/`./mvnw test`.
- EMB-PAY backlog (brainstormed 2026-08-16): completed the customer-facing payment cycle. `billing/event/BillingRequested` + `BillingEventListener` + `BillReadyMessage` existed fully tested but UNPUBLISHED (no controller ever fired the event) — EMB-PAY-01 added `POST /billing/sessions/{sessionId}/request` (WAITER) to publish it; Spring's event bus is synchronous, so calculate+split+broadcast complete before the HTTP response returns. There is still NO `GET` endpoint for `Bill`/`BillSplit` — the customer/waiter only ever learn bill state via `/topic/session/{id}` broadcasts (`BILL_READY`/`SPLIT_PAID`/`DIGITAL_PAYMENT_INITIATED`); `websocket.ts`'s waiter handler writes straight into the `['bill', sessionId]` React Query cache via `setQueryData` (no query fn exists to `invalidateQueries` against). Known gap: a customer who reloads/reconnects after missing a broadcast sees stale bill data until the next one arrives. `PaymentService.initiateDigitalPayment` is a stub gateway (`gatewayRef = "STUB-..."`); `confirmDigitalPayment` stays WAITER-only by design, which is why `DIGITAL_PAYMENT_INITIATED` had to be broadcast too. Reports 124–127.
- EMB-STAFF backlog (brainstormed 2026-08-17): `User.active` is now LOAD-BEARING, not cosmetic — `EmberUserDetailsService` sets `.disabled(!active)`, `AuthService.login` separately rejects inactive users (it bypasses `UserDetailsService` entirely, manual `findByEmail`+password-match), and `SecurityConfig.jwtAuthFilter` only sets `SecurityContextHolder` auth when `userDetails.isEnabled()` — an already-issued JWT for a since-deactivated user 401s on its very next request via the existing `anyRequest().authenticated()` path, no new response code needed. This is the pattern for any future "must re-check DB state per-request" auth rule (mirrors the existing `Restaurant.status` tenant-suspension check in the same filter). "Delete" staff = deactivate (`active=false` via existing `PATCH /admin/staff/{id}`), a deliberate choice over hard-delete to preserve `Payment.processedBy`/`CashShift.openedBy` audit trails (those are plain varchar, not real FKs).
- EMB-RV backlog (plan under `docs/superpowers/specs/2026-08-17-refunds-and-voids-design.md`, backend EMB-RV-01..05 done, frontend EMB-RV-06..08 in progress): void a `Bill` pre-payment, refund (full/partial) a `CONFIRMED` `Payment` post-payment, both WAITER-only, additive/append-only. `Refund` never mutates `Payment` (no `PaymentStatus.REFUNDED`); `BillSplit.paid`→`status` (`BillSplitStatus` UNPAID/PARTIALLY_PAID/PAID) so partial refunds are representable; `BillStatus.VOIDED` terminal, `Bill` gains `voidedBy`/`voidedAt`/`voidReason`. A physical refund's till impact is a plain `CashMovement`/`CASH_OUT` against whichever shift is open *now* (never rewrites a `CLOSED` shift) — `PaymentService` builds it directly via `CashMovementRepository`, deliberately NOT through `CashShiftService.recordMovement`, to avoid a circular dependency now that `CashShiftService.getDetail` (EMB-RV-04) depends on `PaymentService`. `refunded_by`/`voided_by` are plain varchar `users.id`, same convention as `processed_by`/`opened_by`. EMB-RV-06 (report 142): `api.ts` gained `billingService.voidBill`/`listPayments`/`refundPayment`/`listRefunds`; `sessionStore`'s `markSplitPaid`→`markSplitStatus`+`clearBill`. EMB-RV-07 (report 143): new `VoidBillModal`/`RefundPaymentModal` (waiter `TableInformation.tsx`, mirrors `ChargeTableModal.tsx`'s pattern); `Bill.tsx` (customer) also reads `split.status`. EMB-RV-08 (report 144): `ShiftHistoryTable.tsx` gained an expandable per-shift payments row reusing (not duplicating) `RefundPaymentModal`; `CashShiftDetailResponse.payments` is optional in the generated schema, needs `?? []` at the call site (same convention as the analytics response types). No `claude-in-chrome` browser tool was available this session — EMB-RV-07/08's manual-verification steps were substituted with a full curl/psql-verified API walkthrough (fresh test tenant via the seeded platform operator) against the live worktree backend instead; a real click-through is still owed. **Post-review fix (bugfix-refund-modal-review-findings):** task-reviewer caught 2 gaps in the plan's own `RefundPaymentModal.tsx` code — `onSuccess` didn't invalidate `['cashShiftDetail']`, so a refund from Task 8's admin view left that page stale (no WS subscription to self-correct it) — fixed by also invalidating that key's prefix; and the amount-sync `useEffect` depended on `[payment]` (whole object), so any background refetch of the same payment (e.g. window refocus, default `staleTime`) silently reset a partially-typed refund amount back to the full balance — fixed by depending on `[payment?.id]` instead, which only re-syncs on an actual payment switch. **EMB-RV-final-review-fix-wave (report 145, EMB-RV backlog now mergeable):** fixed all 6 whole-branch-review findings — `calculateBill` now guards via `findBySessionIdAndStatusNot(sessionId, VOIDED)` so voiding actually frees a session to re-bill (was matching any-status bill, a dead `findBySessionIdAndStatusNot` call site since Task 1); `Bill.java`'s `@Table` dropped its stale `uk_bills_tenant_session` JPA constraint (would've fought V8's partial-unique-index under `ddl-auto: update`, same bare-`@Table` pattern as `CashShift.java`); `SplitPaidMessage` migrated `paid:boolean`→`status:String` end-to-end (backend DTO+call sites+tests, frontend `websocket.ts`'s two `?? 'PAID'` fallbacks dropped); `registerPhysicalPayment`/`initiateDigitalPayment` now reject a `VOIDED` bill at creation time; the admin "Reembolsar" button (unreachable by WAITER, 403'd for ADMIN — `POST /billing/payments/{id}/refund` is WAITER-only by deliberate design, not relaxed) moved to a new "Pagos" card on the waiter's own `CashRegister.tsx`, reusing `RefundPaymentModal`; that modal now resolves by exact `paymentId` when the caller has one (new waiter call site does) instead of always name-matching, which could previously grab an already-refunded payment when a participant paid twice on one bill. Backend 686/686 green, frontend build green.

## Task Queue Status
- [x] **Completed (Milestones 1–5, task-5.1–5.21 + bugfix-kds-stale-orders + bugfix-customer-login-500 + refactor-analytics-types-schema):** Frontend/backend stability, WebSocket/error-boundary hardening, backend multi-tenancy & transactional billing, security/config hardening, frontend testing & tenant lifecycle, global 403 handling, `backend-types.ts` regen, ADMIN Plan/Estado UI (reverted, kept for a future billing portal), `MENU`/`BILLING`/`HARDWARE`/"Horario" settings tabs, KDS status-transition controls + stale-order fix + WS topic + connection badge (task-5.9–5.11), CUSTOMER login 500 fixed + tenant-binding moved to table-join (reports 55–56), analytics backend (`/range`/`/summary`/`/sales`/`/products`/`/tables`, task-5.12–5.16, reports 57–61) and its full `/admin/analytics` frontend (page scaffold + Summary Cards + Sales chart + Product Performance + Table Analytics, task-5.17–5.21, reports 62–65+67) with a types refactor to `components['schemas']` in between (report 66). Server-side pagination added to Categories and Menu Items admin views (feature-catalog-pagination, report 68) with its controls restyled to match `FloatingNav` (feature-pagination-floating-style, report 69). `PlatformOperator` entity/repository/seed migration (EMB-PC-01, report 70). `PlatformAuditLog` entity/repository/migration (EMB-PC-02, report 71). `PlatformJwtService`/`PlatformOperatorDetailsService` (EMB-PC-03, report 72). `/platform/**` `SecurityFilterChain` + `PlatformJwtAuthFilter` + cross-rejection test (EMB-PC-04, report 73). `PlatformAuthController` login + self-service password change (EMB-PC-05, report 74). `PlatformRestaurantController` list + detail (EMB-PC-06, report 75). `PATCH /platform/restaurants/{id}/status` + audit log write (EMB-PC-07, report 76). `POST /platform/restaurants` tenant onboarding + audit log write (EMB-PC-08, report 77). `GET /platform/audit-log` paginated + `restaurantId`-filterable audit trail (EMB-PC-09, report 78). Frontend `/console/*` scaffold (EMB-PC-10, report 79). `PlatformProtectedRoute`/`PlatformLayout`/`/console/login` (EMB-PC-11, report 80). Console restaurant list page (EMB-PC-12, report 81). Console restaurant detail page + status toggle + audit history (EMB-PC-13, report 82). Console create-restaurant form + self password-change UI (EMB-PC-14, report 83) — EMB-PC backlog complete. Reports 01–83.
- [x] **EMB-LP-01..18 (Landing page, COMPLETE):** Astro scaffold+sitemap+SEO/Layout+brutalist theme+Nav+Hero+Features+Pricing+CTA+Footer+StickyMobileCTA+CookieBanner+index assembly+404+privacy/terms+contact form+analytics+a11y pass. See Active Context bullet for architecture. Reports 84–101.
- [x] **docs-architecture-diagram:** Root `ARCHITECTURE.md` — system connection diagram + order-lifecycle sequence diagram (Mermaid), brainstormed with user. Report 104.
- [x] **docs-architecture-word:** `docs/ARCHITECTURE.docx` — full class-by-class backend/frontend inventory, tech stack, E2E workflow; supersedes/replaces `ARCHITECTURE.md`. Report 105.
- [x] **bugfix-comanda-resend:** `ItemSent` gained `type`/`sessionItems`, broadcast via `SessionWebSocketListener`; `websocket.ts` handles `ITEMS_CONFIRMED` to sync item status, disabling re-send. Report 106.
- [x] **bugfix-kitchen-orphan-item:** new `KitchenItemRemoved` event; `KitchenService.handleItemDeleted` strips the item from `KitchenOrder` on `DeleteItem`, broadcasts on `/topic/kitchen/{tenantId}`. Report 107.
- [x] **bugfix-kitchen-empty-order:** empty-after-delete `KitchenOrder` retires instead of showing a blank card; reused order on a later confirm reactivates. Report 108.
- [x] **feature-comanda-historial:** `ComandaView` current-order/Historial split, whole-table running total; deleted dead `ItemStatusUpdated` customer status sync. Report 109.
- [x] **feature-session-activity-log:** persisted `Session.activityLog` (`ITEM_SENT`/`ITEM_DELETED`); waiter Actividad card reads it, no longer derived from live items. Report 110.
- [x] **redesign-analytics-enterprise-ui:** Shadcn Card/Table polish + Recharts `AreaChart` for `/admin/analytics`; zero hook/query changes. Report 111.
- [x] **summarycards-kpi-accent-refinement:** KPI card values/icons now use the brand accent (`text-primary`/`bg-primary/10`); uppercase titles. Report 112.
- [x] **analytics-cards-padding-and-title-polish:** `py-6` on all `/admin/analytics` Cards, icon-chip `CardTitle`s, `TableAnalytics` stats restyled to match KPI accent. Report 113.
- [x] **EMB-FloatingNav:** `/admin/staff` Gestión de Personal (header/search, department filters, employee card grid + ghost add-card, bottom KPIs), `FloatingNav` `Users` link. Report 114.
- [x] **feature-waiter-dashboard-table-orders-panel:** `/waiter/tables` sidebar now shows real non-draft orders (scrollable) via `sessionInformation`, replacing the static placeholder. Report 115.
- [x] **EMB-CR-01..07 (Cash Register & Daily Shift Management):** `cashregister` backend module (entities/migration/repositories, service+events+websocket, `/cash-shifts` controller) + payment-shift gate (EMB-CR-01–04), waiter operate page + admin oversight/Z-report page (EMB-CR-05–07). Reports 116–122. Pending: final whole-branch review + manual browser check.
- [x] **bugfix-waiter-tables-realtime-orders:** `/waiter/tables` "Detalles de mesa" panel now live-updates on new/confirmed/deleted orders via a new independent `waiterSessionSubscription` WS slot. Report 123.
- [x] **EMB-PAY-01..04 (Customer Payment Cycle):** `POST /billing/sessions/{id}/request` wires the previously-unpublished `BillingRequested` event; `SPLIT_PAID`/`DIGITAL_PAYMENT_INITIATED` broadcasts (EMB-PAY-01). Frontend `billingService` + WebSocket wiring, no GET endpoint so state is WS-push-only (EMB-PAY-02). Waiter `ChargeTableModal` + real split/payment UI in `TableInformation.tsx` (EMB-PAY-03). Customer `Bill.tsx` "Mi Cuenta" screen + nav entries (EMB-PAY-04). Reports 124–127.
- [x] **bugfix-billing-request-generated-type + bugfix-cobrar-mesa-no-billable-items:** `billingService.requestBilling` switched to the regenerated `RequestBillingRequest` type (128); "Cobrar Mesa" disabled until an item is `READY`/`DELIVERED`, fixing a 409 on confirm (129).
- [x] **EMB-STAFF-01..04 (Employee CRUD):** `POST /admin/staff` — tenant-bound `CreateStaffRequest(name,email,password,role)`, rejects CUSTOMER + duplicate email (01) + `CreateStaffModal.tsx` (02), reports 130–131. `PATCH /admin/staff/{id}` gained `name`/`email`; `active=false` now actually revokes access — `AuthService.login` + `EmberUserDetailsService`+`SecurityConfig.jwtAuthFilter` all gate on it, not just cosmetic (03) + `EditStaffModal.tsx`/`GlobalDeleteModal`'s `DELETE_STAFF` (labeled "Desactivar", not "Eliminar" — no hard-delete endpoint exists, by design) (04), reports 132–133. bugfix-staff-generated-types (134) then swapped `UpdateStaffProfileRequest`/`PublicBranding` to their newly-regenerated schemas.
- [x] **bugfix-waiter-tables-require-open-caja:** `/waiter/tables` grid + `Asignar Mesa` button disabled with centered overlay message when no `OPEN` `cashShiftService.current()` shift (135); tweak-waiter-tables-caja-overlay-style then swapped disabled-card `opacity-50`→`blur-sm` and overlay text to brand red `#8c1717` (136).
- [x] **EMB-RV-01:** `Refund` entity + `BillSplitStatus` + `BillStatus.VOIDED` + `Bill` void columns + `V8` migration + repo methods. Report 137.
- [x] **EMB-RV-02:** `BillingService.voidBill` + `PaymentService.refundPayment` service logic. Report 138.
- [x] **EMB-RV-03:** `BillingController` void/refund endpoints + request DTOs. Report 139.
- [x] **EMB-RV-04:** `CashShiftDetailResponse` gains a shift's payments. Report 140.
- [x] **EMB-RV-05:** `AnalyticsService` nets refunds out of revenue. Report 141. EMB-RV backend stream (01-05) complete.
- [x] **EMB-RV-06:** Frontend shared prep — regenerated `backend-types.ts`, `api.ts` void/refund methods, `uiStore`/`sessionStore`/`websocket.ts` wiring. Report 142.
- [x] **EMB-RV-07:** `VoidBillModal`/`RefundPaymentModal` + `TableInformation.tsx`/`Bill.tsx` status-read updates. Report 143.
- [x] **EMB-RV-08:** `ShiftHistoryTable.tsx` expandable payments + refund action. Report 144. EMB-RV backlog (01-08) complete.
- [x] **bugfix-refund-modal-review-findings:** `RefundPaymentModal.tsx` now also invalidates `['cashShiftDetail']` on refund success, and syncs its amount field on `[payment?.id]` (not the whole object) so a background refetch can't silently overwrite a typed partial-refund amount. Report 144 (appended).
- [x] **EMB-RV-final-review-fix-wave:** Fixed 2 Critical + 4 Important whole-branch-review findings (void-then-rebill guard, stale JPA unique constraint, `SplitPaidMessage` status migration, voided-bill payment guard, admin→waiter refund-button relocation, `RefundPaymentModal` payment-id resolution). Report 145. EMB-RV backlog now mergeable.