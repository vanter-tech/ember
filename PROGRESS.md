# PROGRESS.md — Active Execution State

## Current Execution State
- **Last Completed Task (report 320, Task D of the payment-flow bug cluster — CLUSTER COMPLETE, on `feat/hpd-14-monitoring`):** user reported a cluster of payment/session-lifecycle bugs (bill vanishes for a diner who leaves-and-rejoins during payment; waiter can't close a partially-paid table without deleting delivered items; no reachable physical-payment path / caja record; customer bounced off the menu after re-login while their table is still active; can join a 2nd table; no "abandon table" button). Root-caused to 3 clusters — **RC1** the frontend had NO way to *fetch* an existing bill (waiter `['bill',id]` query was a `Promise.resolve(undefined)`/`enabled:false` stub; customer `Bill.tsx` read only the persisted zustand store; both fed solely by the one-shot `BILL_READY` STOMP frame); **RC2** no settle/close path for a partially-paid session + `removeItem` only blocks `PREPARING` not `READY`/`DELIVERED`; **RC3** no "one active session per customer" concept, no leave endpoint, and `logout()` drops the tenant-scoped token so re-login can't read the still-open session. Split into Tasks A–D. **Task A done:** new `WaiterBillStateResponse` DTO + `PaymentService.getBillState(sessionId)` + `GET /billing/sessions/{sessionId}/bill` (WAITER/CUSTOMER, customer gated by new `SessionService.isParticipant`, 204 when no bill); `billingService.getBillState` in api.ts; `TableInformation.tsx` `['bill',id]` now really fetches (WS `setQueryData` merges unchanged); `Bill.tsx` mount-query → `setBillReady` rehydrate. 905/905 backend (900+5 new; first run caught the `@WebMvcTest` slice needing `@MockBean SessionService`), `pnpm run build` green, lint 0 errors. **User decisions for later tasks:** Task C = when a diner abandons mid-payment, announce to remaining participants + waiter that they left and their share is redistributed among those present; abandoning a table with delivered/confirmed items = items stay on the table bill (only DRAFT discarded). **Task B done (report 318):** `SessionService.removeItem` guard widened from `PREPARING` only to `PREPARING`/`READY`/`DELIVERED` (msg → "already been sent to the kitchen"); `SessionServiceTest` guard test parameterized over the 3 states; `TableInformation.tsx` per-item trash `Button` now `disabled` + `title={t('cannotRemoveSentItem')}` when sent-to-kitchen; new `cannotRemoveSentItem` es/en key. Backend `./mvnw test` exit 0 (911 tests, 0F/0E), `pnpm run build` green, lint 0 errors. **Task C done (report 319):** `PaymentService.redistributeSplit(billId, departingName)` — bill OPEN + departing split UNPAID (else 409); spreads `departing.amount` equally (FLOOR + remainder on last) onto the other non-PAID splits, deletes the departing split, bill total unchanged, broadcasts `SplitsRedistributedMessage(type,billId,departedParticipantName,splits)`. `PaymentService.settleAndClose(billId)` — bill OPEN + every split PAID (else 409 "still unpaid"), then bill→PAID + publish `PaymentCompleted` (reuses `PaymentCompletedListener` close + SESSION_CLOSED). `POST /billing/bills/{id}/splits/redistribute` + `POST /billing/bills/{id}/settle`, both WAITER. Frontend: `billingService.redistributeSplit`/`settleAndClose`; `websocket.ts` handles `SPLITS_REDISTRIBUTED` (customer `replaceSplits`, waiter `['bill',id]` splits replace) + `lastBillRedistribution`/`clearBillRedistribution` (mirrors `lastLowStockAlert`); `sessionStore.replaceSplits`; announcement toast in `CustomerLayout` (always-mounted customer shell) + `TableInformation`; per-UNPAID-split `UserMinus` "quitar comensal" button + "Cobrar y cerrar mesa" footer button; 7 waiter + 1 customer i18n keys. 925 backend (911+14), `pnpm run build` green, lint 0 errors, `test:run` 41/41. **Task D done (report 320) — payment-flow cluster COMPLETE:** new `session/event/ParticipantLeft` (`PARTICIPANT_LEFT`), broadcast by `SessionWebSocketListener` (per-session topic) + `WaiterWebSocketListener` (floor topic). `SessionService.leaveSession(id,email)` — OPEN + participant guard; discards the leaver's DRAFT items (publishes a `DeleteItem`/`ITEM_DELETED` each), keeps sent items on the table bill, removes the participant; closes the session when no participants + nothing billable, else publishes `ParticipantLeft`. `billing/listener/ParticipantLeftListener` — if a non-voided bill has the leaver's UNPAID split, calls `PaymentService.redistributeSplit` (Task C) best-effort (catch+log, never fails the leave; event-driven to keep session module off billing). `SessionService.resumeSession(id,email)` — untenanted `findById` (like `joinSessionCode`), OPEN + participant checks, `bindResolvedTenant`; controller wraps `withRescopedToken` → `JoinSessionResponse{session,token}`. `rejectIfSeatedElsewhere` in `joinSession`+`joinSessionCode` → 409 when the user has another OPEN session in the tenant (tenant-scoped; no cross-tenant finder). `POST /sessions/{id}/leave` + `/resume`, both `hasRole('CUSTOMER')` (existing `/sessions/**`→authenticated rule covers the tenant-less token, as with `/join`). Frontend: `SessionTableService.leaveSession`/`resumeSession`; `sessionStore.removeParticipant`; `websocket.ts` `PARTICIPANT_LEFT` (customer removeParticipant, waiter invalidate `['sessionDetails',id]`); `FloatingNav` `DoorOpen` "Abandonar mesa" button + confirm dialog (CUSTOMER when a participant) → leave + clearSession + `/customer/home`; `Login.tsx` CUSTOMER branch resumes a persisted `sessionStore.id` (setAuth{token} + setSession + `/customer/menu`, fallback clearSession + `/customer/home`); `JoinTableModal` 409 toast; i18n common ×7 + customer ×1 + auth ×1. Tests: `SessionServiceTest` +12, `SessionControllerTest` +5, new `ParticipantLeftListenerTest` ×4. Backend `./mvnw test` exit 0 (945 tests, 0F/0E), `pnpm run build` green, lint 0 errors, `test:run` 41/41. **The 4-task payment-flow bug cluster (A–D) is now fully done on `feat/hpd-14-monitoring`.**
- **Sub-project: Hosted Production Deployment — spec + plan DONE (2026-08-28), branch `feat/hosted-production-deployment` off `main` `040bb72`.** Stand up the hosted SaaS SKU on one GCP `e2-medium` VM running Docker Compose (`postgres`/`app`/`cloudflared`/`backup`), fronted by Cloudflare (Pages ×2 at `ember.vanter.net` + `app.ember.vanter.net`, Tunnel at `api.`, R2 at `cdn.`); backups to GCS; Ops Agent monitoring; migration path to Cloud Run + Cloud SQL documented. **Digital payments + HA out of scope.** Spec `docs/superpowers/specs/2026-08-28-hosted-production-deployment-design.md`, plan `docs/superpowers/plans/2026-08-28-hosted-production-deployment.md` (22 tasks HPD-01..22 in the Task Queue). **Division of labor:** maintainer does the GCP track (Phases 1–3, 5), colleague does the Cloudflare track (Phase 4). Report 278's prometheus/grafana cleanup lives on `feat/hub-packaging-installer`, NOT this branch — cherry-pick aborted on a `PROGRESS.md` conflict; merge B1 to `main` soon to end the divergence.
- **BRANCH EVENT 6 (2026-08-28):** `emb-i18n-08` (49 commits: postgres migration, EMB-MOD, EMB-INV, restaurant onboarding, all of Ember Hub HUB-01 + post-HUB-01 + `hub-license-activation`, EMB-PRINT debugging arc, EMBER-FIX, report 260 lint cleanup) merged to `main` via PR #55 (`34f5c6d`); user merged on GitHub after local 3-part verification passed (backend `./mvnw test`, frontend build+test, printing-agent package). `emb-i18n-08` retired local + remote. **All future work now branches from `main` as short-lived `fix/*` / `feat/*` branches.** **CI blockers found during the merge — FIXED by report 261 (PR #56, `b7fc1b7`):** `lint.yml`'s frontend job ran `npm ci` in a pnpm repo (`setup-node` hard-failed at ~5s); job names (`Lint Java Backend` etc.) never matched the ruleset's required contexts (`lint-backend`/`lint-frontend`/`lint-gateway`); and no job produced `lint-gateway`. Now: frontend job on `pnpm/action-setup@v4` + `pnpm install --frozen-lockfile` + `pnpm run lint`; jobs renamed to the exact required contexts; a trivial passing `lint-gateway` stub added (no `gateway/` module yet — replace its body when EMB-GATEWAY starts). All 3 checks green on PR #56, `mergeStateStatus: CLEAN`. PR-per-task workflow to `main` now works.
- **Flyway V1 consolidated (2026-08-24, on `emb-i18n-08` directly, no branch):** `V2` couldn't
  bootstrap a genuinely empty Postgres (it ALTERs tables only `ddl-auto` ever created, never a
  runnable script) — surfaced by a `flyway:clean` recovery earlier the same day. Generated
  `backend/src/main/resources/db/migration/V1__baseline_consolidated.sql` via `pg_dump
  --schema-only` against the fully-migrated schema (stripped `flyway_schema_history` and the
  psql-only `\restrict`/`\unrestrict` lines pg_dump 16.1+ emits; re-added V4's platform-operator
  seed insert, schema-qualified as `public.platform_operators` since pg_dump's own header sets
  `search_path` to empty). Archived V2–V15 to `backend/src/main/resources/db/migration-archive/`
  (outside Flyway's scanned path, kept for their explanatory comments). Verified end-to-end
  against a throwaway empty database: `V1` alone creates the full schema, `ddl-auto=validate`
  passes with zero mismatches, full app boot + `/platform/auth/login` both work; the real dev DB
  (already baselined at V15) correctly shows `V1` as `Below Baseline` and never re-runs it.
  **Known cosmetic drift:** the dump was taken against a schema rebuilt via `ddl-auto=create`
  during the earlier recovery, so a few constraints early migrations named explicitly (e.g.
  `uk_platform_operators_email`) now carry Hibernate's generated name instead
  (`platform_operators_email_key`) — no functional difference, documented in V1's own header.
- **Bill fix backfill (report 161 → 264):** the 3 pre-existing stuck-OPEN dev bills (ids 1, 2, 10) get VOIDed via the DEV-ONLY `docs/db/dev-void-stuck-bills.sql` (idempotent, guarded, no-ops if the 2026-08-24 recovery already wiped them). Maintainer applies it against dev.
- **Last Completed Task (report 201, bugfix-comanda-modifiers-menu-style):** follow-up to report 200 — user wanted the comanda's modifiers block styled "like a menu" (itemized rows, not a comma-joined line) on a full-card-width red background. `ComandaView.tsx`'s draft (Participants) and sent-order (Historial) item rows now render each `item.modifiers` entry as its own white-text row (`optionName` + `+$priceDelta` when positive) inside a `w-full rounded-md bg-[#8c1717]` block, reusing the page's existing brand red (already used for prices/badges) rather than a new color token. Predecessor: report 200 (comanda modifiers display — `OrderItemDto.modifiers` existed end-to-end but was never rendered on this screen). Implemented on branch `emb-i18n-08`. **Known pre-existing quirk not touched by either fix:** `groupByParticipant`'s dedup-by-`itemId` merges repeat adds of the same menu item into one row with an incremented `cantidad`, but only keeps the FIRST instance's `modifiers` — two adds of the same item with different modifier selections would visually show only one set of modifiers against the combined quantity. Flag if a future task touches this component.
- **Last Completed Task (report 206, restaurant-onboarding Task 5):** wired `useOnboardingGate` into `AdminLayout` (null while loading, `AdminOnboardingWizard` while `needsOnboarding`, else the existing shell). **Found+fixed a real infinite-loop bug in the process** (not prod-shipped before now since the gate wasn't wired until this task): `TopNav`'s own separate `useQuery(['restaurantSettings'])` (`settingStore.ts`) only mounts once the gate's `isLoading` goes false; a fresh observer joining an already-errored, always-stale query triggers a background refetch that flips the shared query's `status` back to `'pending'` mid-flight, which flipped `isLoading` back true, unmounting `TopNav`, detaching its observer, re-settling, remounting, refetching — forever (1000+ fetches/sec in a reduced repro). Fixed by making `useOnboardingGate`'s returned result freeze (`useRef`) the first time the query settles and never recompute after — insulates the gate from background refetches triggered by *any* sibling component sharing the `restaurantSettings` key, present or future. 3/3 new tests + 4/4 existing hook tests (no regression) + 16/16 full suite + `pnpm run build` PASS. Predecessor: report 205.
- **Last Completed Task (report 207, restaurant-onboarding Task 6):** added `waiterTourStore.ts` — Zustand `persist` store with `seenByUserId: Record<string, boolean>`, `hasSeenTour(userId)`/`markTourSeen(userId)`, localStorage key `ember-waiter-tour-storage` (mirrors `localeStore.ts`). Per-user map (not a single global boolean) since two waiters can share a browser/PC. 2/2 new tests PASS, `pnpm run build` PASS. Predecessor: report 206.
- **Last Completed Task (report 208, restaurant-onboarding Task 7):** added 12 ES/EN key pairs to `locales/{es,en}/waiter.ts` for the `react-joyride` waiter tour (grid/panel/action/assign titles+content, next/back/skip/last button labels). EN's two apostrophe-bearing strings had to be written double-quoted (`"it's already occupied"`) since the plan's literal single-quoted snippet would not compile. `pnpm run build` PASS (confirms `satisfies typeof esWaiter` parity). Predecessor: report 207.
- **Last Completed Task (report 209, restaurant-onboarding Task 8):** `WaiterTour.tsx` (new, `pages/waiter/components/`) + 4 stable `id` anchors wired into `Tables.tsx` (grid/panel/action/assign), gated on `useAuthStore().userId` + `waiterTourStore.hasSeenTour`. **The plan's `react-joyride` code was written against the v2 API — the actually-installed `^3.2.0` (Task 1) is a full v3 rewrite** with breaking changes not caught until build/test ran: no default export (`import { Joyride }`, not `import Joyride`); `callback` prop renamed `onEvent`, payload type `EventData` not `CallBackProps` (removed) — `EVENTS`/`STATUS` string constants unchanged; per-step `disableBeacon` renamed `skipBeacon` (without this fix the first step only showed a clickable beacon icon, no title/content text — broke the plan's own 3rd test); `showSkipButton` prop removed, skip visibility now controlled via `'skip'` in the `options.buttons` array; `styles={{ options: {...} }}` doesn't exist — `styles` (CSS) and `options` (behavior/theming incl. `primaryColor`/`buttons`) are separate top-level props. **This is now the reference for any future `react-joyride` usage in this codebase — always verify against `node_modules/react-joyride/dist/index.d.mts` before trusting a plan snippet or memory of the v2 API.** Also fixed an unrelated pre-existing type gap: `TableStatusResponse.tableId` is optional (every generated `components['schemas']` field is, per the established analytics-types convention), so `Tables.tsx`'s `tableIds` prop needed a `.filter((id): id is string => Boolean(id))` after the `.map()` — `tsc -b` caught it, no runtime bug existed. 3/3 new tests + 21/21 full suite (no regression) + `pnpm run build` PASS. Predecessor: report 208.
- **Last Completed Task (reports 210–213, restaurant-onboarding Task 9 + 3 follow-up fixes, all on `emb-i18n-08`):** onboard-09's manual browser verification (done BY THE USER, no `claude-in-chrome` available this session either) caught 2 real bugs the unit tests missed, plus 2 user-requested UX fixes, all same session: (210) `authStore.setAuth`/`logout` now call `queryClient.clear()` — the single app-wide `queryClient` never cleared on identity change, so a stale cross-tenant `['restaurantSettings']` cache entry could freeze `useOnboardingGate` into never showing the wizard for a genuinely fresh restaurant; (211) `useOnboardingGate` no longer freezes on every settle (report 206's fix), only on an **errored** one — the blanket freeze also blocked the wizard's own completion (`needsOnboarding` never flipped true→false after `saveTables`'s `invalidateQueries`, so "Ir al panel" looked broken); (212) new "?" button next to `LanguageSwitcher` in `TopNav` (waiter route only) lets a waiter replay `WaiterTour` on demand via `useUIStore.requestWaiterTour()` — ephemeral, not persisted, separate from `waiterTourStore`'s permanent per-user seen flag; (213) `ModifierGroups.tsx`/`Inventory.tsx` lost their page-local create buttons, now routed through `TopNav`'s existing route-aware action button (its `else-if` chain gained `/admin/modifier-groups`/`/admin/inventory` branches) — matches every other admin list page's convention (`StaffHeader.tsx` etc.), which those two pages had drifted from when first built. Onboarding feature (admin wizard + waiter tour, plan `docs/superpowers/plans/2026-08-24-restaurant-onboarding.md`) is now COMPLETE and manually verified. Postgres migration (branch `ember-postgress-migration`) is COMPLETE, 13/13 plan tasks done, still pending merge back (to `emb-i18n-08` or `main`, user's call) before resuming Ember Hub planning. EMB-GATEWAY stays blocked on GATEWAY-01, deliberately deferred. Mobile-responsive pass from before is still PAUSED, no further task queued there. Realtime is now confirmed working for customer cart sync + waiter participant count; KDS order push and admin inventory low-stock toast are still unverified live (same underlying fix should cover them, but not yet click-through-confirmed).
- **Last Completed Task (report 214, "Catálogo e Inventario" hub, `emb-i18n-08`):** merged the 3 separate admin pages Categorías/Modificadores/Inventario into one hub at `/admin/inventory` with a `CashRegisterBar`-style collapsible sidebar (`InventoryHub.tsx` + `InventoryHubBar.tsx`, new `pages/admin/inventoryHub/`). Sections are real nested routes (`/admin/inventory`, `/categories`, `/categories/:id/items`, `/modifiers`), not local `useState` — user explicitly chose deep-linkable routes over copying `CashRegisterBar`'s exact state-based pattern. `TopNav`'s "+" button now reads the active section from `useUIStore.activeInventoryHubSection` (set by `InventoryHub.tsx` via `useMatch`) instead of `path.includes()`, since 3 routes now share one `TopNav` mount — also the user's explicit choice over path-matching. `FloatingNav` collapsed 3 icons into 1.
- **Last Completed Task (reports 215–218, section-tour rollout, `emb-i18n-08`):** generalized `WaiterTour` into a reusable `SectionTour` (`components/tours/SectionTour.tsx`) + `sectionTourStore.ts` (per-`(sectionId,userId)` seen-flag, replaces the deleted `waiterTourStore.ts`) — any page mounts `<SectionTour sectionId="..." steps={...} />` and gets a replayable Joyride tour + TopNav's "?" button for free, via `useUIStore.activeTourSection`/`requestedTourSection` (TopNav no longer special-cases `isWaiterRoute`, shows "?" whenever any mounted `SectionTour` announces itself). Added `id="topnav-create-button"` to TopNav's own "+" button so any section's tour can point at it directly. Content rolled out to: Catálogo e Inventario's 3 sections (216), Analíticas/Personal/Caja admin+waiter (217), and all 11 Configuración tabs (218, one tour per `SettingsType` via a `TAB_TOUR_KEYS` map keyed against `keyof (typeof dictionaries)['es']['admin']` — no per-tab-component edits needed, all 11 target the same `#settings-tour-content` pane with tab-specific copy; `<SectionTour key={activeSettings} .../>` is deliberately keyed on the active tab to force a remount on tab switch, since a leftover `run:false` from a just-finished tab's tour would otherwise silently suppress the next tab's). Kitchen (KDS) explicitly excluded from this pass (no `TopNav`, would need its own "?" placement — not requested).
- **Last Completed Task (report 219, bugfix-settings-tour-help-button-missing, `emb-i18n-08`):** user manually tested report 218's work and found TopNav's "?" button missing on a fresh `/admin/settings` visit. Root cause: `useSettingsStore.activeSettings` defaulted to `null` and nothing selected a tab on mount (`closeSettings`, which resets to `'BRANDING'`, is dead code — never called) — `Settings.tsx` only mounts `<SectionTour>` when `activeSettings` is truthy, so no tab selected meant no tour registered and no "?" button (and a blank content pane) until the user manually clicked a sidebar tab. Fixed by defaulting `activeSettings` to `'BRANDING'` in `uiStore.ts`, matching `closeSettings`'s own convention.
- **Last Completed Task (report 221, bugfix-topnav-create-button-settings, `emb-i18n-08`):** same fix applied to `/admin/settings`, flagged as a known follow-up after report 220 — `isSettingsRoute` added alongside `isAnalyticsRoute` in the same `null`-rendering branch. Both admin routes with nothing to create now hide the button; `/admin/cash-register` was noticed to have the same unmatched-route gap while building report 222 but was NOT fixed (out of scope for that task) — still open.
- **Last Completed Task (report 222, feat-global-search-panel, `emb-i18n-08`):** TopNav's search input (admin role only) is now a real global search — typing surfaces matching Categorías/Modificadores/Inventario/Personal plus direct shortcuts to Analíticas/Caja/all 11 Configuración tabs, in a dropdown `Popover` panel that opens on click. New `components/ui/popover.tsx` (no Popover primitive existed before — built on the same already-installed `radix-ui` package as `dialog.tsx`/`checkbox.tsx`/`radio-group.tsx`, same missing-shadcn-primitive pattern). Deliberately used `PopoverAnchor` (not `PopoverTrigger`) so clicking inside the already-open input doesn't toggle it closed. `uiStore.searchTerm`/`setSearchTerm` were left untouched — `Staff.tsx`'s existing live in-page filter still works unmodified, the new `GlobalSearchResults.tsx` panel reads the same `searchTerm`. Categories are re-fetched with `size=1000` under a separate `['categoriesSearch']` key (the page-level `['categories', page]` query is paginated at 6). Slash-command syntax (`/...`) was explicitly discussed and deferred to a future task — do not assume it exists.
- **HUB-01 (Portable DB bootstrap + licensing) COMPLETE, all 11 tasks (reports 223–236, `emb-i18n-08`):** `HubProperties`/`HardwareFingerprintService`/`LicenseKeyParser`/`HubStateStore`/`LicenseService` (RSA-signed `license.key`, hardware-locked, 4-day grace window) → `PortableDatabaseBootstrap` (initdb/pg_ctl) → wired into `EmberApplication.main` → `HubBeansConfig`/`GracePeriodInterceptor`/`HubTrayIcon` → manually verified end-to-end against a real EDB Postgres 16.6-1 Windows portable distro. **3 Windows-specific gotchas found+fixed along the way, worth remembering for any future Windows-native subprocess/AWT work:** `initdb -U ember` never creates an app-named database, only `postgres`/`template0/1` (report 233); `pg_ctl start -w`'s child `postgres.exe` processes can inherit the parent's stdout pipe handle, so `Process.getInputStream().readAllBytes()` can hang forever even after `pg_ctl` itself exits — redirect to a file instead of reading the pipe (report 234); Spring Boot forces `java.awt.headless=true` before any profile-scoped property can override it, silently breaking `SystemTray.isSupported()` — must `System.setProperty` before Spring Boot runs (report 235).
- **Last Completed Task (report 237, feat-hub-dashboard-launcher, `emb-i18n-08`):** replaced Ember Hub's immediate auto-boot with a v1 Swing dashboard/launcher (`hub/dashboard/HubDashboard.java`) — status labels (Postgres/Servidor) + Iniciar/Detener/Abrir en navegador/Salir, this is now the primary entry point for the `.exe` the user distributes (`HubTrayIcon` stays as a supplementary quick-access icon once running, untouched). New `HubBootstrapRunner` (`hub/bootstrap/`) extracts the old inline `EmberApplication.bootstrapHub()` license+DB logic minus any `System.exit` (a failure now shows in the dashboard and is retryable, doesn't crash the JVM) and registers a JVM shutdown hook after a successful start — **fixes a real latent bug found+confirmed during this task: no exit path ever stopped the portable Postgres process tree before, every prior HUB-01-11 test session left orphaned `postgres.exe` processes that had to be killed manually.** `PortableDatabaseBootstrap` gained a matching public `stop()`. `HubProperties` gained `serverPort` (`EMBER_HUB_SERVER_PORT`, default 8080) for the "Abrir en navegador" button. **Tech choice, explicitly discussed:** plain Swing (already in the JDK, zero new dependencies) for this v1 pass — a Tauri+webview shell reusing `frontend/`'s real design (matching branding) is the agreed v2 follow-up once this was validated; note `ember_hub.md` §2.3's earlier Tauri rejection was about a *full backend rewrite* in Rust/GraalVM, a different and much larger proposition than a thin shell that only supervises this same unchanged Java process — re-read that distinction before scoping v2, don't treat the two as the same decision. 807/807 PASS. **Live-verified by the user end-to-end, cross-checked from the backend after each click:** Iniciar → real `EmberApplication` PID confirmed owning port 8080; Abrir en navegador → opened the right URL (found: backend doesn't serve the compiled frontend yet, separate un-started task, see below); Detener → port 8080 freed AND every `postgres.exe` process confirmed gone; Iniciar again → clean restart in the same process; Salir → confirmed via `netstat`/`Get-Process` that nothing was left running at all, first time in this whole session that was true after closing Ember Hub. Predecessor: report 236.
- **Last Completed Task (report 238, feat-hub-bundle-frontend, `emb-i18n-08`):** bundled the compiled `frontend/` into the backend jar, mounted at **`/app/**`** (deliberately not root — `GET /kitchen/orders` is both a real protected KDS endpoint and a frontend route; root-mounting would have forced choosing between exposing that API unauthenticated or breaking the route). `application-hub.yml` overrides `server.servlet.context-path` to `/` (hub profile only); `HubWebConfig` gained a `/app/**` resource handler with an `index.html` SPA-fallback `PathResourceResolver`; new `HubSpaRootController` forwards the bare `/app`/`/app/` root server-side (Spring's resource handler rejects an empty path before the custom resolver ever runs) — a forward, not a redirect, so the browser URL stays `/app/` for React Router's `basename` to see the root route. `SecurityConfig` permits `/app/**` (zero collision risk by construction). Frontend: `App.tsx`'s `BrowserRouter` takes `basename` from Vite's built-in `BASE_URL` (regular `pnpm run build` unaffected, stays `/`); new `build:hub` script (`vite build --base=/app/`); `index.html`'s `env-config.js` script tag made relative (Vite doesn't rewrite plain `<script src>` for `--base`, so the absolute form broke under `/app/`). New `ember-hub/build-frontend.ps1` (first file in the packaging folder the spec already named) builds+copies+writes `env-config.js` (computes `window.ENV.EMBW_API_URL`/`EMBW_WS_URL` from `window.location.origin`/`.host` at page-load — no rebuild needed regardless of `EMBER_HUB_SERVER_PORT`). 807/807 backend + 36/36 frontend PASS, regular frontend build unaffected. **Live-verified end-to-end by the user:** `/app/` and deep client routes (e.g. `/app/admin/settings`) serve correctly, `/kitchen/orders` stays 401 (no security regression), the dashboard's "Abrir en navegador" now shows the real Ember UI, and a login attempt correctly reached the real backend (401 — expected, this Hub's DB is genuinely empty, not a network/CORS failure). **Real gap surfaced, NOT fixed, needs its own design session:** there's no path today from "customer activates a Hub license" to "a Restaurant + admin User exist in the local DB" — `LicensePayload` carries only `restaurantId`+`issuedAt`, no admin credentials, and `LicenseService.validateOrActivate()` never touches Postgres. The frontend's "Registrarse" button is the **customer** self-registration flow (tenant-less by design, EMB-i18N era), not admin provisioning — showing it on a fresh Hub install is actively misleading, flagged by the user directly. Also unresolved: how does the platform-operator console's (`/console`, EMB-PC) restaurant/admin creation map to a Hub's own offline local Postgres? Predecessor: report 237.
- **Last Completed Task (report 239, Task 1: hub-license-activation entity, `emb-i18n-08`):** first task of the 10-task `docs/superpowers/plans/2026-08-25-hub-license-activation/` plan — created `HubActivation` JPA entity (`com.vanter.ember.licensing.model`), `HubActivationRepository` Spring Data interface, Flyway migration `V2__hub_activations.sql` (creates table with unique `restaurant_id` constraint), and `HubActivationRepositoryTest` (2 tests: empty result + saved activation). The entity stores server-side records of Hub license activations `(restaurantId, hardwareFingerprint, activatedAt)` — consumed by Task 4's `HubActivationService` to distinguish legitimate retries (same hardware) from license copies (different hardware), layering server-side validation on top of `LicenseService`'s client-side hardware lock. All code taken verbatim from the brief; discovered and fixed the mandatory `@Import(TenantIdentifierResolver.class)` requirement for `@DataJpaTest` when `@TenantId` entities exist project-wide (per PROGRESS.md report 56 note, required even though `HubActivation` itself has no `@TenantId`). 2/2 focused tests PASS, 809/809 full suite PASS (807 pre-existing + 2 new). Predecessor: report 238.
- **Last Completed Task (report 240, Task 2: LicenseIssuingService, `emb-i18n-08`):** second task of the 10-task `docs/superpowers/plans/2026-08-25-hub-license-activation/` plan — created `LicenseIssuingService` (plain class, not @Service in this task — Spring wiring deferred to Task 3), consumes base64-encoded RSA PKCS8 private key, derives public key from CRT parameters (modulus + publicExponent), provides `issue(UUID restaurantId): String` to sign license keys and `publicKey(): PublicKey` for verification. Implemented as test-first: wrote `LicenseIssuingServiceTest` (2 tests: round-trip sign/verify, malformed input throws), then service implementation, then wired `hub.license.private-key` config into `application.yml` (reads `${HUB_LICENSE_PRIVATE_KEY}`) and documented the generation command in `.env.example`. Initially marked service as @Service + @Value, which caused Spring context failures in the test suite since HUB_LICENSE_PRIVATE_KEY is empty in tests and nothing should construct this bean yet — fixed by removing Spring annotations, keeping it a plain class until Task 3 wires it into the Spring context. 2/2 focused tests PASS, 811/811 full suite PASS (809 pre-existing + 2 new). Predecessor: report 239.
- **Last Completed Task (report 241, Task 3: hub-license issuance endpoint, `emb-i18n-08`):** third task of the 10-task `docs/superpowers/plans/2026-08-25-hub-license-activation/` plan — wired Task 2's `LicenseIssuingService` into the existing `PlatformRestaurantService`/`PlatformRestaurantController`: new `issueHubLicense(UUID restaurantId, String operatorEmail): String` (resolves the operator, 404s if the restaurant doesn't exist, signs via `LicenseIssuingService.issue`, writes a `HUB_LICENSE_ISSUED` `PlatformAuditLog` row) and `POST /platform/restaurants/{id}/hub-license` (`text/plain` body). **Plan drift found+fixed:** the brief's literal field (`private final LicenseIssuingService licenseIssuingService;` under `@RequiredArgsConstructor`) broke `SecurityAuditTest` (74/74 errors) — Lombok never copies a field-level `@Lazy` onto the constructor parameter it generates, so the always-scanned `LicenseIssuingService` bean (unlike `HubBeansConfig`'s beans, which are `@Profile("hub")`-gated and simply absent otherwise) was constructed eagerly on every context boot, throwing on the unset `HUB_LICENSE_PRIVATE_KEY` placeholder. Fixed by replacing `@RequiredArgsConstructor` with a manual constructor carrying `@Lazy` directly on that one parameter — confirmed via decompiled bytecode that only the manual-constructor placement actually emits `RuntimeVisibleParameterAnnotations`. 2 new service tests + 3 new controller tests PASS, 816/816 full suite PASS (811 + 5 new, matches the brief's own expected count). Predecessor: report 240.
- **Last Completed Task (report 242, Task 4: HubActivationService, `emb-i18n-08`):** fourth task of the 10-task `docs/superpowers/plans/2026-08-25-hub-license-activation/` plan — created `HubActivationRequest`/`HubActivationResponse` DTOs and `HubActivationService` (`@Service`). The service's `activate(request): response` verifies the license signature (RSA via `LicenseKeyParser.parseAndVerify` + `LicenseIssuingService.publicKey`), resolves the restaurant and its first ADMIN user, enforces server-side hardware lock (idempotent on same fingerprint, 403 "otra PC" on different hardware), and returns restaurant + admin credentials to the Hub. Consumed by Task 5's controller (not yet wired). All code taken verbatim from the brief. 6/6 focused tests PASS, 822/822 full suite PASS (816 pre-existing + 6 new). Predecessor: report 241.
- **Last Completed Task (report 243, Task 5: public activation endpoint, `emb-i18n-08`):** fifth task of the 10-task `docs/superpowers/plans/2026-08-25-hub-license-activation/` plan — wired Task 4's `HubActivationService` into a new public-facing Spring REST controller `HubActivationController` at `POST /hub-activations`. Added `@ExceptionHandler(InvalidLicenseException)` to `GlobalExceptionHandler` (400 BAD_REQUEST), and `.requestMatchers("/hub-activations").permitAll()` to `SecurityConfig` (the Hub's one-time activation call, no JWT required — authenticates via the license signature itself; confirmed via diff this is the ONLY new `permitAll` route added). New `HubActivationControllerTest` with 5 test cases covering happy path + all error conditions (malformed JSON, invalid license, already activated elsewhere, restaurant not found). All code taken verbatim from the brief. **Plan drift found+fixed:** Step 7's full-suite run surfaced a pre-existing Task 4 bug — `HubActivationService`'s `@RequiredArgsConstructor` autowired `LicenseIssuingService` (a `@Lazy`-class-level bean) without `@Lazy` on the injection point, forcing eager construction during `preInstantiateSingletons()` and throwing on the unset `HUB_LICENSE_PRIVATE_KEY` placeholder outside the `hub` profile — 92 errors across every full-context test (`SecurityAuditTest`, `EmberApplicationTests`, `MinioConfigTest`, etc.). Confirmed via `git stash` that this predates this task (already broken at Task 4's `c6e8427`, despite report 242 claiming 822/822). Fixed with the same manual-constructor-plus-`@Lazy`-parameter pattern already used by `PlatformRestaurantService` (report 241) — Lombok never copies a field-level `@Lazy` onto its generated constructor. 5/5 focused tests PASS, 828/828 full suite PASS, 0 failures/errors (one more than the brief's predicted 827 — a pre-existing unrelated test in this environment). Predecessor: report 242.
- **Last Completed Task (report 244, Task 6: HubProvisioningRunner, `emb-i18n-08`):** sixth task of the 10-task `docs/superpowers/plans/2026-08-25-hub-license-activation/` plan — `HubProperties` gained an `activationUrl` field (`EMBER_HUB_ACTIVATION_URL`, default `""`); new `com.vanter.ember.hub.provisioning` package with `HubProvisioningException` and `HubProvisioningRunner` (`@Component @Profile("hub") implements ApplicationRunner`). On boot: no-ops if `RestaurantRepository.existsById(state.restaurantId())` is already true; otherwise POSTs `{licenseKey, hardwareFingerprint}` to Task 5's `POST /hub-activations`, then seeds a local `Restaurant` (reusing the license's `restaurantId` as its own `@Id`) + admin `User` from the response. Any failure (blank `activationUrl`, unreachable server, non-200) becomes a `HubProvisioningException` — consumed by Task 7's broadened `HubDashboard` catch clause, not yet wired. All code taken verbatim from the brief, no drift against current `HubStateStore`/`HardwareFingerprintService`/`Restaurant`/`User`/`RestaurantRepository`/`UserRepository` signatures. **Post-review fix wave (same task, 2 Important findings in the brief itself):** (1) the brief's own 3 tests never exercised the success path — added `run_successfulActivation_savesRestaurantAndAdminUser`, stubbing `POST /hub-activations` with a real `com.sun.net.httpserver.HttpServer` on an ephemeral port (JDK-included, no new dependency) and asserting the captured `Restaurant`/`User` via `ArgumentCaptor`; (2) the outbound `HttpClient.send(...)` call had no connect/request timeout, meaning a silently-packet-dropping firewall on real customer hardware could hang the whole synchronous Spring Boot startup forever with no boot-error message — fixed with `HttpClient.newBuilder().connectTimeout(Duration.ofSeconds(10))` + `.timeout(Duration.ofSeconds(15))` on the request; a timeout now surfaces as `HttpTimeoutException`, already covered by the existing `IOException` catch/rewrap. 5/5 focused tests PASS (1 `HubPropertiesTest` + 4 `HubProvisioningRunnerTest`), 831/831 full suite PASS, 0 failures/errors (830 prior total + 1 new test method). Predecessor: report 243.
- **Last Completed Task (report 245, Task 7: broaden dashboard error handling, `emb-i18n-08`):** seventh task of the 10-task `docs/superpowers/plans/2026-08-25-hub-license-activation/` plan — widened `HubDashboard.java`'s catch clause from `catch (InvalidLicenseException | PortableDatabaseException e)` to `catch (Exception e)`, ensuring that new `HubProvisioningException` failures from Task 6's `HubProvisioningRunner` (an `ApplicationRunner` bean that runs during Spring startup) are caught and displayed in the same retry-friendly error dialog as the pre-existing bootstrap failures. Removed the two now-unused imports (`InvalidLicenseException`, `PortableDatabaseException`). Surgical one-clause change + import cleanup, no new tests needed (Swing desktop paths are manually verified in HUB-01-11 verification, same deferral used by HUB-01-10). 831/831 PASS (unchanged, no new test methods). Predecessor: report 244.
- **Last Completed Task (report 246, Task 8: console-license-button, `emb-i18n-08`):** frontend consumer of Task 3's `POST /platform/restaurants/{id}/hub-license` endpoint. Added `platformRestaurantService.issueHubLicense(id)` method to `platformApi.ts` (POST to `/platform/restaurants/{id}/hub-license`, returns `text/plain` license key), wired into `ConsoleRestaurantDetail.tsx` as a new `useMutation` with onSuccess blob-download handler (creates `Blob`, Object URL, triggers download as `license.key`, cleans up). UI: replaced single "Suspender/Reactivar" button with a flex wrapper containing the new outline "Emitir licencia Hub" button (disabled + "Emitiendo..." text while pending) + the existing Suspender/Reactivar button. `pnpm run build` PASS (no TypeScript errors); `pnpm run test:run` 36/36 PASS (genuinely observed, 61.15s wall time, no new tests per brief). Predecessor: report 245.
- **Last Completed Task (report 247, Task 9: hide-registrarse-hub-build, `emb-i18n-08`):** ninth task of the 10-task `docs/superpowers/plans/2026-08-25-hub-license-activation/` plan — frontend-only, tiny: added `const isHubBuild = import.meta.env.BASE_URL !== '/'` to `Login.tsx` (same BASE_URL signal already used in `App.tsx`'s router basename to detect the Hub variant), then wrapped the "Registrarse" button with `{!isHubBuild && (...)}`. The "Registrarse" link is a customer self-registration flow (join-table/collaborative cart), misleading on the Hub's login screen where the admin is always pre-provisioned (Task 6: `HubProvisioningRunner`). `pnpm run build` PASS (regular variant, BASE_URL=`/`, button still shown); `pnpm run build:hub` PASS (Hub variant, BASE_URL=`/app/`, button hidden); `pnpm run test:run` 36/36 PASS (genuinely observed, 57.39s wall time, no new tests per brief). Predecessor: report 246.
- **Last Completed Task (report 248, final review fix wave for `hub-license-activation`, `emb-i18n-08`):** fixed the plan's one Critical bug — `Restaurant.builder().id(...).build()`+`save()` throws against real Hibernate (`GenerationType.UUID` rejects a caller-assigned id: `save()` calls `merge()`, not `persist()`, once id is non-null); fixed via a native `RestaurantRepository.insertWithId` query + `findById`, wrapped in a `TransactionTemplate` (NOT `@Transactional` — `HubProvisioningRunner.run()` calls the seeding method via `this`, and Spring AOP's proxy-based `@Transactional` is a no-op on self-invocation; `TransactionTemplate` demarcates explicitly instead). Also: `HubActivationController` gained `@Profile("!hub")` (was unintentionally exposed unauthenticated on every Hub's own LAN); `/hub-activations` added to `RateLimitProperties.paths`; `HubProvisioningRunner`'s Jackson mapper now tolerates unknown response fields and its license-file-read/response-parse failures rethrow as Spanish `HubProvisioningException`s without leaking response-body content (which could contain `adminPasswordHash`); `.env.example` corrected (`HUB_LICENSE_PRIVATE_KEY` is validated lazily, not at boot); report 246 corrected (stale "timed out" claim → genuine 36/36 PASS, stale SHA → `09e7909`). New test: `RestaurantRepositoryInsertWithIdTest` (`@DataJpaTest`, real Hibernate — the mocked `HubProvisioningRunnerTest` could not have caught the original bug).
- **Last Completed Task (report 249, PRINT-07 debugging subtask 1: fix print-agent ACK, `emb-i18n-08`):** while debugging why a real printer never showed `PRINTED`/`ERROR` (started from the user's `EPSON L310`/thermal-printer complaint), DB inspection showed every `print_jobs` row stuck at `SENT`/`PENDING`, `last_error` always empty — the ACK never lands. Root-caused (systematic-debugging, DB query + code trace before any fix) two stacked bugs: (1) `WebSocketConfig`'s `setApplicationDestinationPrefixes("/app", "/app/print-agent")` — Spring strips the FIRST matching prefix, so the agent's `SEND /app/print-agent/ack` resolved to `/print-agent/ack`, not `PrintAgentAckController`'s `@MessageMapping("/ack")`; fixed by remapping to `/print-agent/ack`. (2) `PrintAgentChannelInterceptor` only bound `TenantContextHolder` around the SUBSCRIBE frame — `clientInboundChannel` dispatches `@MessageMapping` handlers via a pooled executor, so the ACK's SEND ran on a different thread with no tenant bound, silently scoping `PrintDispatchService.handleAck`'s `findById` to the empty `NO_TENANT` partition; fixed by implementing `ExecutorChannelInterceptor` (`beforeHandle`/`afterMessageHandled`) on the same interceptor bean, which Spring guarantees runs on the handler's own thread. New `PrintAgentAckFlowIntegrationTest` (real STOMP round trip, same pattern as `PrintAgentTokenFlowIntegrationTest`) confirmed RED pre-fix, GREEN post-fix; full backend suite still passes. **Also discovered, not yet fixed (uncommitted local changes predating this session — WINDOWS_QUEUE connection type, `V3__printer_config_windows_queue.sql`, `WindowsPrintQueueSender`):** the installed `EPSON L3210 Series` (not "L310" — no such queue exists on this machine) is inkjet/driver-only, incompatible with `WindowsPrintQueueSender`'s raw-ESC/POS-via-RAW-datatype approach; next subtask is a driver-rendered (`PrinterJob`/`Printable`) path to validate the chain against it before returning to the thermal printer. Predecessor: report 248.
- **Last Completed Task (report 250, PRINT-07 debugging subtask 2: agent dispatch logging, `emb-i18n-08`):** extracted `Main.java`'s inline job-handler lambda into a new testable `PrintJobDispatcher` (decoupled from `StompSession` via an `AckCallback` functional interface). Logs every step (job received, printer matched, print success/failure) to stdout/stderr — this module has no logging framework, kept consistent with `Main`'s pre-existing `System.err.println` convention. When no configured printer matches a job's role (previously a silent no-op, indistinguishable from a printer that printed but forgot to ack), it now sends `PrintJobAck(result="ERROR", printerConfigId=null, error="...role X...")`, so `print_jobs.last_error` finally explains why a job never printed. 3 new tests (real `ServerSocket` for the NETWORK success path, `WINDOWS_QUEUE` w/ bad queue name for the error path, role-mismatch for the no-match path — no Mockito in this module); full `printing-agent` suite + `mvn clean package` (the real shaded jar) both PASS. **Found, not fixed, flagged for later:** `NetworkPrinterSender` can falsely ack `PRINTED` for an unreachable NETWORK printer — `escpos-coffee`'s `TcpIpOutputStream` (confirmed via `javap` disassembly) connects the real TCP socket on a detached background thread fed by an internal pipe, so the caller's writes always "succeed" and any connect failure only reaches a logged `UncaughtExceptionHandler`, never the caller's `catch`. Not blocking today's `WINDOWS_QUEUE`-only setup, but a real bug for any future NETWORK-type printer — see report 250. Predecessor: report 249.
- **Last Completed Task (report 251, PRINT-07 debugging subtask 3: refresh printers per job, `emb-i18n-08`):** `Main.java` used to fetch `GET /printing/agents/me/printers` once per connection and reuse that snapshot for the STOMP session's entire lifetime (hours/days) — a printer added/edited/deactivated mid-session was invisible until the agent's next reconnect. New `PrintJobHandler.handle(job, ackCallback)` refetches printers fresh on every job before calling `PrintJobDispatcher` (report 250); a failed refetch acks `ERROR` with the reason instead of throwing. The connect-time fetch is kept only for its fail-fast/log-count value, no longer as the routing source of truth. 2 new tests (`mockwebserver3`, same pattern as `PrinterConfigClientTest`): 2 dispatched jobs trigger 2 separate HTTP fetches (not a cached one); a failed fetch acks `ERROR`. Full `printing-agent` suite + `mvn clean package` PASS. Predecessor: report 250.
- **Last Completed Task (report 252, WINDOWS_QUEUE connection type + PRINT-07 manual-verification bugfixes, `emb-i18n-08`):** committed, as its own isolated unit, a batch of work completed in an earlier (pre-this-conversation) session that had sat uncommitted while reports 249–251 kept building on top of it. Adds `WINDOWS_QUEUE` as a third `ConnectionType` (`V3` migration, `windows_queue_name` column) for printers that enumerate as a native Windows printer-queue USB device rather than a serial/COM port — new agent-side `WindowsPrintQueueSender` (RAW datatype submission), full admin UI + locale wiring. Also ships 3 real bugs found+fixed during that session's manual verification: (1) `PrintAgent` incorrectly carried `@TenantId`, breaking the `permitAll` token-exchange endpoint's cross-tenant agent lookup — removed, tenant now set explicitly everywhere; (2) `SecurityConfig`'s JWT filter treated every JWT subject as a user email, 500ing on print-agent tokens — now checks the `typ` claim first; (3) `PrintingEventListener` used plain `save()` then immediately read the Hibernate-`@TenantId`-generated `tenantId` before it existed (only populated at flush) — switched to `saveAndFlush`. Full backend + frontend + printing-agent suites re-verified PASS before this commit (frontend hadn't been re-run this session). Predecessor: report 251.
- **Last Completed Task (report 253, PRINT-07 debugging subtask 4: RAW\|DRIVER render mode, `emb-i18n-08`):** new `PrinterRenderMode` (`RAW`/`DRIVER`) on `WINDOWS_QUEUE` printers (`V4` migration, `render_mode NOT NULL DEFAULT 'RAW'` — existing printers unaffected). `WindowsPrintQueueSender.print()` branches: `RAW` keeps the existing raw-ESC/POS-via-spooler path; new `DRIVER` builds a `Printable` (factored into testable `renderToPrintable`, verified with an off-screen `Graphics2D`, no real printer needed) submitted through `java.awt.print.PrinterJob`, letting the queue's actual Windows driver rasterize plain text — needed for printers with zero ESC/POS support. Full stack wired: entity/DTOs/service/self-controller (backend), `PrinterConfigDto` (agent), `AddPrinterModal.tsx` mode selector + `PrintingSettings.tsx` list display + both locales (frontend). **Why:** the user's only currently-connected printer is an `EPSON L3210 Series` (inkjet, driver-only — confirmed no "L310" queue exists on this machine), so validating the print chain end-to-end needs a path this hardware can actually execute. Backend/agent/frontend suites all PASS (backend + printing-agent full, frontend build+test+lint — one pre-existing unrelated lint hit near a touched file, not new). **Not yet verified: an actual physical print to the L3210** — next manual step. Predecessor: report 252.
- **Last Completed Task (report 254, PRINT-07 debugging subtask 5: E2E verification vs real EPSON L3210, `emb-i18n-08`):** manual verification, no code changes. Found+fixed a dev-environment-only blocker: the real dev DB's schema is driven by `ddl-auto=update` on every boot, NOT Flyway (`flyway_schema_history` has one row, the `V15` baseline from 2026-08-24's consolidation — every migration since, `V2`–`V4`, has a version number BELOW that baseline, so Flyway silently treats them all as already-applied and skips them, every boot; **unaddressed, flagged for a future look**). `ddl-auto=update`'s auto-generated `ALTER ... ADD COLUMN render_mode NOT NULL` (no default) failed against the already-populated table; manually ran `V4`'s actual SQL (which does specify `DEFAULT 'RAW'`) directly against the dev DB to unblock. Restarted a stale IntelliJ-debug backend (pre-dated today's code) with `./mvnw spring-boot:run`; confirmed the print-agent's existing stored API key still authenticates; pointed the existing `PrinterConfig` row at the real `EPSON L3210 Series` queue (confirmed via `Get-Printer` — no "L310" queue exists on this machine) with `renderMode: DRIVER`; ran the real agent jar. **Full chain confirmed working on real hardware for the first time this session:** stuck `PENDING` job auto-flushed on agent connect, matched printer, `PrinterJob` submitted with no exception, ACK reached the backend, job flipped to `PRINTED` with no `last_error` — and **the user confirmed real, correctly-formatted paper came out of the L3210.** Closes the PRINT-07 debugging arc opened at this session's start. Predecessor: report 253.
- **Last Completed Task (report 256, hub-license-activation Task 10: manual E2E verification, `emb-i18n-08`):** verified the full license→tenant→admin chain across two live Spring Boot processes (cloud console backend + a real portable-Postgres test Hub, reusing HUB-01-11's extracted Postgres 16.6 binaries under a fresh `C:\ember-hub-test-2\` data dir). **Found+fixed a real tooling gotcha (not a code bug):** this machine's mingw64 `openssl genpkey -outform DER` does NOT emit true PKCS8 despite the documented default — it silently produces raw PKCS1, which `LicenseIssuingService` rejects (`algid parse error, not a sequence`), causing "Emitir licencia Hub" to silently do nothing (no failure toast exists on that button, flagged as a residual gap). Fix: pipe through `openssl pkcs8 -topk8 -nocrypt -inform DER -outform DER` and confirm via `openssl asn1parse` before trusting any future openssl-generated key on this toolchain. All 6 verification steps then passed: license issued from `/console`, test Hub activated and provisioned `Restaurant`+admin `User` locally (confirmed via direct `psql` against the Hub's own Postgres), admin login succeeded through `/app/`, and a Detener→Iniciar cycle confirmed true idempotency (`HubProvisioningRunner` logs "already provisioned locally, skipping activation call.", zero orphaned `postgres.exe` processes, zero second `POST /hub-activations` on the cloud backend's own log). The `hub-license-activation` plan (Tasks 1–10) is now COMPLETE. **Side effects left in place, not committed (both gitignored):** the real dev `.env`'s `HUB_LICENSE_PRIVATE_KEY` is now a real (throwaway-for-dev) signing key, was previously empty — any Hub license issued from this backend from now on is signed by it, and needs the matching `hub-public-key.der` (generated this session, NOT the same as HUB-01-11's original throwaway keypair) to activate against. `backend/src/main/resources/static/` was also rebuilt fresh (`pnpm run build:hub`) to guarantee it reflects every commit through Task 9's hide-Registrarse change. Predecessor: report 255.
- **Last Completed Task (report 255, PRINT-07 debugging subtask 6: fix NetworkPrinterSender false-positive, `emb-i18n-08`):** fixed the bug flagged (not fixed) in report 250 — `NetworkPrinterSender.print()` no longer uses `escpos-coffee`'s `TcpIpOutputStream` (connects the real socket on a detached background thread fed by an internal pipe, so the caller's writes always "succeeded" regardless of actual TCP connection state, silently acking `PRINTED` for an offline/unreachable printer). Now opens a plain `java.net.Socket` directly, connects synchronously with an explicit 5s timeout (previously none), hands `EscPos` the socket's real `OutputStream` — any failure is now a normal `IOException` on the calling thread. New test confirmed RED (nothing thrown, silently "succeeded") pre-fix, GREEN post-fix. User-approved follow-up from this session's point-by-point review of what's left before the print feature "just works" — not in active use today (current setup is `WINDOWS_QUEUE`), but was a real correctness gap for any future `NETWORK`-type printer. Full `printing-agent` suite + `mvn clean package` PASS; real agent process (still running from report 254's E2E verification) restarted against the rebuilt jar, reconnected cleanly, L3210 (`WINDOWS_QUEUE`/`DRIVER`) unaffected. Predecessor: report 254.
- **Last Completed Task (report 259, EMBER-FIX cash shift expiry & forced daily close — EMBER-FIX-01..12 squashed, `emb-i18n-08`):** full plan `docs/superpowers/plans/2026-08-28-cash-shift-expiry-forced-close.md` landed as ONE atomic commit. Backend: pure dependency-free `CashShiftDeadlineService` (`computeExpiresAt` = weekday `closeTime` + 2h `GRACE`, rolls to next day when not after `openedAt`; 12h `CLOSED_DAY_FALLBACK` on closed-day / no-schedule-entry / blank|malformed|null `closeTime`, never throws; `isOverdue`; `prolong` = +1h off max(deadline, now)); `V5__cash_shift_expiry.sql` adds `expires_at`/`prolonged_until`/`prolonged_by`/`prolong_count NOT NULL DEFAULT 0` + backfills OPEN shifts to `opened_at + 12h`; `CashShift` gains those fields + derived `effectiveDeadline()`/`businessDay()`. Enforcement (HTTP 409, stable `code:"CASH_SHIFT_OVERDUE"` via `GlobalExceptionHandler`) at `CashShiftService.recordMovement` + `PaymentService.registerPhysicalPayment` only — digital + table service untouched (`verifyNoInteractions(deadlineService)`). `prolongShift` (pessimistic lock, non-OPEN→`IllegalStateException`, publishes `CashShiftProlonged` → `CashRegisterWebSocketListener` broadcast) behind `POST /cash-shifts/{id}/prolong` (WAITER/ADMIN). `CashShiftResponse` +5 trailing fields (`overdue` recomputed per serialization). Frontend: pure `deriveCashShiftAlert` state machine (`STALE`→`OVERDUE`→`PRE_WARNING`→`IDLE`); `<CashShiftSentinel/>` (60s poll, 30s tick, 3 `AlertDialog`s: pre-warning/overdue offer Prolong·Close·Later[snooze 15min], stale is non-dismissable + lists movement/payment counts) mounted in Waiter + Admin (non-onboarding) shells; `FloatingNav` non-blocking overdue-logout confirm; `CashRegister` disables the movement button while overdue; `TableInformation` physical-payment `onError` recognises the 409 code; `cashShiftService.prolong` + 5 hand-added `backend-types.ts` fields (live :8080 OpenAPI stale, matches platform/analytics precedent); i18n es+en (`waiter.ts` + `common.ts`). **Verification caught + fixed 1 regression from EMBER-FIX-09:** `AdminLayout.test.tsx`'s `@/lib/api` mock lacked `cashShiftService` (the mounted sentinel imports it) — 2 tests failed with `No "cashShiftService" export ... on the mock`; fixed by adding `cashShiftService: { current/detail/prolong }` to the mock (EMBER-FIX-09 only ran build+lint, not `pnpm run test`). `dismissedUntil` was `useRef`→`useState` (plan's verbatim ref-in-render pattern threw 2 `react-hooks` lint errors). "Abrir turno de hoy" prefill dialog deliberately OUT for v1 (`cashShiftOpenTodayButton` key added, unwired). Predecessor: report 258.
- **Last Completed Task (report 260, fix-frontend-lint-errors, `emb-i18n-08`):** cleared all **25 pre-existing frontend eslint errors** — the `lint-frontend` CI check (a required status check in `main`'s repo ruleset, contexts `lint-backend`/`lint-frontend`/`lint-gateway`) was blocking PR #55 (`emb-i18n-08` → `main`). 17 real fixes: `eslint.config.js` gained `no-unused-vars` `{ ignoreRestSiblings: true, ^_ patterns }` (clears the 4 `render={({ field:{ value, ...rest }})}` cases); `websocket.ts` `any`→`StompSubscription`; `sessionStore.updateSession` `any`→`Partial<sessionResponse>`; `settingStore` fn renamed `→ useSettingStore` (rules-of-hooks, 1 importer `TopNav.tsx`); **2 genuine bugs** — `ParticipantsPopUp.tsx` had `useState`/`useTranslation` after an early `return null` (moved above guard), `OrdersDisplay.tsx` `new Date(a?.createdAt!)`→`?? 0`; `avatar.tsx` empty interface→`type`; `Menu.tsx` `Number`→`number` + init `useEffect`→converging render-body `setActiveCategory`; `TableInformation.tsx` comma-operator→2 statements. 8 targeted `eslint-disable-next-line` w/ inline reasons where the pattern is deliberate & a "correct" refactor carries more risk than value: `useOnboardingGate` ×2 (`react-hooks/refs` — documented render-phase latch, report 206), 4× `set-state-in-effect` (otpInput / SectionTour / RefundPaymentModal / EditMenuModal — all prop→state seeding), `uiStore` ×2 (`no-explicit-any` on `modalPayload` — heterogeneous per-modal bag, discriminated-union refactor tracked separately). No behavior change. `pnpm run lint` exit 0 (0 errors / 17 warnings, no `--max-warnings` gate) + `pnpm run build` PASS + `pnpm run test` 41/41 PASS. Predecessor: report 259.
- **Last Completed Task (report 261, fix-ci-lint-workflow, branch `fix/ci-lint-workflow` → PR #56 → `main` `b7fc1b7`):** repaired `.github/workflows/lint.yml` — first task done under the new `fix/*`-branch-from-`main` workflow. 3 defects: (1) `lint-frontend` ran `npm ci` + a nonexistent `package-lock.json` in a pnpm repo → `setup-node` hard-failed at ~5s → now `pnpm/action-setup@v4` v10 + `pnpm install --frozen-lockfile` + `pnpm run lint`; (2) job `name:`s (`Lint Java Backend`/`Lint React Frontend`) never matched the ruleset's required contexts (`lint-backend`/`lint-frontend`/`lint-gateway`) → renamed to exact match; (3) nothing produced `lint-gateway` → added a trivial passing stub (no `gateway/` module yet; replace when EMB-GATEWAY starts). All 3 checks green on PR #56, state `CLEAN`. No app code touched. Predecessor: report 260.
- **Last Completed Task (report 262, fix-user-role-ui, branch `fix/user-role-ui` → PR):** added the missing frontend caller for `PATCH /admin/users/{userId}/role` (`UserAdminController.updateRole`, ADMIN-only, existed since EMB-STAFF, never wired). `api.ts`: `staffService.updateRole(userId, role)` + exported `StaffRole` type (`UpdateUserRoleRequest['role']` minus `CUSTOMER`). `EditStaffModal.tsx`: new **Rol** `<Select>` (WAITER/KITCHEN/ADMIN, reuses `CreateStaffModal`'s pattern + `ROLE_LABELS`); zod `role: z.enum([...])`; `values.role` seeds from `member.role` (fallback `WAITER`); `mutationFn` now async — always `updateProfile(HR fields)`, then `updateRole` **only if role changed**; select `disabled` when editing your own account (anti self-demotion footgun, not a security control). No new i18n (`roleLabel` already es+en). `pnpm run lint` exit 0 / `build` PASS / `test` 41/41. Predecessor: report 261. **Merged to `main` `e995367` (PR #57).**
- **Last Completed Task (report 263, fix-flyway-baseline, branch `fix/flyway-baseline` → PR):** the dev DB's `flyway_schema_history` still carried the old single-row **V15** baseline (from the 2026-08-24 `flyway:clean` recovery), so the renumbered `V2`–`V5` (all `< 15`) were skipped every boot and applied by hand instead (reports 254, 259). Fix = re-baseline dev at **V5** (truthful: the dev schema already physically contains everything through V5). New DEV-ONLY script `docs/db/dev-rebaseline-v5.sql` (guards the expected stale state, `DELETE`s history, inserts one `BASELINE version='5'` row; runbook: stop backend → `pg_dump` backup → `psql -f` → restart expecting "No migration necessary"). Lives in `docs/`, NOT `db/migration/`. `application.yml` flyway comment updated (no value change). Fresh-install path (empty DB runs V1→V5) untouched — it already works. Rejected: per-migration rows (needs hand CRC32, no `flyway repair` — no maven plugin), renumber V2–V5→V16–V19 (would fail on dev, cols exist), re-consolidate V1 (breaks V1 checksum). **Queued follow-up:** dev still runs `ddl-auto=update` (masks Flyway); switching to `ddl-auto=validate` is its own task — booting with `validate` will surface the V1-header "cosmetic drift" (Hibernate-generated vs hand-authored constraint names), each needing a reconciliation migration. Backend `./mvnw test` PASS. Predecessor: report 262. **Merged to `main` `0bed468` (PR #58).**
- **Last Completed Task (report 264, fix-stuck-bills-cleanup, branch `fix/stuck-bills-cleanup` → PR):** LAST Fase-1 minor gap. New DEV-ONLY `docs/db/dev-void-stuck-bills.sql` — prints before-state of bills 1/2/10 (+ their payment/split counts), `UPDATE`s only rows present AND still `OPEN` to `status='VOIDED'` / `voided_at=now()` / a `void_reason` backfill note, prints after-state. Idempotent, safe if the 2026-08-24 `ddl-auto=create` recovery already wiped them. `VOIDED` (not `PAID`, not `DELETE`) because it's the terminal state the re-billing guard `findBySessionIdAndStatusNot(sessionId, VOIDED)` is built around and the honest label (never paid). No app code — docs/ SQL only. Predecessor: report 263.
- **License Heartbeat HEARTBEAT-01..06 (reports 265–270, `feat/hub-license-heartbeat`) — per-task detail in the checkbox queue below:** `HubState.suspendedSince` (4-arg canonical + 3-arg back-compat ctor) → `LicenseService.isSuspendedGraceExpired`/`recordSuspended` + `Duration suspendedGrace` 6th ctor arg + `HubProperties.heartbeatUrl`/`suspendedGraceHours` → cloud `HubHeartbeatService` + DTOs (`@Lazy LicenseIssuingService` on hand-written ctor param, reports 241/243) → cloud `POST /hub-heartbeat` (`permitAll`, `@Profile("!hub")`, in `RateLimitProperties.paths`) → Hub `HeartbeatScheduler` (`@Profile("hub")` `@Scheduled`, all exceptions swallowed, `@EnableScheduling` on `HubBeansConfig`, interval prop in `application-hub.yml`) → `GracePeriodInterceptor` also blocks on `isSuspendedGraceExpired` with a distinct `license_suspended` 403 body. Suites 870→891.
- **Last Completed Task (report 271, HEARTBEAT-07, branch `feat/hub-license-heartbeat`):** final task of the 7-task License Heartbeat plan — wiring only, no new logic. `HubDashboard.java` gained a 4th read-only status label (`GridLayout(3,1)→(4,1)`, frame `190→215` tall) showing `Licencia: sin estado local` / `Licencia: SUSPENDIDA (desde hace <n>)` / `Licencia: OK · último contacto hace <n>` — read from its own `new HubStateStore(properties.stateFile())` (no Spring context yet at dashboard launch), refreshed on the same 3 paths that touch the other labels (constructor, `ApplicationReadyEvent`, `stopServicesInBackground`); `licenseStatusText`/`humanizeSince` helpers verbatim from the plan (min <60m / h <48h / d). No test — Swing, manually verified (same deferral as `HubTrayIcon`). `.env.example` gained the first `EMBER_HUB_*` block (after `HUB_LICENSE_PRIVATE_KEY=`): `EMBER_HUB_HEARTBEAT_URL` (blank disables), `_HEARTBEAT_INTERVAL_MS=300000`, `_SUSPENDED_GRACE_HOURS=48`, `hub.latest-version=`. Full suite **891/891 PASS** (unchanged from report 270 — no new tests). **PENDING (require operator action, cannot run here):** the plan's manual 2-process smoke (cloud :8081 + Hub :8080, steps in report 271 §6) and the PR to `main`. License Heartbeat plan (HEARTBEAT-01..07) code COMPLETE on branch. Predecessor: report 270.
- **Last Completed Task (report 272, HPD-01, branch `feat/hosted-production-deployment`):** first code task of the Hosted Production Deployment plan (`docs/superpowers/plans/2026-08-28-hosted-production-deployment.md`). `ImageUploadService.uploadImage` dropped its `bucketName` param — bucket now always from `minio.bucket`, return value is `minio.public-url + "/" + <uuid>.jpg` (no bucket segment, so it works behind R2's `cdn.ember.vanter.net` custom domain), `PutObjectArgs` stamps `Cache-Control: public, max-age=31536000, immutable`. `deleteImage` is null/blank-safe and derives the object from the URL's last path segment (was `split("/",3)[2]`, which threw once prod URLs lost the bucket segment). New `MinioProperties.publicUrl` (`MINIO_PUBLIC_URL`, default `http://localhost:9000/ember-media` — dev unchanged). Removed dead `extension()` helper + the now-orphan `MinioProperties` field/import in `MenuItemService`. Callers (`CategoryService` ×2, `MenuItemService` ×2) updated; `CategoryServiceTest`/`MenuItemServiceTest` 2-arg `uploadImage` stubs + `MenuItemServiceTest`'s dead `@Mock MinioProperties` cleaned up. `ImageUploadServiceTest` rewritten to 6 cases. **Plan drift:** `.env.example` is at the repo root, not `backend/src/main/resources/`; the plan's `Cache-Control` assertion read `PutObjectArgs.extraHeaders()` but the MinIO SDK's `.headers(Map)` populates `.headers()` — fixed the accessor, header still ships on the PUT. Full suite **893/893 PASS** (891 baseline + 2 new). Also unblocks Hub portable-MinIO image serving (report 258). Predecessor: report 271.
- **Last Completed Task (report 273, HPD-02, branch `feat/hosted-production-deployment`):** added `config/PublicPingController` — `@RequestMapping("/public")` + `GET /ping` (`produces=text/plain`) returning literal `"pong"`; live route is `GET /v1/public/ping` under the app context path. No new security rule — `SecurityConfig` already has `/public/**` permitAll (line 61). This is the public liveness URL for the Cloud Monitoring uptime check, since HPD-03 will move `/actuator/**` (incl. `/actuator/health`) onto a loopback-only management port unreachable through the Tunnel. **Plan drift:** the plan's sketch test imported only the controller with no `@MockBean`s and failed context load — imported `SecurityConfig` autowires `RestaurantRepository` + the JWT-filter collaborators; followed the plan's own "mirror an existing `config`/`restaurant` `*ControllerTest`" note and copied `PublicRestaurantControllerTest`'s `@WebMvcTest`+`@Import({SecurityConfig,CorsConfig})`+`@MockBean{JwtService,UserDetailsService,UserRepository,RestaurantRepository}` shape. 1/1 focused test (RED first: missing symbol) + full suite **894/894 PASS** (893 baseline + 1 new). Predecessor: report 272.
- **Last Completed Task (report 274, HPD-03, branch `feat/hosted-production-deployment`):** appended 3 prod-only settings to `application-prod.properties` — `server.forward-headers-strategy=framework` (reconstruct `https://api.ember.vanter.net` scheme/host behind the `cloudflared` peer), `management.server.port=8081` (prod compose publishes this on `127.0.0.1` only → reachable by the on-VM Ops Agent for the HPD-14 scrape, never via Tunnel/LAN; main API stays on 8080), `management.endpoint.health.show-details=never` (base `application.yml` sets `always`). This is why HPD-02's `/v1/public/ping` exists — the uptime check can no longer reach `/actuator/health`. **Test approach:** plan's primary `@SpringBootTest`+`@ActiveProfiles("prod")` variant needs a live Postgres (prod pins `ddl-auto=validate`) and the suite has no prod-profile boot-test convention, so used the plan's documented fallback — `ProdManagementPortConfigTest` loads `application-prod.properties` from the classpath and asserts the 3 keys (RED first: all `null`). Full suite **897/897 PASS** (894 baseline + 3 new). Predecessor: report 273.
- **Last Completed Task (report 275, HPD-04, branch `feat/hosted-production-deployment`):** `AuthRateLimiterFilter.resolveClientIp` — behind a configured trusted proxy, now prefers `CF-Connecting-IP` (when it holds an IP literal, via the existing `normalizeIp`/`isIpLiteral` helpers) over the rightmost-non-proxy `X-Forwarded-For` walk, which stays as the fallback. New `CF_CONNECTING_IP` constant + one javadoc line; change confined to the existing `trustedPeer` gate (untrusted peer still ignores the header, non-literal value falls through unchanged — no new spoof surface). Needed because the Cloudflare Tunnel makes the `cloudflared` sidecar the permanent TCP peer, so `getRemoteAddr()`/its XFF hop no longer identify the real client and every external caller would share one bucket. 3 new tests (2 RED first; the untrusted-peer guard test green by construction). Full suite **900/900 PASS** (897 baseline + 3), `SecurityAuditTest` 75/75. `security-review` skill run on the diff — findings in report 275 §7. Predecessor: report 274.
- **Last Completed Task (report 276, HPD-05, branch `feat/hosted-production-deployment`):** first Phase 2 (deployment artifacts) task — created `deploy/docker-compose.prod.yml` (project `ember-prod`: `postgres` pg16 tuned + `ember_pgdata` vol; `app` GHCR image on `prod` profile, publishes **only** `127.0.0.1:8081` for the Ops Agent — `8080` unpublished, reached only by `cloudflared` as `http://app:8080`; `cloudflared` w/ `TUNNEL_TOKEN`; `backup` `build: ./backup` for HPD-06) + `deploy/.env.prod.example` (key-list template only, no values — datastore, auth secrets, R2 incl. `MINIO_PUBLIC_URL` [HPD-01], CORS, `EMBER_RATELIMIT_TRUSTED_PROXIES=172.16.0.0/12` [HPD-04], `TUNNEL_TOKEN`, `BACKUP_GCS_BUCKET`, `EMBER_IMAGE_TAG`). No app code, no tests. **Verify:** `docker compose ... config -q` exit 0. **Plan drift (host-only, no file change):** the plan's `config --env-file .env.prod.example` expects exit 0 but that file leaves `SPRING_DATASOURCE_PASSWORD=` blank and the `${VAR:?}` guard rejects empty — validated with placeholder values for the 3 `:?`-guarded vars; and `app`'s `env_file: /opt/ember/.env` is a real VM-absolute path, stubbed transiently for the Windows check. `build: ./backup` context absent until HPD-06 — `config` still passes. Suite unchanged **900/900** (no code touched). Predecessor: report 275.
- **Last Completed Task (report 277, HPD-06, branch `feat/hosted-production-deployment`):** authored the `backup` service image the prod compose's `build: ./backup` points at. `deploy/backup/Dockerfile` — `google/cloud-sdk:slim` + `postgresql-client` + `cron`; `/etc/cron.d/ember-backup` runs `0 8 * * *` (08:00 UTC); `CMD` = `cron && tail -F /var/log/backup.log` (stays up, logs visible via `docker compose logs backup`). `deploy/backup/backup.sh` — `set -euo pipefail`; `pg_dump -Fc "$PGDATABASE" | gzip` → `gs://$GCS_BUCKET/postgres/<YYYY-MM-DD>.dump.gz` (conn from the standard `PGHOST/PGUSER/PGPASSWORD/PGDATABASE` env the compose already passes), Sunday (`date -u +%u == 7`) also copies to `postgres-weekly/`, then `prune(prefix,keep)` lists the prefix oldest-first and `rm`s the leading `count-keep` — called for `postgres`/`DAILY_RETENTION` (14) and `postgres-weekly`/`WEEKLY_RETENTION` (8). GCS auth is keyless via the VM's attached SA / metadata server — the only backup secret is `BACKUP_GCS_BUCKET`, already in `.env.prod.example`. No app code, no unit tests. **Verify:** `docker build deploy/backup` clean; dry run against the running dev `postgres` (network `ember_default`) produced a non-empty 17839-byte `.dump.gz` (the `gcloud storage cp`/`prune` paths need a real bucket + SA → exercised in HPD-12/Phase 3). `bash -n backup.sh` OK; `shellcheck` not installed on this host. Suite unchanged **900/900** (no code touched). Predecessor: report 276.
- **Last Completed Task (report 279, HPD-07, branch `feat/hosted-production-deployment`):** Phase 2 wrap — `deploy/deploy.sh` (config via `EMBER_VM`/`EMBER_ZONE`/`EMBER_ENV_SECRET` env + `$1` image tag; step 1 pipes `gcloud secrets versions access latest` through `gcloud compute ssh --tunnel-through-iap` into `sudo install -m 600 /dev/stdin /opt/ember/.env` then appends `EMBER_IMAGE_TAG=<tag>`; step 2 a second IAP SSH cmd: `compose pull` + `up -d` + a 20×6s loop polling `compose exec -T app wget -qO- .../v1/actuator/health | grep -q UP`, exit 1 + `logs --tail=80 app` on exhaustion), marked executable via `git update-index --chmod=+x`. `deploy/RUNBOOK.md` skeleton — spec §3 topology diagram (+ the `backup` service line HPD-06 added), a First-time-provisioning index of HPD-11..19 (one bullet each, command bodies filled in as those tasks run), Routine deploy / Recovery (app unhealthy · Postgres corruption→HPD-21 · VM lost→snapshot) / Secret rotation→HPD-13/20 / quarterly Restore test→HPD-21. No app code, no unit tests. **Verify:** `shellcheck deploy/deploy.sh deploy/backup/backup.sh` via `koalaman/shellcheck:stable` image (no native shellcheck on host) → exit 0, no warnings. **Report is 279 not 278** — `reports/278-chore-remove-prometheus-grafana-mongo-orphan.md` already exists on `feat/hub-packaging-installer`; skipped to avoid a merge-time collision. Suite unchanged **900/900** (no code touched). Predecessor: report 277.
- **Last Completed Task (report 280, HPD-08, branch `feat/hosted-production-deployment`):** created `.github/workflows/backend-image.yml` — `backend-image` workflow, triggers `push` tags `v*` + `workflow_dispatch`, `permissions: contents read / packages write`. One `build` job on `ubuntu-latest`: `checkout` → `docker/login-action@v3` (ghcr, `github.actor` + `GITHUB_TOKEN`) → a "Derive tags" `run` step (tag event → `ghcr.io/vanter-tech/ember-backend:<version, v-stripped>` + `:latest`; manual `workflow_dispatch` → `:<short-sha>` only, no `:latest` move) → `docker/build-push-action@v6` context `backend`, `push: true`. **Plan drift (2):** (1) dropped the sketch's `actions/setup-java@v4` + `mvnw package` step — `backend/Dockerfile` is already multi-stage (`maven:3.9-eclipse-temurin-17` runs `mvn package -DskipTests`, then copies the jar into `17-jre-alpine`), so a pre-build step would compile twice; the plan's own note said keep whichever avoids a double build. (2) hardened the tag-derive step — the sketch's bare `${GITHUB_REF_NAME#v}` yields an invalid `/`-containing tag on a manual branch run, so it now branches on `GITHUB_REF_TYPE`. **Verify:** `actionlint` via `rhysd/actionlint:latest` container (runs shellcheck on the `run` block too) → exit 0, no findings. No app code, no tests — suite unchanged **900/900** (report 275). Predecessor: report 279.
- **Last Completed Task (report 281, HPD-09, branch `feat/hosted-production-deployment`):** new `frontend/scripts/gen-env-config.mjs` (Node ESM, not app-imported) + `build:pages` script (`tsc -b && vite build && node scripts/gen-env-config.mjs`) next to `build:hub`. The script reads `EMBW_API_URL`/`EMBW_WS_URL` env (dev fallbacks match `entrypoint.sh`: `http://localhost:8080/api/v1`, `.../v1/ws`) and writes `dist/env-config.js` = `window.ENV = { EMBW_API_URL: "…", EMBW_WS_URL: "…" }` (values JSON-stringified). Cloudflare Pages is a static upload with no boot step, so the runtime `window.ENV` contract the SPA loads from `/env-config.js` must be baked at build time from Pages project env vars. `build`/`build:hub` untouched. No plan drift — code verbatim from the plan. **Verify:** `build:pages` with prod vars → `dist/env-config.js` holds the `https://`/`wss://` pair; `pnpm run build` OK, `pnpm run test:run` **41/41**, `pnpm run lint` exit 0 (17 warnings, unchanged). No backend files — suite stays 900/900. Predecessor: report 280.
- **Last Completed Task (report 283, HPD-11, branch `feat/hpd-11-runbook` off `main` `8fd4a9e`):** recorded the live GCP provisioning in `deploy/RUNBOOK.md` § First-time provisioning → GCP. Project `ember-prod-vanter` (`us-central1` / `us-central1-a`), APIs enabled (compute/secretmanager/monitoring/logging/iap/storage), billing budget alert set. Firewall: `allow-iap-ssh` (tcp:22 ← `35.235.240.0/20`) only; `default-allow-ssh`/`-rdp` deleted. VM `ember-prod` e2-medium Ubuntu 24.04, 30 GB pd-standard, Shielded, OS Login, internal `10.128.0.2`. **DEVIATION from the plan's `--no-address`:** the VM carries an external IP (`34.44.144.220`) for egress (GHCR / `apt` / `cloudflared`) — no Cloud NAT was set up; inbound is still fully closed by the firewall (only IAP tcp:22). On the VM: `docker.io` 29.1.3 + `docker-compose-v2` 2.40.3 (enabled), 2 GB swap + fstab, `/opt/ember` (755), `unattended-upgrades`; `docker-compose.prod.yml` + `backup/{Dockerfile,backup.sh}` staged under `/opt/ember`; `docker run --rm hello-world` confirms egress. Docs-only, no code — suites unchanged. Predecessor: report 282.
- **Last Completed Task (report 284, HPD-12, branch `feat/hpd-12-backups` off `feat/hpd-11-runbook`):** recorded the GCS backup wiring in `deploy/RUNBOOK.md` §GCP. Bucket `gs://ember-backups-ember-prod-vanter` (pre-existing, private): granted the VM default compute SA (`253780825021-compute@developer.gserviceaccount.com`, already `cloud-platform` scope so no stop/start) `roles/storage.objectAdmin` on it; enabled object versioning. Daily boot-disk snapshot policy `ember-daily-snap` (`us-central1`, 7-day retention, 07:00 UTC, `keep-auto-snapshots` on disk delete) created and attached to `ember-prod`. Verified: PAP `enforced`, uniform BLA `true`, `versioning_enabled true`, disk lists the policy. **DEVIATION:** bucket is `US` multi-region, not `us-central1` (plan text) — cost-only, nothing depends on the region. Docs-only, no code — suites unchanged. Predecessor: report 283.
- **Last Completed Task (report 285, HPD-13, branch `feat/hpd-13-secrets-bringup` stacked on `feat/hpd-12-backups`):** first real bring-up of the hosted stack. Published the backend image via a `v0.1.0` tag → CI `backend-image.yml` → `ghcr.io/vanter-tech/ember-backend:0.1.0`+`:latest`. GHCR package is **private** (org policy blocks public container packages) → the VM does a one-time `docker login ghcr.io` with a classic `read:packages` PAT (creds persist in `/root/.docker/config.json`). Assembled the full `.env` body and stored it as one Secret Manager secret `ember-prod-env` (real rotated `SPRING_DATASOURCE_PASSWORD`/`JWT_SECRET`/`PLATFORM_JWT_SECRET`, fresh RSA keypair for `HUB_LICENSE_PRIVATE_KEY` — public key saved to `gs://ember-backups-ember-prod-vanter/keys/hub-public-key.der`; `TUNNEL_TOKEN` + `MINIO_*` left as `PLACEHOLDER` for HPD-20). `./deploy/deploy.sh latest` → `app healthy`; `flyway_schema_history` shows V1–V5 all `success=t`; `backup.sh` run in-container produced `gs://ember-backups-ember-prod-vanter/postgres/2026-08-29.dump.gz` (keyless SA auth works). `cloudflared` crash-loops on the placeholder token — expected, resolved in HPD-20. **FIX shipped:** `deploy/docker-compose.prod.yml` + `deploy/deploy.sh` polled `:8080/v1/actuator/health`, but HPD-03 moved management to port `8081` with no `/v1` prefix — both now poll `http://localhost:8081/actuator/health`. **RUNBOOK:** HPD-13 provisioning block + the `## Secret rotation` section filled in. **Hub follow-up:** the new keypair invalidates any previously issued Hub `license.key`; `ember-hub/keys/hub-public-key.der` must be replaced with the saved public key (Hub work, not this plan). Predecessor: report 284.
- **Last Completed Task (report 286, HPD-14 partial, branch `feat/hpd-14-monitoring` stacked on `feat/hpd-13-secrets-bringup`):** GCP observability for `ember-prod`. **Ops Agent 2.70.0** installed — the documented `dl.google.com/cloudagents/add-google-cloud-ops-agent.sh` bootstrap now 404s, so installed from the apt repo (`packages.cloud.google.com/apt google-cloud-ops-agent-noble-2`). `/etc/google-cloud-ops-agent/config.yaml` = `hostmetrics` (guest CPU/mem/disk/swap) + a `prometheus` receiver scraping `localhost:8081/actuator/prometheus` (56 `jvm_*` lines confirmed; `micrometer-registry-prometheus` already in the pom). Email notification channel `projects/ember-prod-vanter/notificationChannels/1594227198064578240` + 3 enabled alert policies: CPU > 0.9/10m, `memory/percent_used` > 90/10m, `disk/percent_used` > 85/5m. Config + policy JSON committed under `deploy/monitoring/`. **DEFERRED to HPD-20** (need the live Tunnel hostname): the uptime check on `https://api.ember.vanter.net/v1/public/ping`, its alert policy, and the stop-`app`-6-min end-to-end alert test. Docs + config files only, no app code. Predecessor: report 285.
- **Last Completed Task (report 287, landing-restyle-saas-coherence, branch `feat/hpd-14-monitoring`):** ad-hoc (not an HPD queue task). Replaced the landing site's throwaway brutalist theme (zero-radius, `shadow-brutal` 6px black boxes, 3–4px black borders, `font-black uppercase` headings, `font-mono` eyebrows, `#f5f5f0` cream bg, `#8c1717` `--color-accent`) with the SaaS design system so `ember.vanter.net` and `app.ember.vanter.net` look like one product. `landing/src/styles/global.css` token block rewritten to the exact `oklch` values from `frontend/src/index.css` `:root` (`background/foreground/card/primary/muted/border/input/ring/destructive/...` + the `--radius: 0.625rem` scale); the old red accent now maps onto `--color-primary` (same brand maroon the SaaS already uses). Real Inter webfont added via new dep `@fontsource-variable/inter` (offline-friendly on Pages, mirrors the SaaS's Geist). Mechanical class swap across all 11 `.astro`/`.tsx` components + `index`/`404`/`thank-you`/`terms`/`privacy` pages: thick black borders→`border border-border`, `shadow-brutal*`→`shadow-sm/md/lg`, square→`rounded-lg/xl`, red buttons→`bg-primary text-primary-foreground` + `hover:bg-primary/90`, all-caps `font-black` headings→sentence-case `font-semibold/bold`, `text-foreground/80`→`text-muted-foreground`, Pricing's solid-black featured card→white card w/ `ring-1 ring-primary`, `font-mono "— feature"` list→flex row + inline check `<svg>` (no icon dep), CTA `bg-foreground`→`bg-primary`, Nav→`bg-background/80 backdrop-blur`, ContactForm errors→shadcn `destructive` token. **No JSX structure / props / `client:*` / ARIA / copy changes** (beyond dropping forced uppercase); legal-page content untouched — only the card chrome around it. `astro.config.mjs`, `functions/api/contact.ts`, `constants.ts`, SEO/Analytics/Layout, build scripts all untouched. Verified: `cd landing && pnpm install && pnpm build` green (5 pages, sitemap, image opt OK); built CSS carries `--color-primary:oklch(39.5% .175 28.5)`, `--color-destructive`, the `body` base rule, and one Inter `@font-face`; `grep` for `shadow-brutal|border-foreground|font-mono|font-black|bg-accent` across `landing/src` = 0 matches. No file outside `landing/` touched → backend/frontend suites not re-run. Predecessor: report 286.
- **Last Completed Task (report 288, landing-hero-dashboard-image, branch `feat/hpd-14-monitoring`):** ad-hoc follow-up to report 287. Swapped the hero's placeholder framed box for a real waiter-dashboard screenshot (`landing/src/assets/Hero.png`, 3827×2041, maintainer-supplied; Astro `<Image>` at `width={1600}` emits a ~26 KB WebP) shown as a large tilted "live panel" — `.hero-media` container `perspective: 1600px`, image `transform: rotateY(-14deg) rotate(1.5deg)` so the right edge recedes, layered soft shadow, `rounded-xl` + `ring-1 ring-black/5` instead of a solid border. Hero section is `flex min-h-dvh items-center` so it fills the viewport (next section no longer peeks); image widened past its column (`md:w-[140%] lg:w-[158%]` + `md:-mr-28 lg:-mr-44`) with `section overflow-hidden` guarding against horizontal scroll. Hover (`@media (hover:hover)`): panel straightens + lifts (`rotateY(-5deg) rotate(0) translateY(-8px) scale(1.02)`, deeper shadow, 0.5s ease). Tilt + widening removed under 768px; `prefers-reduced-motion` drops the transition. Left column reworked: eyebrow → pill badge (border + `bg-muted/50` + `bg-primary` dot); headline → new `--font-display: 'Anton'` (`@fontsource/anton`, CBUM-wordmark stand-in), `uppercase`, `text-5xl sm:text-6xl lg:text-7xl` — hero only, body stays Inter. A maintainer-supplied `Hero.svg` was tried first and discarded: it's a cropped viewport (`viewBox 361.13×1152` over the same 3827×2041 raster, translated) that zooms to one table card at hero size. `pnpm build` green. Old `product-screenshot.png` now unused but left in place. Predecessor: report 287.
- **Last Completed Task (report 289, landing-plans-page-comparison, branch `feat/hpd-14-monitoring`):** ad-hoc follow-up to report 288. Plans moved to their own view: new `landing/src/pages/planes.astro` (header + plan cards + full feature-comparison table + CTA + Footer). New `landing/src/lib/plans.ts` is the single source for `PLANS` and the `COMPARISON` matrix (4 groups / 21 rows, cell = `true`/`false`/string); new `PlanCards.astro` (shared by home teaser + `/planes`) and `PricingTable.astro` (`overflow-x-auto`, `min-w-[760px]`, sticky first column, Pro column tinted, check svg / `—` / text values). Pricing removed from the home entirely (`Pricing.astro` deleted, `index.astro` is now Hero→Features→Contact→CTA→Footer). `NAV_LINKS`: `#features`→`/#features`, `#pricing`→`/planes`; Hero "Ver planes" button → `/planes`. Comparison tiers were drafted from Ember's documented capabilities (CLAUDE.md §1) — **maintainer should review which tier each feature actually unlocks.** `pnpm build` green, 6 pages. Predecessor: report 288.
- **Last Completed Task (report 290, landing-mobile-nav-portal-fix, branch `feat/hpd-14-monitoring`):** the mobile burger menu did nothing usable. Root cause: `MobileNavDrawer`'s `position: fixed; inset-0` overlay lived inside `<header>`, which got `backdrop-blur` in the report-287 restyle — `backdrop-filter` makes an ancestor a containing block for `fixed` descendants, so the drawer collapsed to the ~69px header strip (island hydrated fine, `open` state toggled — pure CSS bug). Fix: render the overlay via `createPortal(overlay, document.body)` behind a `mounted` (useEffect) SSR guard; z bumped `z-50`→`z-[60]`. Verified: drawer now a direct `<body>` child, overlay measured full-viewport (1912×918), panel pinned right, dismiss via backdrop/×/Esc all work. `pnpm build` green. Predecessor: report 289.
- **Last Completed Task (report 291, landing-hero-editorial-serif, branch `feat/hpd-14-monitoring`):** hero reworked — maintainer disliked the Anton headline and found the hero "muy simple". `--font-display` swapped Anton → **Fraunces Variable** (`@fontsource-variable/fraunces`, editorial high-contrast serif); `h1` no longer uppercase, `font-medium leading-[1.05]`, `sincronizado` in `italic text-primary`. Enrichments added to `Hero.astro`: **two** `blur(90px)` `var(--color-primary)` glows (`.hero-glow--right` behind the dashboard + `.hero-glow--left` behind the headline, diagonal balance), a mask-faded `radial-gradient` dot grid, a `rotate(-3deg)` floating "Mesa 8 · lista" status card over the image (`hidden sm:flex`, straightens on `.hero-media:hover`), and a **feature bar** — full-width `border-t bg-muted/40` strip listing real SaaS features (Carrito colaborativo por QR · Comandas de cocina (KDS) · División de cuentas y caja · Analítica por período · Multi-restaurante), `md:flex-nowrap md:justify-between`. Section is `flex min-h-[calc(100svh-4.25rem)] flex-col justify-center` (4.25rem clears the sticky nav; `justify-center` keeps content+bar as one centred block); grid natural-height `items-center py-10`, bar `mt-4 md:mt-8` — tight to the content (~72px below the CTAs), not pinned to the fold. Decorative layers `aria-hidden pointer-events-none`; content grid `relative`. `pnpm build` green, no h-overflow. Predecessor: report 290.
- **Last Completed Task (report 292, landing-hero-floatingnav-and-drawer-anim, branch `feat/hpd-14-monitoring`):** polish round. `MobileNavDrawer.tsx`: overlay now always-mounted (portaled) with `transition-opacity`/`transition-transform` so it fades + slides on both open and close; burger bars `transition-all` morph to ✕ (`rotate ±45` / `opacity-0`); `inert={!open}` on the closed overlay; button toggles + `aria-label` swaps. `Hero.astro`: column gap `gap-10` → `gap-12 md:gap-16 lg:gap-20` (text/image separation); new `.hero-nav` floating pill over the dashboard **top-left** (4 icon buttons — grid active, list, user, logout — echoing Ember's in-app FloatingNav), `rotate(3deg)`, straightens with the panel + status card on `.hero-media:hover`, `hidden sm:flex aria-hidden`. `pnpm build` green. Predecessor: report 291.
- **Last Completed Task (report 293, landing-header-nav-refactor, branch `feat/hpd-14-monitoring`):** `Nav.astro` refactor — maintainer found the header too plain. Added a `bg-primary` flame-chip brand mark (rotates on logo hover), route-aware active-link pills (`aria-current="page"` + `bg-muted`; `/#features` anchors never "current"), grouped auth actions with a `w-px` divider and an arrow on the primary "Registrarme" button. Layout: brand alone on the left, nav links + auth + mobile drawer wrapped in one right-aligned group (`justify-between`). Plus a scroll-aware sticky: starts `border-transparent bg-transparent` (floats over the hero) → gains `bg-background/80 backdrop-blur border-border shadow-sm` past `scrollY > 8` via a `data-scrolled` attr toggled by an `is:inline` script. Header ~61px. Mobile drawer untouched (already body-portaled). `pnpm build` green. Predecessor: report 292.
- **Last Completed Task (report 294, landing-features-page, branch `feat/hpd-14-monitoring`):** same treatment as `/planes` (report 289) for the features. New `landing/src/pages/funcionalidades.astro` (header + feature cards + per-feature detail section — number/title/description + a `sm:grid-cols-2` check-list of concrete capabilities per feature — + CTA + Footer). New `landing/src/lib/features.ts` (`FEATURES` with `points[]`, drafted from CLAUDE.md §1) + `FeatureCards.astro` shared component. `Features.astro` deleted; `index.astro` no longer renders it (home = Hero → Contact → CTA → Footer). `NAV_LINKS`: `/#features` → `/funcionalidades` (header + mobile drawer follow). `pnpm build` green, 7 pages, nav active state works on the new route. Predecessor: report 293.
- **Last Completed Task (report 295, landing-scrollytelling-walkthrough, branch `feat/hpd-14-monitoring`):** new `Walkthrough.astro` on the home between Hero and Contact — scroll-driven feature walkthrough. `md:grid-cols-2`: left = 4 `min-h-[72vh]` step blocks (Mesas / Cocina-KDS / Detalle y cobro / Analítica, content from CLAUDE.md §1); right = a `sticky top-[13vh] h-[74vh] overflow-hidden` box holding the **bare hero-style `Hero.png`** (tilted `rotateY(-13deg)`, `ring-1`, hero shadow — no card/frame, per maintainer follow-up). A `<script>` `IntersectionObserver` (`rootMargin -45% 0 -45%`) writes `data-active="0..3"` on the section; CSS transitions `transform-origin` + `rotateY/rotate/scale` per step (zoom table grid `1.7` → pan `1.45` → zoom detail panel `2.15` → zoom out nearly flat `1.12`), a label chip + progress dots follow. `prefers-reduced-motion` disables transforms; mobile has no sticky (static framed image per step). **Single-asset caveat:** only the waiter-floor screenshot exists — steps 2 (KDS) and 4 (Analítica) are pans of that image; dropping `assets/kds.png`/`analitica.png` + per-step `src` swap is a small follow-up. `pnpm build` green, 7 pages. Predecessor: report 294.
- **Last Completed Task (report 296, landing-remove-walkthrough-contact-page, branch `feat/hpd-14-monitoring`):** removed the report-295 scroll-zoom walkthrough (`Walkthrough.astro` deleted, out of `index.astro`) — maintainer rejected it. Contact moved to its own route: new `landing/src/pages/contacto.astro` — **info, no form** (maintainer's call): 3 icon cards (Correo `mailto:` / Oficina / Horario) + a "Pedir una demo" `mailto:` CTA strip. Dead form files **deleted**: `ContactForm.tsx`, `landing/functions/api/contact.ts` (+ empty `functions/`), `thank-you.astro` — **this removes the landing side of HPD-10 (report 282): the `/api/contact` Pages Function + `CONTACT_WEBHOOK_URL` are no longer used.** `ContactSection.astro` deleted; `NAV_LINKS` gains `/contacto`; `Footer.astro` "Contacto" `mailto:` → `/contacto`. Home is now **Hero → CTA → Footer** only; the three sub-pages (Funcionalidades / Precios / Contacto) are all in the nav. `pnpm build` green, 8 pages. Predecessor: report 295.
- **Last Completed Task (report 297, landing-info-section, branch `feat/hpd-14-monitoring`):** new "Info" nav item → `/info` docs-style section with a second-level sidebar. New `InfoLayout.astro` (Nav + `md:grid-cols-[220px_1fr]` `md:sticky` `<aside>` + slot + Footer; sidebar = Resumen / Manual de usuario / Videos de Ember with `aria-current`). Pages: `/info` (overview + 2 card links), `/info/manual` (9 module sections from CLAUDE.md §1 + anchor TOC, marked "Borrador"), `/info/videos` (6-card `aspect-video` grid — renders an `<iframe>` when an `embed` URL is set, else a "Próximamente" placeholder). `NAV_LINKS` gains `/info` (`Nav.astro`'s `isActive` prefix-matches `/info/*`). Content is scaffolding. `pnpm build` green, 10 pages. Predecessor: report 296.
- **Last Completed Task (report 298, landing-home-sections-bigger-hero, branch `feat/hpd-14-monitoring`):** the home had shrunk to Hero→CTA→Footer. Added 4 new components — `HowItWorks` (3 numbered steps), `FeaturesTeaser` (2-up cards from `lib/features.ts` + "Ver todas →" `/funcionalidades`), `WhyEmber` (4 icon value props), `Faq` (6 native `<details>` accordion, no JS, full `max-w-6xl` width, each `<summary>` header a `bg-primary` red bar with white text + white `+`→`×` marker, answer panel on `bg-card`). `index.astro` = Nav→Hero→HowItWorks→FeaturesTeaser→WhyEmber→Faq→CTASection→Footer. **Hero enlarged:** headline `→ xl:text-[5.25rem]` (~84px), bigger paragraph/CTAs, `<Image width={2200}>`. Section `flex min-h-[calc(100svh-4rem)] flex-col` + grid `flex-1 content-center` + feature bar flush at bottom → fills the viewport, the next section's "Cómo funciona" heading no longer peeks. Image bleed pulled back (`xl:w-[156%]`, `xl:-mr-24`) so it stays inside the layout (~611px, not running off-screen) and the hero reads centred instead of left-heavy. `pnpm build` green, 10 pages. Predecessor: report 297.
- **Last Completed Task (report 299, landing-tiltedshot-component, branch `feat/hpd-14-monitoring`):** extracted the hero's image treatment into reusable `landing/src/components/TiltedShot.astro` — wraps Astro `<Image>`, props `src`/`alt`/`width`/`flip`/`eager`/`class`/`frameClass`; scoped `<style>` carries the `rotateY(-14deg)` tilt + layered shadow + hover-straighten + mobile-flatten + reduced-motion. Slotted children marked `data-float` (with inline `--float-rot`/`--float-rot-hover`) get the chip base rotation and straighten on `.tiltshot:hover` via a `:global()` selector. `Hero.astro` refactored to use it (nav pill + status card become `data-float` slots); `.hero-media*`/`.hero-nav`/`.hero-card` CSS deleted, `.hero-grid`/`.hero-glow*` kept. No visual change. Reuse: `<TiltedShot src={kds} alt="…" flip />`. `pnpm build` green, 10 pages. Predecessor: report 298.
- **Last Completed Task (report 300, landing-funcionalidades-tiltedshots, branch `feat/hpd-14-monitoring`):** `funcionalidades.astro` detail section reworked from text-only `divide-y` rows into a `space-y-24` list of alternating **text + `<TiltedShot>`** rows (`md:grid-cols-2`, odd rows `md:[&>*:first-child]:order-2`; image `flip` + `-ml`/`-mr` bleed follow the side). All 4 shots import the same `assets/Hero.png` placeholder (comment: swap per-view captures — analytics/categorías/KDS — pointing each `src` at its own image). `<section>` gained `overflow-hidden`. `pnpm build` green, 10 pages, no h-overflow. Predecessor: report 299.
- **Last Completed Task (report 301, landing-hero-centered, branch `feat/hpd-14-monitoring`):** `Hero.astro` converted from the text-left/image-right split to a **centered stacked hero** — content wrapper `flex flex-col items-center text-center`, headline `xl:text-[6.25rem]` (~100px) `max-w-4xl text-balance`, paragraph `max-w-2xl` centered, CTAs `sm:justify-center`, and the `<TiltedShot>` below at `max-w-5xl` (1024px, centered; responsive-width + `-mr` bleed dropped). Status-card chip moved bottom-left → bottom-right (diagonal with the nav pill). Removed `min-h-[calc(100svh-…)]`+`flex-1` (a big centered image is > 1 screen by design; section is content-sized so the headline never clips). Dot-grid mask re-centered `72% 34%`→`50% 28%`. `pnpm build` green, 10 pages, no h-overflow. Predecessor: report 300.
- **Last Completed Task (report 302, landing-dark-mode, branch `feat/hpd-14-monitoring`):** dark theme for the landing with a header toggle. `global.css` keeps the light `@theme` as base; new `:root[data-theme="dark"]` redefines every `--color-*` (warm near-black bg `oklch(0.16 0.008 40)`, **ember red kept, brightened to `oklch(0.62 0.2 27)`** unlike the SaaS which drops it, white/10 borders). `Layout.astro` `<head>` has a blocking `is:inline` script: `data-theme = localStorage['ember-theme'] ?? OS-pref` (no FOUC). `Nav.astro` gained a `#theme-toggle` icon button (moon/sun via `[data-theme]` CSS, visible all breakpoints) that flips `data-theme` + persists. Touch-ups: `TiltedShot` `ring-black/5`→`ring-border` + `brightness(0.9)` on the light screenshot in dark; **floating `data-float` chips re-declare the light `--color-*` locally so they stay light over the light app screenshot in dark mode**; `MobileNavDrawer` scrim `bg-foreground/50`→`bg-black/50`. Everything else was already token-based. `pnpm build` green, 10 pages, verified dark↔light toggle + `/planes`/CTA/footer/FAQ in dark, no h-overflow. Predecessor: report 301.
- **Last Completed Task (report 303, landing-cta-redesign, branch `feat/hpd-14-monitoring`):** replaced the full-width `bg-primary` `CTASection` band. After a few iterations (contained card → +glow → −glow → −card) the final is a **wide card with a blurred SaaS screenshot background**: `max-w-6xl` wrapper, `relative isolate overflow-hidden rounded-2xl border bg-card` card; `Hero.png` `<Image alt="" aria-hidden>` as `absolute inset-0 -z-10 scale-125 object-cover blur-2xl` at `opacity .55` (`.24` dark) over a `bg-card/45` scrim; content (dash + `text-primary` eyebrow + `text-foreground` h2 + lede + `bg-primary` button + "o mirá los planes →" link) sits legibly on top. Token-based → correct in dark. Also affects `/planes` and `/funcionalidades` (shared component). `pnpm build` green, 10 pages. Predecessor: report 302.
- **Last Completed Task (report 304, landing-feature-images, branch `feat/hpd-14-monitoring`):** swapped the `Hero.png` placeholder in `/funcionalidades` for the real per-feature admin screenshots. `lib/features.ts` each `Feature` gains `image: ImageMetadata` (Cart.png→01, Kitchen.png→02, Table_view.png→03, Analytics.png→04, all new in `src/assets/`); `funcionalidades.astro` `<TiltedShot src={feature.image}>` + dropped the placeholder import. `pnpm build` green, 10 pages (4 PNGs → WebP). Predecessor: report 303.
- **Last Completed Task (report 305, landing-i18n, branch `feat/hpd-14-monitoring`):** ad-hoc. Full-site i18n on the landing — Spanish base (no prefix), English at `/en/` via Astro's built-in `i18n` (`prefixDefaultLocale: false`). New `landing/src/i18n/ui.ts` (flat `Record<Lang,Record<string,string>>`, ~230 keys, ES verbatim from the old components + EN translations for the whole site incl. `/info/*` + legal) and `i18n/utils.ts` (`getLang(Astro.currentLocale)`, `useTranslations`→`t()` w/ fallback, `localizePath`, `switchLocalePath`). New `LangSwitcher.astro` (ES/EN control) wired into `Nav.astro` **to the right of the theme toggle**, which itself moved to the right of "Registrarme" per the maintainer. Every `.astro` component + `Layout`/`InfoLayout` now render `t('…')` and route internal links through `localizePath`; `<html lang>` = `Astro.currentLocale`. Data files became locale factories (`getFeatures(lang)`, `getPlans(lang)`, `getComparison(lang)`). The 3 React islands (`MobileNavDrawer`/`StickyMobileCTA`/`CookieBanner`) take a `lang` prop. **Routing without body duplication:** each `src/pages/en/<name>.astro` is a 3-line wrapper re-rendering the ES page component — `Astro.currentLocale` resolves to `'en'` from the URL, so the shared tree renders English. `pnpm build` green, **20 pages** (10 ES + 10 EN). Verified via built-HTML greps (no key leaks, EN fully translated, nav `/en/`-prefixed, island `lang` prop per locale) + browser (toggle position, ES↔EN switch, `<html lang>`, translated nav/h1/CTA). Placeholder company data (Vanter S.A./Quito/RUC/email) left as-is in both languages — maintainer will swap in real Nicaragua info later. Predecessor: report 304.
- **Last Completed Task (report 306, landing-client-router, branch `feat/hpd-14-monitoring`):** ad-hoc. Added Astro's `<ClientRouter />` (ex `<ViewTransitions />`, renamed in Astro 5; repo is on Astro 7.2.2) to `Layout.astro` `<head>` for SPA-style navigation + link prefetch. Two swap-model adjustments: (1) the anti-FOUC theme `<script is:inline>` body became a named `applyTheme()` called once at first paint **and** registered on `document`'s `astro:after-swap` (persists across transitions, runs before paint, re-applies `data-theme` since swapped-in pages ship without it) — deliberately NOT `data-astro-rerun` (would re-add a duplicate listener each swap); (2) `Nav.astro`'s trailing inline script (scroll→`data-scrolled` on `#site-header`, click→theme toggle) wrapped in a `setup()` registered on `astro:page-load` (fires initial load + every client-side nav), removing the stale `window` scroll listener before re-adding. `pnpm build` green, 20 pages. Browser-verified: real client-side nav (no full reload), dark theme + active nav pill survive navigation with no flash, scroll-aware header re-binds. Predecessor: report 305.
- **Last Completed Task (report 307, landing-real-company-info, branch `feat/hpd-14-monitoring`):** ad-hoc. Swapped the landing's placeholder Ecuador company data for the real details: `Vanter S.A.` → `Vanter` (startup, no legal suffix), `RUC 1792999999001` removed, `Av. Amazonas… Quito, Ecuador` → `Managua, Nicaragua`, `ventas@ember.vanter.com` → `tofernandoband01@outlook.com` (Footer, contacto ×3, plans.ts Enterprise CTA, ui.ts privacy/terms es+en), hours `9–18 GMT−5` → `8–18 GMT−6`, legal jurisdiction repointed to Nicaragua (Ley No. 787 de Protección de Datos, leyes de la República de Nicaragua, jueces de Managua — es+en), legal "última actualización" → 29 ago 2026, and domain normalized to `ember.vanter.net` (robots.txt sitemap + Plausible `data-domain`, matching `astro.config.mjs` `site:`). Footer's Vanter column also gained a `mailto:` line. FB/IG links deferred (pages not created yet); `og-image`/icons still placeholder art (need real files). Legal pages remain reference translations, not a legal review. 6 files: `Footer.astro`, `Analytics.astro`, `contacto.astro`, `plans.ts`, `ui.ts`, `public/robots.txt`. `pnpm build` green, 20 pages; `grep dist/` for `vanter.com`/`Ecuador`/`Vanter S.A.`/RUC → 0. Predecessor: report 306.
- **Last Completed Task (report 308, landing-real-brand-assets, branch `feat/hpd-14-monitoring`):** ad-hoc. Wired the maintainer's real brand art. 3 source files added to `src/assets/` (`Ember_logo.svg` solid maroon flame, `ember-apple-touch-icon.png` 180×180, `ember-og-image.png` 1200×630). A one-off `sharp` script regenerated the `public/` set: `favicon.svg` (logo path re-wrapped square + `prefers-color-scheme: dark` fill `#e8a9a9`), `favicon.ico` (16/32/48 PNG entries from the touch icon), `apple-touch-icon.png` + `og-image.png` (real art, alpha stripped), new `icon-192.png` / `icon-512.png` for PWA. `site.webmanifest` icon list expanded. New `Flame.astro` (inline flame, `fill=currentColor`); `Nav.astro` brand chip + a new matching chip in `Footer.astro` now use it (option A — white flame on `bg-primary` square). `Layout.astro` untouched (same `public/` filenames). `pnpm build` green, 20 pages; `dist/` + curl content-types verified; browser-checked header/footer/favicon. **Still placeholder:** none of the launch assets now — FB/IG footer links remain pending (pages not created). Predecessor: report 307.
- **Last Completed Task (report 309, landing-compare-trust-sections, branch `feat/hpd-14-monitoring`):** ad-hoc — priority round 1 of the landing-review content gaps. Two new home sections following the existing section idiom (no new deps, shared inline check `<svg>`): `Compare.astro` ("Ember vs. lo de siempre" — 3 cols Comanda a voz/papel · POS tradicional · **Ember**, 4 rows Toma de pedido/Dividir la cuenta/Estado de cocina/Puesta en marcha; desktop `grid-cols-[1.2fr_1fr_1fr_1fr]` with the Ember column highlighted, mobile = one card per approach Ember-first; no competitor named) placed directly under the hero; `Trust.astro` ("Sin letra chica" / "Lo que podés esperar de Ember" — 5-item check grid: Free sin tarjeta / cancelás cuando quieras / datos aislados / funciona con tus impresoras / sin apps para el comensal + "Detrás de Ember está Vanter, desde Managua, Nicaragua." → `/contacto`) placed before the final CTA. **No fabricated testimonials/logos — Ember has no customers yet, so the trust block states only verifiable guarantees.** Home order is now Nav→Hero→Compare→HowItWorks→FeaturesTeaser→WhyEmber→Faq→Trust→CTASection→Footer. `ui.ts` +33 keys × es/en (`cmp.*`/`trust.*`, EN apostrophes as `’`); `index.astro` imports/placement; `en/index.astro` untouched (renders `<Page />`, locale from `Astro.currentLocale`). Hero + Info-nav deliberately NOT touched (maintainer's call). `cd landing && pnpm run build` green, 20 pages; both sections confirmed in `dist/index.html` + `dist/en/index.html` + `astro preview` visual check (dark). **Deferred to next landing tasks:** hero screencast/video section, pricing context (needs business input), SEO/a11y technical fixes (hreflang, JSON-LD, FRONTEND_URL assert, skip link, reduced-motion, cookie/Plausible copy). Predecessor: report 308.
- **Last Completed Task (report 310, landing-hreflang-og-locale, branch `feat/hpd-14-monitoring`):** task-L3 — i18n SEO metadata. `SEO.astro` only (all 20 pages route through it via `Layout`/`InfoLayout`): imports `switchLocalePath`, derives the page's ES + EN URLs from `Astro.url.pathname` against `Astro.site`, emits `<link rel="alternate" hreflang="es|en|x-default">` (x-default → prefix-less ES) on every page + `og:site_name="Ember"` + `og:locale` (`es_ES`/`en_US` from `Astro.currentLocale`) + `og:locale:alternate`. No per-page opt-out — 404 also gets alternates (`/404/` ↔ `/en/404/`), harmless. `pnpm run build` green, 20 pages; verified in `dist/` across ES/EN root, `/planes/`, nested `/info/manual/`, `404.html` — every alternate pair resolves correctly. **task-L2 (hero screencast video) moved to LAST per maintainer.** Remaining landing tech tasks: L4 JSON-LD (Organization/SoftwareApplication/FAQPage), L5 (FRONTEND_URL assert · skip-link · hero alt · prefers-reduced-motion · cookie/Plausible copy). Predecessor: report 309.
- **Last Completed Task (report 311, landing-jsonld-structured-data, branch `feat/hpd-14-monitoring`):** task-L4 — new `StructuredData.astro` emits one `<script type="application/ld+json" is:inline>` `@graph` on the home only (rendered in `index.astro` inside `<Layout>`, so `/` gets the es graph and `/en/` the en graph): `Organization` (`Vanter`, Managua/`NI`, logo `/icon-512.png`, `@id …/#organization`), `WebSite`, `SoftwareApplication` (`Ember`, `BusinessApplication`/`Web`, locale-aware `url`, desc from `seo.home.desc`, single free `Offer` $0 — **paid tiers omitted while pricing is TBD**), `FAQPage` built from the same `faq.1..6.q/a` i18n keys `Faq.astro` uses (6 questions). `<` escaped to `<`. `pnpm run build` green, 20 pages; `node`-parsed both home files — valid JSON, 4 types, 6 FAQ entries, `inLanguage` es/en, `software.url` `…/` vs `…/en/`; JSON-LD present on exactly the 2 home pages, nowhere else. Remaining landing tech task: L5 (FRONTEND_URL assert · skip-link · hero `alt` · prefers-reduced-motion · cookie/Plausible copy). Then L2 (hero screencast video, needs the clip) + pricing track. Predecessor: report 310.
- **Last Completed Task (report 312, landing-a11y-hardening, branch `feat/hpd-14-monitoring`):** task-L5 — 5 small fixes across 13 landing files. (1) `constants.ts` `console.warn` at build when `PROD && !PUBLIC_FRONTEND_URL` (kept a warn not a throw so local `astro build` still works; CF-Pages-gated hard-fail is a trivial follow-up). (2) `global.css` global `@media (prefers-reduced-motion:reduce)` `*` catch-all + `.skip-link` styles. (3) `Layout.astro` skip-link `<a href="#main-content">` (localized) as first body child; **every page now wraps its post-`<Nav>` content in `<main id="main-content" tabindex="-1">`** (index/funcionalidades/planes/contacto/privacy/terms/404 + InfoLayout) — adds the missing `main` landmark too. Browser-verified: skip link is first focusable, `a.click()` → `location.hash=#main-content` + focus lands on `<main>`. (4) `Hero.astro` image `alt` `t('hero.eyebrow')` → new `t('hero.shotAlt')` (es+en) — only that attribute, hero otherwise untouched. (5) `ui.ts` `privacy.s4.body` (es+en) rewritten: was "con tu consentimiento, cookies de analítica" but Plausible is cookieless + ungated → now "únicamente cookies estrictamente necesarias" + cookieless non-advertising analytics; cookie banner text was already accurate, untouched. `pnpm run build` green, 20 pages; grep-verified `id="main-content"`/`skip-link` on all 20, reduced-motion `*` block in compiled CSS, alt + privacy wording localized. **Remaining: task-L2 (hero screencast video — needs the clip) + pricing track (needs business input).** Predecessor: report 311.
- **Last Completed Task (report 313, landing-pricing-annual-toggle, branch `feat/hpd-14-monitoring`):** pricing track — decided after a market analysis (NI/Central America context, Fudo/Parrot/Loyverse refs): **Free $0 / Starter $19 / Pro $49 / Enterprise a medida**, por local, USD, IVA aparte, anual = pagás 10 usás 12 (Starter $190/año, Pro $490/año), sin trial separado (Free es la prueba), cobro recurrente aún sin resolver → no se afirma checkout de tarjeta. `plans.ts`: Starter $29→$19, Pro $79→$49 + `priceAnnual`/`periodAnnual`/`annualNote` en Free/Starter/Pro. **New monthly⇄annual toggle** (no framework, `Nav.astro`-style `<script is:inline>` on `astro:page-load`): `planes.astro`'s existing `<main id="main-content">` gets `data-billing="monthly"` (spans cards + comparison table); `PlanCards.astro` renders the segmented control + dual `data-price-monthly`/`data-price-annual` markup + `data-annual-note`; `global.css` hides the inactive variant (default monthly → no flash, degrades if JS fails); `PricingTable.astro` headers use the same dual spans so they switch in lockstep. Copy (es+en): `plan.perYear`, `ppage.billing.*` new; taglines → segmento ("sodas, comedores y cafés" / "restaurantes con volumen"); `ppage.lede` += "Empezás gratis, sin tarjeta"; `ppage.fineprint` += línea anual. **NOT changed:** feature gating / límites de mesas por plan (revisión aparte desde report 289); JSON-LD sigue con solo el `Offer` Free. `pnpm run build` green, 20 pages; browser-verified toggle flips todos los precios (cards + tabla) es/en, Enterprise queda "A medida". **Landing pendiente: solo task-L2 (video screencast del hero — falta el clip).** Predecessor: report 312.
- **Last Completed Task (report 314, landing-sitemap-i18n, branch `feat/hpd-14-monitoring`):** SEO — `astro.config.mjs` `sitemap()` → `sitemap({ i18n: { defaultLocale: 'es', locales: { es: 'es', en: 'en' } }, filter: p => !p.includes('/404') })`. Ahora cada URL del `sitemap-0.xml` lleva `<xhtml:link rel="alternate" hreflang="es|en">` (par mutuo, mirror del hreflang del `<head>` del report 310) y se excluyen los 404 (`/en/404/` se colaba). Sin `lastmod` (no hay fecha real; un "now" por build es peor que nada). `pnpm run build` green; sitemap pasó de 20 a **18** entradas, las 18 con alternates, string `404` ausente. **Discutido con el user (no en código):** "Preferred sources" de Google NO aplica (es feature de Google News/Top Stories para medios de noticias, US/India, no es señal de ranking ni meta tag). Estado SEO técnico: bien para el tamaño del sitio (title/desc/canonical/hreflang/OG/JSON-LD/sitemap/`<main>`/skip-link). Lo que falta mueve-aguja está fuera del código: contenido real (blog/guías) y autoridad de dominio (dominio nuevo, sin backlinks). **Pendiente acordado:** redactar el contenido de `/info/manual` + `/info/videos` (hoy "Borrador"/"Próximamente", indexables) y luego decidir noindex/contenido. Landing técnica: solo queda task-L2 (video hero). Predecessor: report 313.
- **Last Completed Task (report 315, landing-info-manual-videos-content, branch `feat/hpd-14-monitoring`):** contenido real para la sección Info. `ui.ts` (es+en): `manual.lede` sin "Borrador"; `manual.s1..s9` ampliadas de stub de una línea a referencia de 3–5 oraciones por módulo (nivel conceptual/flujo, sin nombres de botones), basadas en features reales (asistente de alta, 4 roles, mapa de piso en vivo, QR/código de 5, estados KDS + demora, cierre de caja + arqueo + turno vencido/prolongar, agente de impresión red/Windows/ESC-POS/driver, catálogo + tours "?"); nueva **`manual.s10` "Solución de problemas"** (QR, impresora, pedido no llega a cocina, turno vencido, no veo módulo=rol). `videos.lede` + los 6 `videos.N` reescritos (sin cambio de estructura, **sin noindex** por decisión del maintainer; `embeds[]` sigue en `null`). `manual.astro`: +id `solucion-problemas`, body se parte por `\n` en `<p>` (secciones de una línea igual, la de troubleshooting queda como lista de 5). Nuevo `landing/docs/video-guiones.md` — guiones de grabación (objetivo/duración/pantalla/narración) + dónde subir (YouTube canal Vanter, público o no listado; pegar `youtube.com/embed/ID` en `videos.astro` `embeds[]`; **nunca en el repo**; alternativas Vimeo/Cloudflare Stream). `pnpm run build` green, 20 pages; `grep dist` sin "Borrador"; 10 secciones es+en, s10 renderiza 5 `<p>`. **Landing: solo queda task-L2 (video screencast del hero — falta el clip).** Predecessor: report 314.
- **Last Completed Task (report 316, landing-video-modal, branch `feat/hpd-14-monitoring`):** `/info/videos` — visor modal al hacer clic en vez de 6 `<iframe>` fijos. `videos.astro`: modelo `embeds`(URLs)→`ids` (ID de YouTube / URL YT o Vimeo / fallback); `resolveVideo()` deriva miniatura (`i.ytimg.com/vi/<id>/hqdefault.jpg`) + embed `youtube-nocookie.com/embed/<id>?autoplay=1&rel=0`. Tarjeta con video = `<button data-video-open>` con miniatura + círculo play `bg-primary`; sin id sigue el placeholder "Próximamente". Modal = `<dialog id="video-modal">` nativo (foco atrapado + Esc + scroll-lock gratis), chrome con tokens (`rounded-xl border-border bg-card shadow-lg`, header con título + `×`, `::backdrop` negro/60 vía `is:global`). Script `is:inline` en `astro:page-load` (patrón `Nav.astro`): iframe vía `createElement` (sin innerHTML); se remueve el iframe para cortar reproducción en **todas** las vías de cierre (`close`/`cancel`/keydown Escape/botón `×`/clic en backdrop) — el evento `close` solo no dispara confiable bajo automatización. Claves `videos.play`/`videos.close` (es+en). Guiones doc actualizado (pegás el ID en `ids[]`). `pnpm run build` green; con todos los `ids` en null la página tiene **0** `<iframe>` (nada de YouTube carga hasta el clic). Verificado en navegador con un ID de prueba temporal (revertido antes del commit): miniatura+play, modal abre centrado con player `nocookie`, Esc y `×` cierran y remueven el iframe, reabrir funciona. **Landing: solo queda task-L2 (video screencast del hero — falta el clip).** Predecessor: report 315.
- **Last Completed Task (report 321, HPD-18 repo side, `main`):** stand up the SaaS SPA at `app.ember.vanter.net` as its own Cloudflare **Worker** (static assets), separate from the `ember` Worker that serves the landing — a static-assets Worker has one `assets.directory`, so the SPA needs its own. New `frontend/wrangler.jsonc` (`name: "ember-app"`, `assets ./dist`, `not_found_handling: "single-page-application"` for React Router). Fixed `frontend/scripts/gen-env-config.mjs` no-env fallback `…/api/v1` → `…/v1` (matches every consumer). Verified `EMBW_API_URL=https://api.ember.vanter.net/v1 EMBW_WS_URL=wss://api.ember.vanter.net/v1/ws pnpm run build:pages` → `dist/env-config.js` carries the wss/api values; `index.html` loads it. No frontend source touched. **Operator follow-up (not in commit):** CF Worker `ember-app` connected to Git (root `frontend`, build `pnpm run build:pages`, deploy `npx wrangler deploy`, build vars `EMBW_API_URL`/`EMBW_WS_URL`/`NODE_VERSION=20`); move custom domain `app.ember.vanter.net` off `ember` onto `ember-app`; then HPD-17 Tunnel + HPD-20 secret wiring. Predecessor: report 320.
- **Current Active Task: standing up `app.ember.vanter.net` (HPD-17/18/20 Cloudflare track).** HPD-18 repo side done (report 321). Next: operator creates the `ember-app` Worker + moves the domain, then the `ember-prod` Tunnel for `api.ember.vanter.net` and the `TUNNEL_TOKEN` → `ember-prod-env` redeploy. — HPD-14's GCP-side scope done (report 286); its uptime-check remainder folded into HPD-20. **The entire maintainer GCP track (HPD-11..14) is now as complete as it can be without the Cloudflare deliverables.** Remaining: HPD-15..19 (Cloudflare track — colleague), HPD-20 (integrate real `TUNNEL_TOKEN` + R2 into `ember-prod-env`, redeploy, uptime check, full smoke), HPD-21 (restore test), HPD-22 (E2E walkthrough). Phase 1 + Phase 2 repo tasks (HPD-01..10) + License Heartbeat merged to `main` (`8fd4a9e`). **Repo branches `feat/hpd-11-runbook` → `feat/hpd-12-backups` → `feat/hpd-13-secrets-bringup` → `feat/hpd-14-monitoring` are stacked, not yet pushed/PR'd** — bundle into one PR to `main` when ready. Live prod stack: `postgres`+`app`+`backup` healthy on `ember-prod`, Ops Agent reporting; `cloudflared` crash-looping on the placeholder token (expected until HPD-20).
- System health: **on `feat/hpd-14-monitoring` (stacked on hpd-13 → hpd-12 → hpd-11, off `main` `8fd4a9e`)** — HPD-14 adds `deploy/monitoring/` (Ops Agent config + 3 alert-policy JSON) + a RUNBOOK block; no code. HPD-13 shipped a 2-line health-check-port fix to `deploy/docker-compose.prod.yml` + `deploy/deploy.sh` (verified live: `deploy.sh` reported `app healthy`). No backend/frontend code touched across HPD-11..14. Last verified suites: backend `./mvnw test` PASS full suite **900/900** (report 275); `printing-agent`: `mvn test` + `mvn clean package` PASS (report 255); frontend `pnpm run build` PASS + `pnpm run test:run` **41/41** PASS + **`pnpm run lint` exit 0** (report 281); `landing` `pnpm build` PASS (report 308 — real brand assets; SaaS restyle + dark mode + i18n + client router + real data + real favicon/OG/logo all shipped on this branch, reports 287–308). Still pending: FB/IG footer links (pages not created yet). Prior branch `feat/hub-license-heartbeat` (891/891, report 271) still PENDING its operator smoke + PR to `main`. `printing-agent`: `mvn test` + `mvn clean package` PASS (report 255); frontend `pnpm run build` PASS + `pnpm run test` **41/41** PASS + **`pnpm run lint` exit 0** (report 260). CI `lint.yml` fixed (report 261) — 3 required checks green, PR-per-task workflow works. **Fase 1 minor gaps DONE (reports 260–264):** lint cleared, CI fixed, role-edit UI, dev Flyway re-baseline script, stuck-bills VOID script. **Fase 2 decomposition:** v1-remaining Hub work split into sub-projects A (cloud sync), B (packaging/lifecycle), C (local backup); A split into A1 (license heartbeat — spec+plan DONE, tasks HEARTBEAT-01..07 queued) and A2 (data sync, later). Digital-payments item deliberately excluded. **Fase 3:** hardware bridge (cash drawer + barcode). Queued follow-ups: dev `ddl-auto=validate` + drift-reconciliation migration; run the two `docs/db/dev-*.sql` scripts against dev. Ember Hub licensing pipeline (HUB-01 + `hub-license-activation` COMPLETE, report 256 verified live). Still-owed from before (unrelated): EMB-CR whole-branch review; manual browser check of EMB-CR/EMB-STAFF/EMB-RV/EMB-CLP/EMB-CLH pages incl. bento layout + progress bar + the analytics fix, PLUS EMB-i18N-02..07's switcher toggle, the Settings sidebar accordion, the Ticket subtab/preview, staff create/profile grid, category/menu modal grid+EditCategoryModal, the "Impresoras" tab, EMB-i18N-08's validation/toast copy in both languages, MOD-04's admin UI, MOD-05's customer selector modal, MOD-06's KDS modifier display, KDS order push, admin inventory low-stock toast, reports 197/198/199's toast restyle+position+accent border+slide animation in light+dark (no `claude-in-chrome` tool available in any of these sessions — this is exactly how the WS regression went unnoticed for multiple tasks until a user manually reported it); `/landing` follow-ups. The 3 stray pre-existing uncommitted files noted in report 179 remain stashed on `refactor/admin-modals-grid`'s branch history — still unresolved.

## Active Context & Recent Decisions
- **Postgres migration (COMPLETE, 2026-08-24, branch `ember-postgress-migration`, plan `docs/superpowers/plans/2026-08-24-ember-postgres-migration.md`):** `session`/`kitchen` moved from MongoDB to JPA/Postgres — the whole backend is now on one persistence engine. `MongoTenantBackfill`/`KitchenOrderActiveBackfill` (Mongo-only, no-schema-migration workarounds) are deleted; `V15__session_kitchen_postgres.sql` owns the schema from day one. `Session` deliberately has NO `@TenantId` (its `findByJoinCodeAndStatus` must see across tenants before a customer's JWT is bound — same reason `User` stays outside `@TenantId`); `KitchenOrder` DOES get `@TenantId`, matching `MenuItem`/`Category`/`ModifierGroup`. `participants`/`items`/`activityLog` (List-typed JSON fields) map via `@JdbcTypeCode(SqlTypes.JSON)` with no `columnDefinition` override — Postgres resolves that to native `jsonb` for List fields (unlike `RestaurantSettings.payload`, a single object, compatible with plain `text`); V15 declares those columns `jsonb` directly to match, confirmed via a real local Postgres boot (declaring them `text` made `ddl-auto=update` try an unsupported `alter column ... set data type jsonb`). Repository methods that filtered inside the embedded arrays (`findByTenantIdAndParticipants_UserId`, `findByTenantIdAndItems_Status`) became Java-side `default` methods instead of native JSONB SQL, for H2/Postgres portability — both are currently unused outside tests. **Gotcha for any future `@DataJpaTest`:** its entity scan isn't scoped to the test's package, so it also picks up every `@TenantId` entity project-wide and puts Hibernate into multi-tenant mode — `@Import(TenantIdentifierResolver.class)` is required even when the entity under test has no `@TenantId` of its own. **Gotcha for any future `@TenantId` entity test:** Hibernate resolves the tenant identifier once per Hibernate session, and `@DataJpaTest`'s default shared transaction opens that session before `@BeforeEach`/the test body runs — `@Transactional(propagation = Propagation.NOT_SUPPORTED)` is required (see `AbstractTenantIsolationTest`) whenever the tenant is bound inside the test rather than by a real request filter, and raw `EntityManager` calls won't work under `NOT_SUPPORTED` (no ambient transaction) — go through the repository instead.
- Monolith root at `ember/`; Java 17 + Spring Boot 3.5.14 / React 19 + TS + pnpm. Product: multi-tenant restaurant platform (collaborative cart, KDS, floor/waiter management, admin analytics). task-3.2 deleted `spring-kafka` — Spring `ApplicationEventPublisher` is the only event bus, do not reintroduce a broker.
- task-3.3: `GlobalExceptionHandler` extends `ResponseEntityExceptionHandler` (mandatory — a bare `Exception.class` catch-all would outrank `DefaultHandlerExceptionResolver` and degrade 400s to 500); RFC 7807 via `problem(status, detail, path)`, 500s log ERROR with a `traceId`, never echo `ex.getMessage()`. task-2.15's **Flyway** (`baseline-on-migrate`, migrations from V2, disabled in tests/H2 `create-drop`): every schema change goes in `db/migration/` AND the entity mapping; `ddl-auto: update` silently can't add `NOT NULL` columns without a default to a non-empty table — check boot logs for `CommandAcceptanceException`, don't trust a clean boot.
- (Superseded 2026-08-24 by the postgres migration bullet above — `Session`/`KitchenOrder` are JPA now, `MongoTenantBackfill`/`KitchenOrderActiveBackfill` are deleted.) Original task-2.17 rule still holds behaviorally: `Session`/`KitchenOrder` carry `tenantId`, every finder is tenant-first, route session reads through `SessionService.findById`. Hibernate JPA side uses `DISCRIMINATOR` multi-tenancy (task-2.14): `@TenantId` on `Category`/`MenuItem`/`Bill`/`BillSplit`/`Payment`/`DiningTables`/`RestaurantSettings`/`KitchenOrder` (`User`/`Session` deliberately excluded — both need untenanted pre-tenant-bind lookups); isolation repo tests extend `config/AbstractTenantIsolationTest`. `GET /kitchen/display`'s live-only filter / `GET /kitchen/orders` staying unfiltered (originally report 52) is now just `KitchenOrder.active`, set directly wherever it changes — no backfill runner needed once Flyway owns the schema.
- task-3.4: `config/CorsProperties` (`ember.cors.*`) is the ONE origin policy (tenant subdomains → `allowed-origin-patterns`, never `allowed-origins`); task-3.5's `AuthRateLimiterFilter` buckets `(tenant, clientIp)`, applies servlet-wide via `RateLimitProperties.paths`, not `SecurityConfig`. task-3.1: `.env` (gitignored) holds all secrets, no fallback/fail-fast boot, add new ones to `.env.example` too; old secrets remain in git history, rotation pending. task-4.3's `GET /public/restaurants/{slug}/branding` (permitAll) and the JWT filters are the ONLY places allowed to touch `TenantContextHolder`. task-4.4 made `Restaurant.plan`/`status` load-bearing (403 when `status != ACTIVE`).
- task-3.6–4.2: `SettingsPayload.paymentGateway`/`businessHours`/`billing.taxRules` now fully wired frontend-side (task-5.4–5.8); `GET /kitchen/orders`/`/catalog/items` return `Page<T>` (hand-written in `api.ts`). Remaining task-5.x gaps: `api.ts`'s interceptor only special-cases 401 (not 403); `backend-types.ts` predates task-4.3/4.4 additions. Plan/subscription self-service is real on the backend (`RestaurantAdminController`) but has NO frontend home — belongs on a future customer/billing portal, not the admin app; `api.ts`'s `restaurantAdminService` stays unused until then (report 44) — don't re-add a Plan settings tab. `/admin/analytics` (task-5.17–5.21, `FloatingNav.tsx`'s `BarChart3` link) is now COMPLETE: `SummaryCards`/`SalesChart`/`ProductPerformance`/`TableAnalytics` all consume `analyticsService` in `api.ts`, whose response types are `components['schemas'][...]` aliases (refactor-analytics-types-schema, report 66) — every field on them is OPTIONAL, and this project's build has `strictNullChecks` effectively ON despite no `strict`/`strictNullChecks` key in either tsconfig (verify with `npx tsc -b` if unsure), so every numeric/array field needs a `?? 0`/`?? []` default at the call site, not just at declaration. `SalesChart` was restyled (redesign-analytics-enterprise-ui, report 111) onto a Recharts `AreaChart` (brand-red gradient fill, `type="monotone"`) — `recharts` is now a real dependency, so any future analytics chart should reuse it rather than reintroducing a plain-CSS bar pattern. STILL no shared date-range picker UI anywhere on the page — any future analytics work adding one should make it shared, not per-widget.
- task-5.10/5.11: KDS subscribes to `/topic/kitchen/{tenantId}` (`KitchenLayout` + `websocket.ts`'s `subscribeToKitchen`), just calling `queryClient.invalidateQueries(['kitchenOrders'])` on any message rather than reconciling payloads client-side; backend `KitchenWebSocketListener` broadcasts, `KitchenItemUpdated` gained `tenantId`. No STOMP SUBSCRIBE-destination ACL exists for this topic (or `/topic/session/{id}`) — pre-existing, accepted gap. Connection-state `Badge` reading `useWebsocketStore.isConnected` lives in `OrdersDisplays`, not `KitchenLayout`.
- CUSTOMERS ARE NOT TENANT-BOUND (reports 55–56): `User.restaurantId` is null for them by design (staff keep it, still required); a customer's tenant is only known once they JOIN A TABLE, so login/register mint a JWT with NO `rid` and `jwtAuthFilter` waves tenant-less CUSTOMERs through (staff still gated). `joinSessionCode` looks the code up untenanted (`findByJoinCodeAndStatus`, rejects a code open at 2 restaurants), `joinSession` reads the tenant off the signed QR token; both bind it via `SessionService.bindResolvedTenant` (the one non-filter place allowed to touch `TenantContextHolder`, and it re-checks `ACTIVE` since the filter no longer can) and return `JoinSessionResponse{session,token}` — the client MUST swap to that token (`JoinTableModal`), as menu/items/confirm all need `rid`. `issueTenantScopedToken` refuses to re-scope staff. `/t/:slug` (`TenantLanding`) is still UNREACHABLE — no inbound link anywhere; don't build flows through it without wiring an entry point first.
- task-5.12: `analytics` module (`controller`/`service`/`dto`) owns NO persistence — it only aggregates over `billing`/`session`/`catalog`. `AnalyticsController` carries a CLASS-LEVEL `@PreAuthorize("hasRole('ADMIN')")` so task-5.13–5.16's handlers inherit the gate; tenant always via `TenantContextHolder.requireTenantId()`, never a request param. Analytics repo queries name the tenant explicitly (`where b.tenantId = :tenantId`) ON TOP of the `@TenantId` filter — a context slip here returns plausible numbers instead of throwing. Read shapes are JPQL constructor-expression records living in the OWNING module's repo package (`billing.repository.BillActivityWindow`), not interface projections and not analytics DTOs. Seed endpoint `GET /admin/analytics/range` (billing window for task-5.19's date pickers) — add new routes to `SecurityAuditTest`'s 401 matrix.
- task-5.13 (`GET /admin/analytics/summary?from&to`) fixes the metric semantics task-5.14–5.16 must reuse: revenue = CONFIRMED `Payment` sums (never PENDING, never bill totals); AOV = PAID-`Bill` `sum/count` (2dp HALF_UP); `activeSessions` is a LIVE `countByTenantIdAndStatus(OPEN)` that deliberately IGNORES the window. `from`/`to` are optional and INCLUSIVE, defaulted in the service (`EPOCH_FLOOR` = 1970-01-01, never `LocalDateTime.MIN` — no SQL `timestamp` holds it — and `now()`), echoed back in the response; an inverted window throws `IllegalArgumentException` (409) rather than reading as "no sales". Null aggregates normalise to `0`/`0.00`. task-5.14 (`/sales`) reuses that window helper and those metrics: DB groups only by `year()/month()/day()` (NOT `date_trunc` — spells differently on H2 vs Postgres), week/month/year rolled up in Java by `SalesGranularity` (ISO Monday weeks); series is gap-free (zero-filled) and, with no `from`, starts at the first ACTIVE bucket, not `EPOCH_FLOOR` (which would emit ~20k empty daily buckets); unknown granularity → `IllegalArgumentException` (409), never a silent DAY fallback. task-5.15 (`/products`) is the one analytics read that crosses repositories (both Postgres since the 2026-08-24 migration, previously Postgres+Mongo): `BillRepository.findPaidSessionIds` → `SessionRepository.findByTenantIdAndIdIn` (line items, skip `DRAFT`) → `MenuItemRepository.findByTenantIdAndIdInWithCategory` (`join fetch` category) — same "a sale is a settled bill" rule, but its `totalRevenue` is ITEM money and deliberately ≠ `/summary`'s payment revenue (tax/tips live on the bill). Shares/`cumulativeShare` are computed over the FULL set before `limit` truncates; deleted menu items keep the name they were sold under (null category), renamed ones report the current catalogue name. task-5.16 (`/tables`) ALSO crosses repositories: `Bill` has no `tableId` of its own, so `BillRepository.findPaidBillActivity` (sessionId+total+createdAt) joins back to `Session.tableId`/`createdAt` in the service, same as products. `Session` has no closed/settled timestamp, so session duration is approximated as `bill.createdAt - session.createdAt`, in minutes; `activeTableCount`/`averageTurnoverRate` are LIVE like `/summary`'s `activeSessions`, ignoring the window; a deleted table keeps the revenue it earned (null `tableNumber`, same pattern as a deleted menu item).
- EMB-PC backlog (COMPLETE, reports 70–83): `/console/*` platform-operator portal, fully isolated from the tenant app — `PlatformOperator`/`PlatformAuditLog` have no FK to `Restaurant`/`User`, signed with a separate `platform.jwt.secret`, own `SecurityFilterChain` on `/platform/**` (`@Order(1)`) that never touches `TenantContextHolder`; frontend `platformApi.ts`/`usePlatformAuthStore` are a fully separate Axios instance + auth store from the tenant app's. `pnpm run openapi` regen still doesn't cover `platform/**` schemas (hand-typed instead). feature-catalog-pagination (report 68): `GET /catalog/categories` now takes `Pageable`/returns `Page<CategoryResponse>` — read `.content`, not a bare array; `components/PaginationControls.tsx` is the shared Prev/Next control (0-indexed, self-hides at `totalPages <= 1`, SELF-POSITIONED fixed `bottom-8 right-8` floating pill per feature-pagination-floating-style report 69) — reuse for any future paginated admin list.
- EMB-CR backlog (brainstormed 2026-08-16, spec+plan under `docs/superpowers/`): single shared till per tenant (`cash_shifts` partial unique index on `tenant_id WHERE status='OPEN'`), blind arqueo (close endpoint accepts only `countedCash`; server reveals `expected`/`variance` in the response), no separate Z-report entity — `CLOSED` shift rows are write-once and "Corte Diario" is a derived rollup query over them. `openedBy`/`closedBy`/`createdBy`/`processedBy` are plain `varchar` `users.id` columns, NOT JPA `@ManyToOne` — avoids `LazyInitializationException` from `User.restaurantId`'s LAZY association (`open-in-view` is false). `CashShiftService.openShift` takes `tenantId` as an explicit param (matches every other tenant-aware service method), not `TenantContextHolder.requireTenantId()` internally as the plan's literal text had it — a plan-text gap the implementer corrected, confirmed by a Mockito strict-stubbing failure. `PaymentService.registerPhysicalPayment` now hard-requires an `OPEN` shift (gained `CashShiftRepository`/`UserRepository` deps, stamps `cashShiftId`/`processedBy`); DIGITAL payments unaffected. Deliberately NO new frontend WebSocket subscription at the time: `websocket.ts`'s single `currentSubscription` slot was already claimed by `subscribeToWaiter` for the whole `/waiter/*` subtree, so a `subscribeToCashRegister` would have silently broken floor-dashboard real-time updates — the backend still broadcasts to `/topic/cash-register/{tenantId}`, unconsumed. The shared-slot design was fixed by bugfix-waiter-tables-realtime-orders (report 123), which added an independent `waiterSessionSubscription` slot + `subscribeToWaiterSession`/`unsubscribeFromWaiterSession` — that's now the pattern for any future waiter-subtree subscription that must coexist with the tenant-wide one (e.g. a future `subscribeToCashRegister` should get its own slot too, not reuse `currentSubscription`). **Process note:** both halves were meant to run under `subagent-driven-development` via two parallel fork subagents, but forks cannot spawn further subagents ("Fork is not available inside a forked worker") — both fell back to implementing directly with self-review only, no per-task independent reviewer, hence the whole-branch review dispatched afterward.
- EMB-LP backlog (COMPLETE, reports 84–101): `ember/landing/` marketing site — FULLY STANDALONE Astro 7.2.2 package (own lockfile/workspace, zero coupling to `frontend`, static-build only, deployed separately), `site: 'https://ember.vanter.com'`. Placeholder brand art (`og-image.png`/`apple-touch-icon.png`/hero screenshot) still needs swapping for real assets before launch. `astro build` is its own verification command, not `pnpm run lint`/`./mvnw test`.
- EMB-PAY backlog (brainstormed 2026-08-16): completed the customer-facing payment cycle. `billing/event/BillingRequested` + `BillingEventListener` + `BillReadyMessage` existed fully tested but UNPUBLISHED (no controller ever fired the event) — EMB-PAY-01 added `POST /billing/sessions/{sessionId}/request` (WAITER) to publish it; Spring's event bus is synchronous, so calculate+split+broadcast complete before the HTTP response returns. There is still NO `GET` endpoint for `Bill`/`BillSplit` — the customer/waiter only ever learn bill state via `/topic/session/{id}` broadcasts (`BILL_READY`/`SPLIT_PAID`/`DIGITAL_PAYMENT_INITIATED`); `websocket.ts`'s waiter handler writes straight into the `['bill', sessionId]` React Query cache via `setQueryData` (no query fn exists to `invalidateQueries` against). Known gap: a customer who reloads/reconnects after missing a broadcast sees stale bill data until the next one arrives. `PaymentService.initiateDigitalPayment` is a stub gateway (`gatewayRef = "STUB-..."`); `confirmDigitalPayment` stays WAITER-only by design, which is why `DIGITAL_PAYMENT_INITIATED` had to be broadcast too. Reports 124–127.
- EMB-STAFF backlog (brainstormed 2026-08-17): `User.active` is now LOAD-BEARING, not cosmetic — `EmberUserDetailsService` sets `.disabled(!active)`, `AuthService.login` separately rejects inactive users (it bypasses `UserDetailsService` entirely, manual `findByEmail`+password-match), and `SecurityConfig.jwtAuthFilter` only sets `SecurityContextHolder` auth when `userDetails.isEnabled()` — an already-issued JWT for a since-deactivated user 401s on its very next request via the existing `anyRequest().authenticated()` path, no new response code needed. This is the pattern for any future "must re-check DB state per-request" auth rule (mirrors the existing `Restaurant.status` tenant-suspension check in the same filter). "Delete" staff = deactivate (`active=false` via existing `PATCH /admin/staff/{id}`), a deliberate choice over hard-delete to preserve `Payment.processedBy`/`CashShift.openedBy` audit trails (those are plain varchar, not real FKs).
- EMB-RV backlog (COMPLETE, spec `docs/superpowers/specs/2026-08-17-refunds-and-voids-design.md`, EMB-RV-01..08 + final-review fix wave, reports 137–145): void a `Bill` pre-payment, refund (full/partial) a `CONFIRMED` `Payment` post-payment, both WAITER-only, additive/append-only. `Refund` never mutates `Payment` (no `PaymentStatus.REFUNDED`); `BillSplit.paid`→`status` (`BillSplitStatus` UNPAID/PARTIALLY_PAID/PAID) so partial refunds are representable; `BillStatus.VOIDED` terminal, `Bill` gains `voidedBy`/`voidedAt`/`voidReason`. A physical refund's till impact is a plain `CashMovement`/`CASH_OUT` against whichever shift is open *now* (never rewrites a `CLOSED` shift) — `PaymentService` builds it directly via `CashMovementRepository`, deliberately NOT through `CashShiftService.recordMovement`, to avoid a circular dependency now that `CashShiftService.getDetail` depends on `PaymentService`. `refunded_by`/`voided_by` are plain varchar `users.id`, same convention as `processed_by`/`opened_by`. `api.ts` gained `billingService.voidBill`/`listPayments`/`refundPayment`/`listRefunds`; `sessionStore`'s `markSplitPaid`→`markSplitStatus`+`clearBill`. New `VoidBillModal`/`RefundPaymentModal` (mirrors `ChargeTableModal.tsx`'s pattern); `Bill.tsx` (customer) reads `split.status`. `ShiftHistoryTable.tsx` (admin) shows an expandable per-shift payments row, READ-ONLY — the refund action itself lives on the waiter's own `CashRegister.tsx` "Pagos" card (both reuse, never duplicate, `RefundPaymentModal`; the admin page can't act since `POST /billing/payments/{id}/refund` is WAITER-only by deliberate design). `RefundPaymentModal` resolves by exact `paymentId` when the caller has one, falling back to name-matching only when it doesn't (the live-table-view caller). `SplitPaidMessage` is `status:String` (not a boolean) end-to-end, backend and frontend. `calculateBill`'s guard is `findBySessionIdAndStatusNot(sessionId, VOIDED)` so voiding actually frees a session to re-bill. `Bill.java` has NO JPA `uniqueConstraints` (would fight V8's partial-unique-index under `ddl-auto: update` — same bare-`@Table` pattern as `CashShift.java`). All 3 payment-creation/confirmation entry points (`registerPhysicalPayment`/`initiateDigitalPayment`/`confirmDigitalPayment`) reject a `VOIDED` bill. No `claude-in-chrome` browser tool was available this session — verification was a full curl/psql-verified API walkthrough against the live worktree backend instead of a click-through; a real one is still owed. Backend 687/687 green, frontend build green.
- EMB-i18N backlog (brainstormed 2026-08-18, spec `docs/superpowers/specs/2026-08-18-emb-i18n-design.md`, plan `docs/superpowers/plans/2026-08-18-emb-i18n.md`, EMB-i18N-01..07 COMPLETE, EMB-i18N-08 still queued): tenant frontend only (customer/waiter/kitchen/admin/auth) — `/console` explicitly excluded. ES (default) + EN, client-side only (Zustand `persist` `localeStore`, own localStorage key, no backend/`User`/tenant-setting changes, no browser-language auto-detection). Custom `locales/{es,en}/*.ts` dictionaries per role namespace (not react-i18next); each `en/*.ts` declared `satisfies typeof es*` so `tsc -b` fails on a missing/extra key. `LanguageSwitcher.tsx` (built on the existing `ui/select.tsx`) is inserted next to the 4 duplicated "Ember" wordmark spots without merging those headers into one shared component. Full per-role namespace registration: `auth`/`common` (02), `customer` (03, 78 keys), `waiter` (04, 106 keys), `kitchen` (05, 12 keys), `admin` core (06, 98 keys), `admin` analytics+settings (07, ~140 keys). Widest dedup patterns and disclosed gaps (untranslated `TIER_LABELS`/`itemStatus.ts` lookup tables, `toast.*` calls deferred to EMB-i18N-08) documented per-task in reports 166–172. `Home.tsx`/`Bill.tsx`'s loyalty dashboard (landed via EMB-CLP/CLH after EMB-i18N-03) is already on `t()` keys, reconciled as part of this session's `main` merge (see BRANCH EVENT 3).
- EMB-PRINT backlog (COMPLETE, brainstormed 2026-08-22, spec `docs/superpowers/specs/2026-08-22-emb-print-design.md`, plan `docs/superpowers/plans/2026-08-22-emb-print.md`, report 180): new `printing` module (`PrintAgent`/`PrinterConfig`/`PrintJob`, `V12__printing.sql` — V11 is permanently burned in the real dev DB by the reverted EMB-ACC backlog, fixed via `flyway:repair`, never reuse V11). Agents authenticate with a static API key hashed via the existing `PasswordEncoder`, exchanged for a short-lived JWT (`POST /printing/agents/token`, `permitAll`) carrying `{agentId, tenantId, typ:"print-agent"}`; they connect to a SECOND, fully isolated STOMP endpoint `/ws/print-agent` with its own `PrintAgentChannelInterceptor` — `JwtChannelInterceptor`/`/ws` (tenant-facing) are never touched, same isolation precedent as EMB-PC's `/platform/**`. `PrintingEventListener` consumes the EXISTING `PaymentCompleted`/`KitchenItemsConfirmed` events unmodified, gated on `RestaurantSettings.hardware.autoPrintTickets`/`printCustomerReceipt`; `PrintJob.payload` is structured text, never raw ESC/POS bytes — only the agent renders bytes. `PrintDispatchService` broadcasts to every active `PrinterConfig` of a job's role, grouped by owning agent (a tenant can run multiple agents — required since USB printers are only reachable from the PC they're plugged into); undelivered jobs stay `PENDING`, flushed on the agent's next CONNECT via an internal `PrintAgentConnected` event (`ApplicationEventPublisher`/`@EventListener`) — **do not** wire `PrintDispatchService` as a direct constructor dependency of `PrintAgentChannelInterceptor`, that closes a real circular bean cycle back through `SimpMessagingTemplate` (which needs every `WebSocketMessageBrokerConfigurer`, including the one owning this interceptor, to exist first) — only `E2EOrderFlowTest`'s full-context boot catches this, not unit tests. The local agent lives in **`printing-agent/`, a separate Maven project at the repo root** (own `pom.xml`, not a module of `backend/`'s single-module POM) — plain Java 17 + `spring-websocket`/`spring-messaging` (no Spring Boot), `escpos-coffee` for ESC/POS rendering, `jSerialComm` for USB, packaged via `maven-shade-plugin` into a runnable jar (`mvn clean package`). Admin UI: new "Impresoras" subtab next to Ticket Settings, wired through the existing `useUIStore` modal/settings pattern (`CREATE_PRINT_AGENT`/`ADD_PRINTER`/`PRINTING`), not a bespoke local-state modal. PRINT-07 (physical printer) is unverified — no hardware available.
- EMB-i18N-08 backlog (COMPLETE, report 181, the deferred final task of EMB-i18N): a module-scope `z.object({...})` can't call the `useTranslation()` hook, so every such schema became a `createXSchema(t: ReturnType<typeof useTranslation<'ns'>>['t'])` factory function, invoked inside the component as `const xSchema = useMemo(() => createXSchema(t), [t])` — that's the pattern for ANY future validation schema needing localized messages. 11 files got this treatment: `Login`/`Register`, `{Create,Edit}RewardModal`, `{Open,Move,Close}ShiftDialog` (cashRegister), `{Create,Edit}StaffModal`, `{New,Edit}{Menu,Category}Modal` — the last 4 already build their schema inline inside the component body (not module scope), so those just got `t()` calls substituted directly, no factory needed. Toast copy across 23 files (found via `grep -rl "toast\.(success|error)("`, `pages/console/**` excluded per the original plan's constraint) moved to `t()` keys the same way; several were byte-identical across files (`admin.settingsSavedToast`/`settingsSaveErrorToast` alone covers 9 settings pages) — consolidated into shared keys rather than duplicated per-file. `EditCategoryModal.tsx` needed this too despite not being in the original EMB-i18N-08 plan's file list (created later, in report 179) — found by grepping live code instead of trusting the stale plan list; that's the check to repeat if this task's file list is ever reused as a checklist for something else. A few EN toast strings being extracted were already-broken English leaking into the Spanish UI (e.g. `"An ERROR has occurred"`) — writing a real `es`/`en` pair for those is a side effect of correct extraction, not a separate cleanup pass.
- EMB-CLP backlog (brainstormed 2026-08-17, sub-project A of two — engine only; the Profile bento-grid page is sub-project B, a separate future spec/plan once A ships): accrual is per-participant off the EXISTING `PaymentCompleted` event (no new event type) — a settled bill with 3 paying participants credits 3 separate awards, keyed off each `BillSplit.amount`, never once per table. Single accrual mode per tenant (`BY_VISIT` flat or `BY_AMOUNT_SPENT` rate, admin-selected, not both). Tier (`BRONCE/PLATA/ORO/PLATINO`) is COMPUTED ON READ from `LoyaltyAccount.totalPoints` against `RestaurantSettings`'s thresholds — deliberately never stored, so an admin changing a threshold never leaves stale tiers on existing accounts. `LoyaltyAccount` links `(tenantId, userId)`, created lazily on first table-join at that tenant (not registration/login) — the correct hook given `User.restaurantId` is null for customers by design (see CUSTOMERS ARE NOT TENANT-BOUND, above). `LoyaltyReward` is catalog-display-only in v1 (tier-gated via `requiredTier`, no points cost, no redemption, no billing integration). New `loyalty` module, new `/loyalty` endpoints — no `/api` prefix (matches every real controller; the `/api/...` rows in `SecurityAuditTest` are a known-stale leftover, not a convention). Spec: `docs/superpowers/specs/2026-08-17-customer-loyalty-program-design.md`. EMB-CLP-04: `LoyaltyAccountService.findOrCreate` (idempotent) + `LoyaltyAccountJoinListener` (`loyalty.listener`, `@EventListener` on the EXISTING `ParticipantJoined` event — no `SessionService` changes, covers both `joinSession`/`joinSessionCode`) is the account-creation path; `findOrCreate` is public specifically so EMB-CLP-05's accrual listener can call it again as a safety net (spec §3.3 step 4) without duplicating get-or-create logic. No settings-enabled gate on creation — spec §3.4 doesn't call for one, only the accrual listener guards on `loyalty.enabled`. EMB-CLP-05: `LoyaltyAccrualListener` (`loyalty.listener`, `@EventListener` on `PaymentCompleted`) resolves tenant via `TenantContextHolder.requireTenantId()` (the event carries no tenantId field, unlike `ParticipantJoined`), reads `SettingsPayload.LoyaltySettings` via `SettingService.getSettings`, no-ops if `enabled=false`; resolves each `BillSplit.participantName` against `SessionService.findById(sessionId).getParticipants()` (`Participant.name`→`userId`) — a split with no matching participant is silently skipped, not an error. `LoyaltyAccountService.credit(account, points, reason, billId)` (new, `@Transactional`, added `LoyaltyTransactionRepository` dependency) is now the one path that bumps `totalPoints` — always paired with one `LoyaltyTransaction` ledger row in the same call, so the two can never drift apart. No split-status filtering needed in the listener: `PaymentCompleted` only fires (from `PaymentService`, both firing sites) once every `BillSplit` on the bill is already `PAID`. EMB-CLP-06: `LoyaltyRewardController`/`LoyaltyRewardService` (new `loyalty.controller`/`loyalty.dto` packages) — CRUD relies solely on `LoyaltyReward`'s existing `@TenantId` Hibernate filter for tenant scoping (no explicit tenantId re-check needed on lookup, unlike `User`-touching services), same pattern as `CategoryService`/`MenuItemService`; `list` reuses the pre-built `LoyaltyRewardRepository.findByTenantId`. No dedicated service/controller test added — spec §5 only scopes `SecurityAuditTest` rows to this task.
- EMB-MOD backlog (brainstormed 2026-08-22, spec `docs/superpowers/specs/2026-08-22-emb-mod-design.md`, plan `docs/superpowers/plans/2026-08-22-emb-mod.md`, MOD-01/MOD-02 COMPLETE reports 182–183): reusable modifier groups (`ModifierGroup`/`ModifierOption`, tenant-scoped on the group) attached to `MenuItem` via an explicit join entity `MenuItemModifierGroup` (plain `menuItemId`/`groupId` columns, not JPA associations — mirrors `PrintJob.sourceId`'s "plain reference" convention), `V13__modifiers.sql`. Selection rules (`SINGLE_REQUIRED`/`MULTI_OPTIONAL`/`MULTI_LIMITED`, `minSelections`/`maxSelections`) are fixed per-group, not per-assignment. Options only ever ADD to price (`priceDelta >= 0`, never negative/override). Soft-deactivate only (`active` flag), never hard-delete — `OrderItem.modifiers` (added in MOD-03) is a snapshot, so a later catalog edit never changes an already-placed order. Payment gateway (EMB-GATEWAY) architecture question resolved during this backlog's kickoff conversation but the backlog itself stays deferred: `PaymentGatewaySettings.provider`/`secretRef` is already per-tenant, so a future `PaymentGatewayAdapter` can be built generically (GATEWAY-02) without picking one bank up front — not yet started.
- MOD-02 (report 183): `ModifierGroupController` lives at `/catalog/modifier-groups` (route-prefix correction vs. the spec's literal `/admin/...` text — real controllers gate mutations with method-level `@PreAuthorize`, not a path prefix, same as `CategoryController`/`MenuItemController`). `MenuItemResponse.from` now REQUIRES a `modifierGroups` list arg (no zero-arg overload) — any future new call site must supply `modifierGroupService.findActiveGroupsForMenuItem(itemId)`. `ModifierGroupService.findActiveGroupsForMenuItem` is the one shared read path for admin catalog AND the customer digital menu (`MenuService.getMenu`) — filters to active groups + active options only. Adding a constructor dependency to `MenuItemService` broke `MenuItemServiceTest`'s `@InjectMocks` (null mock → NPE) even though that test wasn't in the plan's file list — the fix-pattern is: add the new `@Mock`, stub it only in the test methods that actually reach the new code path (strict stubbing rejects unnecessary stubs).
- MOD-03 (report 184): `SessionService.addItem` gained a 4th param `selectedOptionIds: List<Long>`; the new private `resolveSelectedModifiers` validates each ID against the menu item's `modifierGroups` (rejects unknown option IDs and out-of-range per-group selection counts via `InvalidModifierSelectionException`→400, the one cart-selection exception that isn't a 409) and returns the `SelectedModifier` snapshot list stored on `OrderItem.modifiers`; stored `OrderItem.price` = `menuItem.getPrice()` + sum of selected deltas. `AddItemRequest` defaults `selectedOptionIds` to `List.of()` via a compact constructor when the client omits it, so existing no-modifier callers are unaffected. **Two call sites outside the plan's stated file list needed the same signature update and would NOT compile otherwise** — `SessionControllerTest.java` (`sessionService.addItem(...)` mock + `new AddItemRequest(...)` x3) and `E2EOrderFlowTest.java` (`new AddItemRequest(...)` x1) — found by grepping every `addItem(`/`AddItemRequest(` call site repo-wide rather than trusting the plan list; same "grep live code, don't trust a stale plan list" lesson as MOD-02's `MenuItemServiceTest` gap.
- MOD-04 (report 185): admin UI at `/admin/modifier-groups` (`ModifierGroups.tsx`), `NewModifierGroupModal`/`EditModifierGroupModal` (name/selection-type/min-max + `useFieldArray` options), `ModifierGroupAssignmentField` (checkbox+reorder picker) reused by both `NewMenuModal`/`EditMenuModal`, which call `modifierGroupService.assignToMenuItem` in their mutation's `onSuccess`. **Gap not in the plan's file list:** `frontend/src/components/ui/checkbox.tsx` didn't exist — built on the already-installed unified `radix-ui` meta-package (re-exports `Checkbox`, no new dependency), matching the newer function-component/`data-slot` convention (`separator.tsx`/`tabs.tsx`), not the older `forwardRef` style `switch.tsx` still uses. `EditModifierGroupModal`'s per-option `Switch` is **one-directional** (bound to `deactivateOption` only, `disabled` once off) — the backend has no reactivate-option endpoint (`updateOption` never touches `active`), only `setActive` at the group level. Zod schema uses `.optional()` not `.nullable()` for `minSelections`/`maxSelections` (matches the generated `ModifierGroupRequest` type), avoiding an `as any` cast.
- MOD-05 (report 186): customer selector `SelectModifiersModal.tsx`, opened from `Menu.tsx`'s "+" button only when `item.modifierGroups.length > 0` (no-modifier items still add straight to cart, unchanged path). `RadioGroup` for `SINGLE_REQUIRED`, `Checkbox` list capped at `maxSelections` for `MULTI_OPTIONAL`/`MULTI_LIMITED`; submit disabled until every group's `minSelections` is satisfied; live total = `item.price` + selected `priceDelta`s. `SessionTableService.addItem` gained an optional 3rd param `selectedOptionIds: number[]`, sent as `{menuItemId, selectedOptionIds}` — matches `AddItemRequest`'s existing compact-constructor default from MOD-03, no backend change needed. **Same missing-shadcn-component gap as MOD-04:** `frontend/src/components/ui/radio-group.tsx` didn't exist, built the same way (unified `radix-ui` package's `RadioGroup` export, `data-slot` convention, no new dependency) — this is now the established pattern for any future missing shadcn primitive in this codebase.
- MOD-06 (report 187, EMB-MOD backlog COMPLETE): `KitchenItem` gained `modifiers: List<String>` — flat option display names only, no price, matching `KitchenItem`'s existing price-free shape; populated in `KitchenService.handleOrderItemAdded` via `OrderItem.getModifiers().stream().map(SelectedModifier::getOptionName)`. `PrintingEventListener.renderKitchenPayload` renders each as an indented `  · <optionName>` sub-line under the item name. `QueueCard.tsx`/`FocusedCard.tsx` render a comma-joined modifiers line under the item name, conditionally on non-empty. Extended the pre-existing `KitchenServiceTest` (found via `Glob` per the plan's step 5 instruction) rather than creating a new test file. No new shadcn component or i18n keys needed — read-only backend + display-only frontend, no new form inputs or user-facing copy beyond the modifier names themselves (which are already-translated catalog data, not UI strings).
- EMB-INV backlog (COMPLETE, brainstormed 2026-08-22, spec `docs/superpowers/specs/2026-08-22-emb-inv-design.md`, plan `docs/superpowers/plans/2026-08-22-emb-inv.md`, executed task-by-task at the user's request, reports 188–192): stock is tracked per-`MenuItem` only (no per-ingredient/recipe model), opt-in via `InventoryItem` (no row = untracked, never decremented, never auto-86'd). Decrements on `KitchenItemsConfirmed` (kitchen confirmation), not on `PaymentCompleted`. `InventoryService.applyStockSideEffects` is the ONE shared method (kitchen-decrement, admin restock, admin create) that auto-86s/un-86s `MenuItem.available` and broadcasts `LOW_STOCK` on `/topic/inventory/{tenantId}` — unconditional, no system-vs-admin distinction. `InventoryItemRepository.applyClampedDelta` is a single atomic `@Modifying` JPQL `UPDATE` clamping at zero in SQL — no `@Version` needed. `/catalog/inventory` CRUD (`InventoryItemController`, class-level `@PreAuthorize("hasRole('ADMIN')")`, one deliberate deviation from the spec's per-method table — mirrors `AnalyticsController`, no inventory route has a legitimate non-admin caller) backs the admin UI at `/admin/inventory` (`Inventory.tsx`, page-scoped STOMP lifecycle mirroring `KitchenLayout`/`WaiterLayout` since no other admin page opens the connection). Frontend's `inventorySubscription`/`subscribeToInventory` WebSocket slot is **dedicated**, never reuses `currentSubscription` (shared by session/kitchen/waiter — the exact bug report 123 fixed for the waiter subtree).
- **WebSocket endpoint isolation gotcha (bugfixes, reports 193+194 — READ THIS before touching any `@EnableWebSocketMessageBroker` config):** Spring does NOT give a second `@Configuration implements WebSocketMessageBrokerConfigurer` its own isolated broker/channel, even with its own `@EnableWebSocketMessageBroker` — it collects EVERY such bean and applies ALL of their `registerStompEndpoints`/`configureClientInboundChannel`/`configureMessageBroker` callbacks onto ONE shared `clientInboundChannel`/`MessageBrokerRegistry`. EMB-PRINT's `PrintAgentWebSocketConfig` assumed otherwise (its own doc comment said so) and shipped TWO stacked regressions from that one wrong assumption: (1, report 193) `PrintAgentChannelInterceptor` ran on every `/ws` CONNECT too, rejecting every tenant JWT with `"Not a print-agent token"`; (2, report 194) `configureMessageBroker`'s `registry.enableSimpleBroker("/topic/print-agent")` call SILENTLY REPLACED (not merged with) `WebSocketConfig`'s `enableSimpleBroker("/topic", "/user")` — `MessageBrokerRegistry#enableSimpleBroker` overwrites its single registration on every call — so even after (1) was fixed, CONNECT/SUBSCRIBE/heartbeats worked but ZERO application broadcasts were ever delivered anywhere. Together these silently broke ALL realtime features (customer cart, KDS, waiter floor, inventory alerts) since report 180, undetected until a user manually reported it (no `claude-in-chrome` browser access in any session since). Fix (1): tag sessions at handshake time (`WebSocketSessionAttributes` + `PrintAgentHandshakeInterceptor` on `/ws/print-agent` only) so `PrintAgentChannelInterceptor`/`JwtChannelInterceptor` no-op for sessions that aren't theirs. Fix (2): `WebSocketConfig.configureMessageBroker` is now the ONE place registering broker prefixes — `enableSimpleBroker("/topic", "/user", "/topic/print-agent")` — and `PrintAgentWebSocketConfig` no longer overrides that method at all. **Any future second/third STOMP endpoint must follow both patterns: handshake-tag for interceptor scoping, and register ALL broker/destination prefixes from the ONE owning config — never assume a second config class adds to instead of replacing the shared registry.** `WebSocketEndpointIsolationTest` (`@SpringBootTest(RANDOM_PORT)` + real STOMP clients, including one that broadcasts via `SimpMessagingTemplate` and asserts delivery) is the only test shape that catches either bug class — a unit test of one interceptor in isolation cannot.

## Task Queue Status
- [x] **Completed through report 179 (Milestones 1–5, EMB-PC, EMB-LP, EMB-CR, EMB-PAY, EMB-STAFF, EMB-RV, EMB-CLP, EMB-CLH, EMB-i18N-01..07, EMB-ACC built-then-reverted, plus misc bugfixes/UI polish):** full core platform — multi-tenancy, catalog, session/cart, KDS, waiter/floor + cash register, refunds/voids, payment cycle, staff CRUD, admin analytics, loyalty program + history, platform-operator console, Astro landing site, i18n infra (ES/EN), settings sidebar (billing/ticket/loyalty subtabs). See Active Context bullets above for architecture/decisions detail.
- [x] **EMB-i18N-08 (COMPLETE, report 181):** Validation (zod) + toast (`react-hot-toast`) copy, all roles. See Active Context bullet for the schema-factory pattern.

### SaaS feature-gap initiative (brainstormed 2026-08-22) — closes gaps before Ember Hub (see `docs/superpowers/specs/ember_hub.md`)

**EMB-PRINT — Hardware Bridge / impresión ESC-POS real** (COMPLETE, report 180, PR #47 — see Active Context bullet for architecture)
- [x] PRINT-01: Contrato `PrintJob` (schema + estados PENDING/SENT/PRINTED/ERROR) y protocolo del agente (WS saliente).
- [x] PRINT-02: Backend — módulo `printing`, cola de jobs, endpoint gateway WS.
- [x] PRINT-03: Backend — publicar `PrintJob` en cierre de bill (recibo) y creación de comanda (cocina), reusando el template de Ticket Settings.
- [x] PRINT-04: Agente local standalone (Java) — conexión saliente, resolución impresora IP:9100/USB, conversión ESC-POS, ACK.
- [x] PRINT-05: Admin UI — configuración de impresoras por restaurante.
- [x] PRINT-06: Reintentos/cola offline si el agente no está conectado.
- [ ] PRINT-07: Prueba end-to-end con impresora física. **Sin verificar — no hay hardware disponible.**
- [x] PRINT-08: Seguridad/aislamiento por tenant de la conexión WS del agente (token propio, no JWT de usuario).

**EMB-GATEWAY — Pasarela de pago real** (spec pendiente, bloqueado por GATEWAY-01)
- [ ] GATEWAY-01: Decisión bloqueante — confirmar proveedor(es) disponibles/certificados en Nicaragua.
- [ ] GATEWAY-02: Interfaz `PaymentGatewayAdapter` desacoplada por tenant/país (reemplaza el stub).
- [ ] GATEWAY-03: Implementación concreta del proveedor elegido.
- [ ] GATEWAY-04: Webhooks/confirmación asíncrona.
- [ ] GATEWAY-05: Reconciliación de pagos pendientes/fallidos.
- [ ] GATEWAY-06: Conectar la tab de settings `paymentGateway` ya existente al adapter real (no requiere UI nueva).

**EMB-MOD — Modificadores de producto** (spec + plan aprobados, `docs/superpowers/specs/2026-08-22-emb-mod-design.md` / `docs/superpowers/plans/2026-08-22-emb-mod.md`)
- [x] MOD-01: Modelo `ModifierGroup`/`ModifierOption`/`MenuItemModifierGroup` (report 182).
- [x] MOD-02: Backend CRUD admin de modificadores (report 183).
- [x] MOD-03: Backend — carrito/orden captura modificadores + ajuste de precio (report 184).
- [x] MOD-04: Admin UI de gestión (report 185).
- [x] MOD-05: Cliente — selector de modificadores al agregar ítem (report 186).
- [x] MOD-06: Propagar modificadores a KDS y ticket impreso (report 187).

**EMB-INV — Inventario básico** (spec + plan aprobados, `docs/superpowers/specs/2026-08-22-emb-inv-design.md` / `docs/superpowers/plans/2026-08-22-emb-inv.md`)
- [x] INV-01: Modelo `InventoryItem` (stock, unidad, umbral bajo-stock). (report 188)
- [x] INV-02: Descuento de stock en `PaymentCompleted`/confirmación de orden. (report 189)
- [x] INV-03: Alertas de bajo stock. (report 189/190/192)
- [x] INV-04: Admin UI de inventario. (report 191/192)
- [x] INV-05: Auto-86 (`MenuItem.available=false`) al llegar a stock 0. (report 189)

**Restaurant Onboarding — Admin wizard + waiter tour** (spec + plan aprobados, `docs/superpowers/specs/2026-08-24-restaurant-onboarding-design.md` / `docs/superpowers/plans/2026-08-24-restaurant-onboarding.md`, frontend-only, zero backend changes)
- [x] onboard-01: Add `react-joyride` dependency. (report 202)
- [x] onboard-02: `useOnboardingGate` hook. (report 203)
- [x] onboard-03: i18n keys for the admin wizard. (report 204)
- [x] onboard-04: `AdminOnboardingWizard` component. (report 205)
- [x] onboard-05: Wire the gate into `AdminLayout`. (report 206)
- [x] onboard-06: `waiterTourStore` (per-user seen flag). (report 207)
- [x] onboard-07: i18n keys for the waiter tour. (report 208)
- [x] onboard-08: `WaiterTour` component + `Tables.tsx` anchors. (report 209)
- [x] onboard-09: Manual verification + `PROGRESS.md` update. (reports 210–213 — 2 bugs found+fixed, 2 UX follow-ups)

**Ember Hub — HUB-01: Portable DB bootstrap + licensing** (spec `docs/superpowers/specs/ember_hub.md`, plan `docs/superpowers/plans/2026-08-24-hub-01-bootstrap-and-licensing.md`, backend-only, new `com.vanter.ember.hub` package gated by `@Profile("hub")`, Windows-only v1. Verified 2026-08-25 against current code: zero drift — `EmberApplication.java` still plain, `GracePeriodInterceptor`'s target paths match `SessionController`/`BillingController`'s real `@PostMapping`s, `docker-compose.yml` is `postgres:16` matching the plan's EDB version note. Executed task-by-task under CLAUDE.md's 1-Task-1-Context lifecycle, not the plan's own suggested subagent-driven-development, to stay consistent with the rest of this branch's session history.)
- [x] HUB-01-01: OSHI dependency + `HubProperties`. (report 223)
- [x] HUB-01-02: `HardwareFingerprintService`. (report 224)
- [x] HUB-01-03: `LicenseKey`/`LicenseKeyParser`/`InvalidLicenseException` (RSA sign+verify). (report 225)
- [x] HUB-01-04: `HubState`/`HubStateStore` (local JSON, Postgres-independent). (report 226)
- [x] HUB-01-05: `LicenseService` (activate/validate, hardware-lock, grace period). (report 227)
- [x] HUB-01-06: `PortableDatabaseBootstrap` (port-in-use/initdb/pg_ctl, actionable boot errors). (report 228)
- [x] HUB-01-07: Wire pre-Spring bootstrap into `EmberApplication.main` + `application-hub.yml`. (report 229)
- [x] HUB-01-08: `HubBeansConfig` (profile-gated Spring beans). (report 230)
- [x] HUB-01-09: `GracePeriodInterceptor` (blocks order/payment writes past the 4-day grace period). (report 231)
- [x] HUB-01-10: `HubTrayIcon` (minimal system tray presence). (report 232)
- [x] HUB-01-11: Manual end-to-end verification on real Windows + portable Postgres — 3 real bugs found+fixed along the way. (reports 233–236, HUB-01 COMPLETE)

**Ember Hub — post-HUB-01 (not yet its own numbered plan):**
- [x] feat-hub-dashboard-launcher: v1 Swing start/stop dashboard, replaces auto-boot as the distributable's entry point; fixed a real orphaned-Postgres-on-exit bug along the way. (report 237)
- [x] Bundle compiled `frontend/` into the backend jar's static resources, mounted at `/app/**`. (report 238)
- [x] **Design gap CLOSED:** license↔tenant↔admin provisioning — implemented end-to-end by the 10-task `docs/superpowers/plans/2026-08-25-hub-license-activation.md` (Tasks 1–9, reports 239–247; see Task Queue Status below). Final whole-branch review (report 248) then fixed the one real Critical bug the plan's own design assumption had: `Restaurant.builder().id(licenseRestaurantId)...build()` + `save()` throws against real Hibernate (`GenerationType.UUID` rejects a pre-assigned id) — fixed with a native `insertWithId` query.
- [ ] v2: Tauri + webview shell reusing `frontend/`'s real design — needs its own plan, not started. (brainstormed 2026-08-26, deliberately deferred as its own sub-project — see bullet below)
- [ ] Windows service auto-start (`sc.exe` + SCM recovery, spec §2.3 "Recuperación ante caídas") — not started, depends on the jpackage/jlink installer below.
- [ ] `jpackage`/`jlink` embedded-JRE `.exe` installer (spec §2.3) — not started.
- [x] **Gap found 2026-08-25, CLOSED (report 257):** portable MinIO bootstrap (`PortableMinioBootstrap`, mirrors `PortableDatabaseBootstrap`) + a third `HubDashboard` status label. Local-only creds (`ember-hub`/`ember-hub-local`), no web console exposed.
- [x] **Manually verified end-to-end against a real `minio.exe` (report 258):** 2 real bugs found+fixed (`createdb -h localhost` IPv6 hang; uploaded images 403'd with no bucket policy) — see Active Context / report 258 for detail.

**Ember Hub — hub-license-activation** (plan `docs/superpowers/plans/2026-08-25-hub-license-activation.md`) — COMPLETE: all 10 tasks + final whole-branch review fix wave (reports 239–248, verified live 256). See Active Context bullet for architecture/decisions.

### EMBER-FIX — Cash shift expiry & forced daily close — COMPLETE (report 259, all 12 tasks squashed into `78f5fe9`, on `main`)
Computed till expiry (`closeTime` + 2h; 12h fallback), unlimited 1h prolongs, 409 block on new physical payments/movements once overdue (digital + table service untouched), `<CashShiftSentinel/>` (pre-warning/overdue/stale modals), non-blocking logout warning. Spec/plan under `docs/superpowers/{specs,plans}/2026-08-28-cash-shift-expiry-forced-close*`. **Not yet done:** live browser click-through of sentinel / overdue-logout / 409 toasts (no `claude-in-chrome`).

### Ember Hub — Fase 2, Sub-project A1: License Heartbeat (spec `docs/superpowers/specs/2026-08-28-hub-license-heartbeat-design.md`, plan `docs/superpowers/plans/2026-08-28-hub-license-heartbeat.md`)
Makes the 4-day offline grace actually reset on cloud contact + lets `/console` suspension stop a Hub after a 48h courtesy grace. Periodic Hub→cloud `POST /hub-heartbeat` (5 min), cloud checks license sig + hardware fingerprint vs `HubActivation` + `Restaurant.status`, answers OK/SUSPENDED. **Branch `feat/hub-license-heartbeat` from `main`. One task per context window, then `/clear`.** Reports 265+.
- [x] HEARTBEAT-01: `HubState.suspendedSince` (4-arg canonical + 3-arg back-compat ctor + `withHeartbeatNow()`/`withSuspendedSince()`); `LicenseService.recordHeartbeatSuccess` uses the helper; `HubStateStoreTest` round-trip + legacy-null-load. (report 265)
- [x] HEARTBEAT-02: `LicenseService.isSuspendedGraceExpired`/`recordSuspended` + 6th ctor arg `Duration suspendedGrace`; `HubProperties` +`heartbeatUrl`/`suspendedGraceHours` (back-compat 11-arg ctor); `HubBeansConfig` + `HubBootstrapRunner` wire the Duration. (report 266)
- [x] HEARTBEAT-03: cloud `HubHeartbeatService` + `HubHeartbeatRequest`/`Response` DTOs. **`@Lazy LicenseIssuingService` on a hand-written ctor param, NOT `@RequiredArgsConstructor`** (reports 241/243). Status = OK if `Restaurant.status==ACTIVE` else SUSPENDED. (report 267)
- [x] HEARTBEAT-04: cloud `HubHeartbeatController` (`POST /hub-heartbeat`, `permitAll`, `@Profile("!hub")`) + `SecurityConfig` permitAll + `/hub-heartbeat` in `RateLimitProperties.paths`. `SecurityAuditTest` unchanged (401-matrix only, doesn't enumerate permitAll). `HubHeartbeatControllerTest` +3. (report 268)
- [x] HEARTBEAT-05: Hub `com.vanter.ember.hub.sync.HeartbeatScheduler` (`@Profile("hub")`, `@Scheduled(fixedDelayString="${ember.hub.heartbeat-interval-ms:300000}")`, all exceptions swallowed in the method) + `@EnableScheduling` on `HubBeansConfig` + interval prop in `application-hub.yml`. Test via `com.sun.net.httpserver.HttpServer` (4: OK/SUSPENDED/500/blank-URL). Full suite 889/889. (report 269)
- [x] HEARTBEAT-06: `GracePeriodInterceptor` also blocks on `isSuspendedGraceExpired` with a distinct `license_suspended` body (keeps `license_grace_period_expired` for the no-contact case). `GracePeriodInterceptorTest` +2. Full suite 891/891. (report 270)
- [x] HEARTBEAT-07: `HubDashboard` license status line + `.env.example` (`EMBER_HUB_HEARTBEAT_URL`/`_INTERVAL_MS`/`_SUSPENDED_GRACE_HOURS`, `hub.latest-version`). Full suite 891/891. Code complete on branch; manual 2-process smoke + PR to `main` PENDING operator. (report 271)

### Hosted Production Deployment (spec `docs/superpowers/specs/2026-08-28-hosted-production-deployment-design.md`, plan `docs/superpowers/plans/2026-08-28-hosted-production-deployment.md`)
Branch `feat/hosted-production-deployment` off `main` `040bb72`. Phases: 1–2 repo code (maintainer), 3+5 GCP runbook (maintainer), 4 Cloudflare runbook (colleague). B1 E2E runs in parallel.
- [x] HPD-01: `minio.public-url` config + `ImageUploadService` builds URL from it, drops `bucketName` param (fixes 3-way bucket-name drift), robust `deleteImage`, immutable `Cache-Control`. Also unblocks Hub (report 258). (report 272 — 893/893)
- [x] HPD-02: `PublicPingController` — unauthenticated `GET /v1/public/ping` (body `pong`, `text/plain`) for the external uptime check. Covered by existing `/public/**` permitAll. (report 273 — 894/894)
- [x] HPD-03: `application-prod.properties` — `management.server.port=8081`, `forward-headers-strategy=framework`, `health.show-details=never`. Config-contract test (`ProdManagementPortConfigTest`) per plan fallback. (report 274 — 897/897)
- [x] HPD-04: `AuthRateLimiterFilter` trusts `CF-Connecting-IP` behind a configured proxy (inside the existing `trustedPeer` gate, XFF stays as fallback). + security-review pass. (report 275 — 900/900)
- [x] HPD-05: `deploy/docker-compose.prod.yml` (`postgres`/`app`/`cloudflared`/`backup`) + `deploy/.env.prod.example`. `docker compose config -q` exit 0. (report 276 — suite unchanged 900/900, no code)
- [x] HPD-06: `deploy/backup/` (`Dockerfile` + `backup.sh`) — nightly `pg_dump -Fc | gzip` → GCS `postgres/` + Sunday `postgres-weekly/`, 14-daily/8-weekly prune, keyless SA auth, cron `0 8 * * *`. Dry run vs dev Postgres → non-empty dump. (report 277 — suite unchanged 900/900, no code)
- [x] HPD-07: `deploy/deploy.sh` (Secret-Manager `.env` push + `compose pull`/`up -d` + 20×6s health-gate, all over IAP SSH) + `deploy/RUNBOOK.md` skeleton (topology, HPD-11..19 provisioning index, deploy/recovery/rotation/restore-test sections). shellcheck exit 0. (report 279 — 278 taken on `feat/hub-packaging-installer`; suite unchanged 900/900, no code)
- [x] HPD-08: `.github/workflows/backend-image.yml` — `build` job: ghcr login + `docker/build-push-action@v6` context `backend`, push `:<version>`+`:latest` on `v*` tag (`:<short-sha>` only on manual `workflow_dispatch`). No `setup-java`/pre-package step — `backend/Dockerfile` is multi-stage and self-compiles. `actionlint` exit 0. (report 280 — suite unchanged 900/900, no code)
- [x] HPD-09: `frontend/scripts/gen-env-config.mjs` + `build:pages` script (env-config.js for Pages). (report 281 — frontend build/test 41/41/lint 0, backend untouched 900/900)
- [x] HPD-10: landing `ContactForm.tsx` real `fetch('/api/contact', POST)` (replaces the 900ms fake) + new `landing/functions/api/contact.ts` Pages Function (JSON parse→400, trim+`EMAIL_RE` validate→400, forward `{text}` to `CONTACT_WEBHOOK_URL` if set, else 204) + `astro.config.mjs` `site:` `.com`→`.net`. `pnpm build` OK (5 pages, sitemap; Functions not bundled by Astro — expected). (report 282 — landing standalone, backend/frontend suites untouched)
- [x] HPD-11: GCP — project `ember-prod-vanter` + APIs, IAP-SSH-only firewall (broad SSH/RDP deleted), VM `ember-prod` (e2-medium/Ubuntu 24.04, **external IP for egress — plan `--no-address` deviation, no Cloud NAT**), 2G swap, Docker 29.1.3 + compose v2, compose stack staged in `/opt/ember`. (report 283 — RUNBOOK §GCP filled, docs-only, suites unchanged)
- [x] HPD-12: GCP — bucket `ember-backups-ember-prod-vanter` (private, PAP enforced, versioning on; **`US` multi-region — plan said `us-central1`**), VM compute SA granted `roles/storage.objectAdmin` on it, daily disk snapshot policy `ember-daily-snap` (7d, 07:00 UTC) attached to `ember-prod`. (report 284 — RUNBOOK §GCP filled, docs-only, suites unchanged)
- [x] HPD-13: GCP — image `v0.1.0`→GHCR (private, VM `docker login` w/ read:packages PAT), `ember-prod-env` secret (real rotated auth secrets + fresh Hub keypair; `TUNNEL_TOKEN`/`MINIO_*` placeholder for HPD-20), `deploy.sh latest`→`app healthy`, Flyway V1–V5 `success=t`, `backup.sh`→`postgres/2026-08-29.dump.gz`. Ships a health-check-port fix (`:8080/v1`→`:8081`) + RUNBOOK provisioning/rotation sections. (report 285)
- [x] HPD-14 (GCP-side): Ops Agent 2.70.0 (apt repo — bootstrap script 404s) + Prometheus scrape of `:8081/actuator/prometheus` + email channel + CPU/mem/disk alert policies; config & JSON in `deploy/monitoring/`. **Uptime check on `/v1/public/ping` + its alert + E2E test → HPD-20** (needs the live Tunnel). (report 286)
- [ ] HPD-15: CF — `ember.vanter.net` zone delegation (NS from `vanter.net`) + DNS records.
- [ ] HPD-16: CF — R2 `ember-media-prod` + `cdn.ember.vanter.net` custom domain + scoped API token → GCP track.
- [ ] HPD-17: CF — Tunnel `ember-prod`, public hostname `api.ember.vanter.net → app:8080`, `TUNNEL_TOKEN` → GCP track.
- [ ] HPD-18: CF — Worker `ember-app` (static assets, root `frontend`, `build:pages`) at `app.ember.vanter.net` + `ember` Worker keeps the landing at `ember.vanter.net`. **Repo side DONE — `frontend/wrangler.jsonc` + env-script fix (report 321).** Remaining: create the `ember-app` Worker in CF connected to Git, set build vars, move the `app.` custom domain off `ember`.
- [ ] HPD-19: CF — HSTS/Always-HTTPS, managed WAF, rate-limit `/v1/auth/*`, block `/v1/actuator/*`.
- [ ] HPD-20: Integration — real `TUNNEL_TOKEN`+R2 into `ember-prod-env`, redeploy, full infra smoke checklist, **+ the HPD-14 uptime check on `api.ember.vanter.net/v1/public/ping` + its alert policy + the stop-`app`-6-min alert test** (`deploy/monitoring/README.md`).
- [ ] HPD-21: Restore test — latest GCS dump → throwaway `pg_restore`, document real-disaster procedure in RUNBOOK.
- [ ] HPD-22: E2E product walkthrough on `app.ember.vanter.net` (2 devices, full order flow) + PROGRESS.md close-out.

### Landing — SEO & customer acquisition (advised 2026-08-30, reports 310–316 shipped the on-page/technical base)
On-page/technical SEO is essentially done (title/desc/canonical/hreflang/OG/JSON-LD/sitemap-i18n/`<main>`/skip-link/real content). What's left is config, content and off-page — most of it NOT in the codebase. Reality: SEO won't drive the first 20–50 customers (those come from outreach / WhatsApp / referrals / local ads); it's the compounding long-term channel, start now.

**LSEO — Now (config, ~1–2 h, off-code)**
- [ ] LSEO-01: Google Search Console — verify the domain, submit `sitemap-index.xml`, watch coverage/queries.
- [ ] LSEO-02: Bing Webmaster Tools — same.
- [ ] LSEO-03: Confirm `PUBLIC_FRONTEND_URL` is set in the Cloudflare Pages project — otherwise every register CTA points to `localhost` in prod (build warns, report 312).
- [ ] LSEO-04: Google Business Profile for Vanter (service-area business; NAP identical to the footer).
- [ ] LSEO-05: Run PageSpeed Insights + Rich Results Test on the deployed URL; fix findings (hero LCP, structured data). Confirm 404 returns HTTP 404 on Pages.

**LSEO — Content (the real traffic lever; Spanish first)**
- [ ] LSEO-06: Stand up a blog/recursos section + keyword research (autocomplete, "otras preguntas", later Search Console).
- [ ] LSEO-07: ~8–15 articles, one query each: "software para restaurantes Nicaragua/CA", "carta/menú QR", "qué es un KDS", "dividir la cuenta sin errores", "cierre de caja y arqueo", "cuánto cuesta un sistema para restaurante", comparativas "Ember vs Fudo/Loyverse/POS tradicional".
- [ ] LSEO-08: Case studies / testimonios once there are customers (also E-E-A-T).

**LSEO — Conversion (traffic → clientes)**
- [ ] LSEO-09: Real contact/demo form (Formspree or Web3Forms) — replaces the `mailto` (was deferred as landing task-L2's sibling).
- [ ] LSEO-10: WhatsApp contact button (strongest B2B channel in NI).
- [ ] LSEO-11: landing **task-L2** — hero screencast video (needs the clip; shows "tiempo real" instead of claiming it).
- [ ] LSEO-12: Retargeting pixel (Meta/Google) added now to build an audience before paid.
- [ ] LSEO-13: Honest social proof near the primary CTA (founder note / "sin tarjeta" / early-adopter offer).

**LSEO — Authority / off-page (new domain ≈ 0)**
- [ ] LSEO-14: Software-directory listings (Capterra, GetApp, G2, SoftwareAdvice) + local NI/CA directories.
- [ ] LSEO-15: Product Hunt launch.
- [ ] LSEO-16: Social profiles (LinkedIn company, IG, FB) linked from the footer → then add `sameAs` to the Organization JSON-LD.
- [ ] LSEO-17: Partnerships — POS-hardware resellers, gastronomy/tourism chambers, local tech press. Reviews on GBP + directories.

**LSEO — Minor in-code tweaks (do on request)**
- [ ] LSEO-18: `BreadcrumbList` JSON-LD on subpages; `AggregateOffer` with the real prices; `VideoObject` once videos exist; `sameAs` once socials exist; decide `noindex` on `/info/videos` while it's placeholder-only.

### Payment-flow bug cluster (user-reported 2026-08-30, root-caused in report 317)
- [x] Task A — `GET /billing/sessions/{sessionId}/bill` fetch endpoint; waiter + customer bill views rehydrate without a live `BILL_READY` frame (report 317).
- [x] Task B — block removing `READY`/`DELIVERED` items (backend `removeItem` guard + disable the trash button in `TableInformation.tsx`). (report 318)
- [x] Task C — waiter "settle & close" for a partially-paid session: on a diner abandoning mid-payment, redistribute their share across participants still present and announce it to the table + waiter. (report 319)
- [x] Task D — `POST /sessions/{id}/leave` + "abandonar mesa" button; reject joining a 2nd table while already in an OPEN session; restore session + tenant-scoped token on customer re-login instead of bouncing to `/customer/home`. Abandoning with non-draft items leaves them on the table bill. (report 320)