# PROGRESS.md — Active Execution State

## Current Execution State
- **Last Completed Task:** report 472 — **`/waiter/tables` grid: white bg on the participant-count badge**; user also flagged the r471 disabled-when-occupied fix as "missing for admin" — verified in code there's no separate admin view/behavior (`Tables.tsx` is shared, `/cash-shifts/current` is `WAITER,ADMIN`-scoped identically) and flagged that back rather than guess a second fix. No test/behavior regression, full suite 135/135, both builds clean.
- **Prev (compressed r470–471):** removed dead "Cobrar mesa"/"Imprimir cuenta" (r470, `WaiterTour` dangling step + 4 orphaned i18n keys also dropped) → "Asignar mesa" primary/red + disabled when occupied (r471). `Tables.tsx` serves both WAITER/ADMIN via one shared route.
- **Prev (compressed r462–469):** icon rebrand (r462) → `build-print-agent` CI fix + npm→pnpm conversion (r463) → **KDS-BULK-STATUS-UPDATE** done (r464–467: `updateItemsStatus` ordinal walk, bulk endpoint, API/i18n, `FocusedCard.tsx` select-all/checkbox/dropdown UI) → dropped dead "Cliente"/"Anular", wired "Imprimir" as manual reprint (r468) → collapsed header into one row (r469).
- **Prev:** report 461 — **Live-testing bug fixes (not EMBER-HUB-V2): QR "iniciar sesión" join broken; export date picker allowed future dates**. QR join (`MenuJoin.tsx`): the authenticated join path (`POST /sessions/{id}/join`) is real and correct — root cause was `submit()`'s `catch` unconditionally clearing the parked QR token and hard-redirecting to `/customer` (account home) on **any** error, not just an actually-dead QR (a 409 already-seated-elsewhere/full table, or a transient blip, both realistic given the login round-trip has to fit the QR's 15min TTL) — so the join never completed and the waiter only ever saw whichever guest attempt succeeded instead. Fixed to only clear+redirect on a genuine 404; anything else keeps the user on the join screen to retry. Also pre-fills the name field with the account's own name (friction + visually distinguishes it from the guest screen's always-empty field). Export (`ExportSettings.tsx`, single component shared by cloud+Hub): added `max={today}` to both date inputs (there was no `max` at all). Password-reset-on-login explicitly deferred by the user. `pnpm run test:run` **128/128** (+4 new), `build`/`build:hub` both clean.
- **Prev:** report 460 — **EMBER-HUB-V2 UX: move "license.key no encontrado" out of the PostgreSQL card**: that message is attributed to `postgres.ERROR` by the backend (blocks everything before Postgres itself starts, per the orchestrator's own pre-existing comment) but reads as a Postgres bug in its own card. `Dashboard.tsx` now detects `postgres===ERROR && license.status===NONE`, passes `error=null` to the Postgres `ServiceCard` instead, and renders the real message as a red (`bg-primary`, matching the header's buttons — no separate "destructive" variant exists) banner right below `LicenseCard`. `ServiceCard`'s `hasContent` fixed to not render an empty chip when `error` is intentionally `null` on `ERROR` (general-purpose fix, not license-specific). `npm test` 13/13 (+1). Verified live: fresh install with license deleted — Postgres card shows only the "Error" badge, banner appears under Licencia with the real text.
- **Prev:** report 459 — **EMBER-HUB-V2 polish: sanitize simulated logs + fix missing logos under `/app/`**: Servidor's canned boot/stop log lines dropped internal names (`"Starting EmberApplication using Java 17..."` → `"Iniciando el servidor…"`; also dropped "Spring" from the stop line) — user flagged exposing the Java class/JVM version as inappropriate. `frontend/InfoSettings.tsx`'s Vanter/Ember logos used absolute `src="/logo.svg"` paths — broken specifically under the Hub build's `--base=/app/`; confirmed root cause via direct HTTP (`/vanter-tech_logo.svg` → **401**, Spring Security's allowlist doesn't cover bare root paths; `/app/vanter-tech_logo.svg` → 200) — fixed with the codebase's existing `import.meta.env.BASE_URL` pattern (already used in `App.tsx`/`isHubBuild.ts`), a no-op for the normal cloud build. `frontend` `pnpm run build`/`build:hub` both clean, `test:run` 124/124; `ember-hub/ui` `npm test` 12/12 unchanged.
- **Prev:** report 458 — **EMBER-HUB-V2 feature: manual "Reiniciar" + full license removal/swap flow**: new `DELETE /api/license` (`HubOrchestrator.removeLicense()` deletes both the license file and the persisted state file — state alone drives `license.status`, so leaving it would keep showing stale "OK"), TDD'd, backend suite still green in full. Rust `kill_process_tree` (`taskkill /F /T`) replaces plain `Child::kill()` — found live that Postgres (started via `pg_ctl start`, which exits itself after launching the real `postgres.exe`) was never actually a killable descendant of the JVM, orphaning it on every restart regardless of the tree-kill (MinIO, a direct child, was fine); real fix is `onRestart` calling the existing graceful `/api/stop` (→ `pg_ctl stop`) first and polling until it clears, before the hard kill. `HubControlServer`'s port is OS-assigned and changes every respawn — `api.ts` cached it forever, so restarting anything used to strand the UI on a dead port; new `resetPortCache()` + a `restartNonce` re-arming `watchAgentShell` fixes that. New header "Reiniciar" button; `LicenseCard`'s new "Eliminar license.key" (shown once a license exists) calls remove-then-full-restart, since a license swap needs a real JVM restart to take effect (validated once, at boot) — deleting the file alone while services are `RUNNING`/stuck `ERROR` does nothing visible. Verified live across repeated full uninstall/install/relaunch cycles: zero orphaned Postgres/MinIO after "Reiniciar", confirmed via `Get-NetTCPConnection`.
- **Prev:** report 457 — **EMBER-HUB-V2 UX: service cards manually collapsible, logs persist, one `#8c1717` box per line**: replaced phase-only `expanded` with real state + an always-visible toggle button, force-opened (never force-closed) on `STARTING`/`STOPPING`/`ERROR` transitions; `useServiceLog` no longer clears revealed lines once the phase moves past starting/stopping — they stay until the *next* start/stop cycle. Log container is now a plain flex list of individually-chipped `bg-[#8c1717]` lines instead of one shared box. Direct user feedback contradicting the original auto-collapse assumption (Servidor stuck expanded was the symptom; the fix generalizes to all three cards). `npm test` 10/10 (+2: persistence across a rerender, manual toggle on ERROR).
- **Prev (compressed, r449–456 — EMBER-HUB-V2's 6 plan tasks + first 2 post-plan fix rounds, full detail in `reports/`):** control server+orchestrator (r449) → headless sidecar, Swing deleted (r450) → `ember-hub/ui/` (r451) → Tauri shell (r452, later corrected by r455) → installer pipeline (r453) → `VERIFY.md`, plan code/doc-complete (r454) → **real-install crash, 3 stacked bugs** (stale jar selection alphabetically not by date, `\\?\` verbatim path unparseable by Java, stale jlink runtime missing `jdk.httpserver` — r455) → Tauri capabilities file was entirely missing (dialog/process silently denied), log color/badge alignment (r456).
- **Prev (compressed, r445–448 — PRINT-AGENT-V2 dashboard polish rounds, full detail in `reports/`):** dashboard redesign, header+2-col cards, `ConnectionCard`/`PrintersSection` (r445) → visual polish, `IconBadge`/`Badge`/`Modal` (r446) → red `Button` variant, pairing accordion, tablet window size (r447) → cursor-pointer, modal spacing, drop backend-URL override, narrower window (r448). Each round rebuilt+published `EmberAgentSetup-0.1.1.exe`.
- **Prev:** report 444 — **PRINT-AGENT-V2 bug fix: `LocalControlServer` missing CORS headers**: user manually installed `EmberAgentSetup-0.1.1.exe` and hit a real bug — window stuck on "Cargando…", pairing (API key/code) button never appeared, window looked frozen on resize. Root cause (found via `systematic-debugging`, reproduced live in a Chromium tab against the actual installed agent's port before fixing): `LocalControlServer` sent no `Access-Control-Allow-Origin`, so the Tauri webview's `fetch()` (always cross-origin — its origin is never `http://127.0.0.1:<port>`) was silently blocked by the browser's CORS policy; `Dashboard.tsx` swallows that error every poll, so `status` stays `null` forever → stuck "Cargando…" + pairing button never renders (it's gated on a non-null status). Fixed with a `CORS_FILTER` (`com.sun.net.httpserver.Filter`) on every `/api/*` context: `Access-Control-Allow-Origin: *` on every response + `OPTIONS` preflight short-circuit (204 + Allow-Methods/Allow-Headers) for the JSON `POST` endpoints. TDD, 2 new tests (both confirmed red before the fix). `mvn -f printing-agent/pom.xml test` **52/52** (+2). Fix re-verified live by rebuilding the app-image (Java) sidecar and hitting it with a real cross-origin fetch from Chromium (now `200 OK`, was `Failed to fetch`). **Also found + fixed a second, pre-existing bug in `build-installer.ps1`** (latent since Task 5/report 442, surfaced only on a *second* rebuild in the same `target/`): `cargo tauri build`'s resource-copy step (`tauri-build`'s `copy_resources`) can't overwrite a read-only destination on Windows, and jpackage's launcher exe is read-only — so every build after the first left a stale read-only file that broke the next one (`Acceso denegado`, os error 5). Fixed by clearing read-only recursively on the previous `target/{release,debug}/app-image` copy before each `cargo tauri build`; verified idempotent (2 consecutive unattended runs both succeeded). Fresh `printing-agent/dist/EmberAgentSetup-0.1.1.exe` (48.17 MB) now contains the CORS fix — **user still needs to reinstall it** for the fix to take effect. **Also found (not fixed, flagged to user):** a leftover Inno-Setup-era install (`Ember Agent.exe --tray`, `app\`, `runtime\` at the top level of `C:\Program Files\Ember Agent\`, dated 2026-09-09) was running concurrently with the new NSIS-installed Tauri shell — installing the new package over the old one doesn't remove the old product's files since they're different installer technologies. User should uninstall the old version before reinstalling the fixed build.
- **Prev (compressed, r438–443 — PRINT-AGENT-V2's original 6 tasks, full detail in `reports/`, checklist in Task Queue Status below):** `LocalControlServer` (r438) → collapse `Main` to sidecar mode (r439) → `printing-agent/ui/` Astro+React+Tailwind (r440) → Tauri shell incl. 3 real bugs fixed live: `beforeBuildCommand` cwd, `autolaunch` import, `agent-ready` event race (r441) → installer pipeline, Tauri bundler replaces Inno Setup incl. the `$ErrorActionPreference`/stderr gotcha (r442) → `VERIFY.md` update (r443, code-complete, only the clean-machine manual run stayed pending).
- **Prev:** report 437 — **Ember Hub installer rebuild + `build-installer.ps1` fix**: PR #120 (EMB-EXPORT) merged to `main`, tagged/deployed backend `v0.2.4` (SaaS). Rebuilding the Hub installer to carry the `.xlsx` export into on-premise hit a real bug: `Build-AppImage`'s frontend step (`pnpm run build:hub`) writes a non-fatal Vite diagnostic to stderr (about `index.html`'s intentionally-plain `env-config.js` `<script>` tag); under the script's `$ErrorActionPreference="Stop"`, Windows PowerShell 5.1 treats ANY native stderr write as a terminating `NativeCommandError` regardless of real exit code, aborting the build before its own (already-correct) `$LASTEXITCODE` check ran. Fixed by scoping that one call to `Continue` locally. Full build now succeeds end-to-end → `ember-hub\dist\EmberHubSetup-0.2.4.exe` (~181 MB; version from `backend/pom.xml`, already `0.2.4-SNAPSHOT` since commit `d81db55a`/#116 — distinct from the stale already-published `EmberHubSetup-0.0.1.exe`, no bucket collision). Not yet published — user runs `deploy/publish-installer.sh hub 0.2.4 EmberHubSetup-0.2.4.exe` from Cloud Shell.
- **Prev:** report 436 — **EMB-EXPORT: .xlsx structure — borders, title bar, row banding**: user re-tested r435's `.xlsx` and reported it still "se ve todo pegado sin espacio entre celdas" (referencing `excel_template.png` in repo root). Added thin borders to every written cell, a merged bold title row per sheet ("Reporte de Ventas"/"Reporte de Productos", white-on-brand-red) pushing the business header block down to rows 1-6 (blank row 7, column headers row 8, data from row 9), and alternating row-fill banding (`RowStyles` record, no-fill/light-gray `#F2F2F2`) on data rows. Column widths padded +640 units past `autoSizeColumn`. TDD'd (+3 tests: border assertion, title/merge assertion, banding assertion). Backend **1233/1233**. Not yet re-verified live in Excel.
- **Prev:** report 435 — **EMB-EXPORT: switch to real .xlsx (Apache POI)**: user hit UTF-8 mojibake in the CSV ("TelÃ©fono") and confirmed CSV can never have real color/bold — chose Apache POI over staying with CSV. `ExportService.buildTenantExportZip` → `buildTenantExportWorkbook`: one `.xlsx` with 2 sheets (Ventas/Productos), bold white-on-brand-red column headers, real numeric/date cells (not text), auto-sized columns. `pom.xml` += `poi`/`poi-ooxml` 5.3.0 (first exception to the spec's "no new dependency" decision, by explicit user request). `CsvWriter` deleted (dead code). `ExportController` → xlsx content-type/filename. Frontend filename `.zip`→`.xlsx` + copy update.
- **Prev:** report 434 — **EMB-EXPORT polish (post-manual-test feedback)**: business header block (Negocio/Nombre legal/RUC/Teléfono/Dirección/Rango exportado) + friendlier column titles; `SettingsBar.tsx` reorders `EXPORT` before `INFO`; `ExportSettings.tsx` drops its inner `Card` (double-frame fix, same as r413's `InfoSettings.tsx`). Superseded by r435's format switch for the CSV-specific parts.
- **Prev:** report 433 — **EMB-EXPORT Task 6 (final): wire the "Exportar datos" tab**: `SettingsType += 'EXPORT'`, `SettingsBar.tsx` (`LEAF` + `buildSettingsNav`, `Download` icon), `GlobalSearchResults.tsx`, `Settings.tsx` case → `<ExportSettings/>`. **EMB-EXPORT was code-complete — all 6 tasks done (r428–r433)** before this polish pass.
- **Prev:** report 432 — **EMB-EXPORT Task 5: frontend `exportService` + `ExportSettings.tsx`**: `exportService.downloadTenantData` (blob GET) + new Card component (this app's first date-range picker — plain `<input type="date">`, no existing calendar component) + temp-anchor blob download + toasts. +10 i18n keys/locale.
- **Prev:** report 431 — **EMB-EXPORT Task 4: `ExportController`**: `GET /admin/export?from=&to=` (ADMIN-only, class-level `@PreAuthorize` like `AnalyticsController`) → zip bytes with `Content-Disposition: attachment`. Backend suite **1235/1235**. All 4 backend tasks of EMB-EXPORT done.
- **Prev:** report 430 — **EMB-EXPORT Task 3: `ExportService`**: `buildTenantExportZip(tenantId, from, to)` — `ventas.csv` (one row per PAID/VOIDED bill: table, date, total, status, confirmed payment methods, participant count) + `productos.csv` (delegates to `AnalyticsService.getProducts`, no limit), zipped via JDK `java.util.zip`.
- **Prev:** report 429 — **EMB-EXPORT Task 2: repository finders**: `BillRepository.findByTenantIdAndCreatedAtBetweenAndStatusIn` (bills in range by status, oldest-first) + `PaymentRepository.findByBillIdIn` (batch form of `findByBillId`).
- **Prev:** report 428 — **EMB-EXPORT Task 1: `CsvWriter`**: dependency-free RFC 4180 CSV row writer (`export/util/CsvWriter.java`), TDD'd — 6/6 tests (plain/comma/quote/newline/null/empty). *(EMB-EXPORT's own report numbering restarted at 428 on this branch, independent from `main`'s — the block below with the same numbers is a different, already-merged PR #119.)*
- **Prev (PR #119, merged to `main` before this branch's numbering restarted):** report 432 — **Waiter table view: table code + QR info button** (ad-hoc): `GET /sessions/{id}/qr` (WAITER-only) now also returns `joinCode` (was qrToken-only); `TableInformation.tsx` += `CircleAlert`-icon `Popover` button next to "Imprimir cuenta" showing the QR + code, lazily fetched, disabled when the table isn't OPEN. `SessionControllerTest.sampleSession()` fixture needed a `joinCode` (was null, `Map.of` would NPE). Backend `./mvnw test` **1216/1216**, frontend `pnpm run build` clean, `test:run` **121/121**. Verified live in Chrome against the user's own (restarted) backend.
- **Prev:** report 431 — **`/waiter/cash-register`: hide write actions from ADMIN** (ad-hoc, found while manually testing r430): not a security bug — backend already `@PreAuthorize("hasRole('WAITER')")`-guards open/close/movements/refund; the route is intentionally `WAITER,ADMIN` like `/waiter/tables`. `CashRegister.tsx` now hides "Open register" and disables Record movement/Close/Refund for a non-waiter (`isWaiter = useAuthStore(s=>s.role)==='WAITER'`), showing `adminCashRegisterReadOnly` instead of letting admin hit a 403. `frontend` `pnpm run build` clean, `test:run` **121/121**. Verified live in Chrome.
- **Prev:** report 430 — **Customer Menu: loyalty points + rewards visibility** (ad-hoc): new `LoyaltySection.tsx` mounted in `Menu.tsx` — points banner (only if `totalPoints > 0`) + reward-catalog strip (only if the account's `rewards[]` is non-empty), whole section gated on `settings.loyalty.enabled`. No backend changes — `/loyalty/accounts/me` already returned everything needed (points/tier/tier-gated rewards), it was just never called from the customer Menu. Rewards stay catalog-display-only (no redeem action) per the existing v1 design (`LoyaltyReward.java`). `frontend` `pnpm run build` clean, `test:run` **121/121**. Not verified in a live browser (needs a seeded loyalty tenant) — relies on the same `/loyalty/accounts/me` contract `Bill.tsx` already uses in prod.
- **Prev:** report 429 — **Landing footer: Facebook link** (ad-hoc): `Footer.astro` first column += circular inline-SVG Facebook icon link (`href="https://www.facebook.com/profile.php?id=61594385545934"`, new `target=_blank`) below the tagline; +1 i18n key/locale (`footer.facebook`). `landing` `pnpm run build` clean (28 pages, unchanged count).
- **Prev:** report 428 — **Settings: "Reporta un problema" floating action** (ad-hoc): fixed circular `destructive`-variant `Flag` button (`h-16 w-16`), bottom-right of `/admin/settings`, at `FloatingNav.tsx`'s own bottom offset (`bottom-[calc(1rem+safe-area)] sm:bottom-8`) — opens a `Popover` (no overlay/blur, page stays interactive) with `ReportIssueForm.tsx` (asunto / quién lo envía pre-filled from `useAuthStore().name` / descripción → `mailto:tofernandoband01@outlook.com` on submit; no backend email service exists in this app). Went through 4 rejected shapes first (in-card `Tabs`; `SettingsBar` expandable group; bottom-left ghost button; blurring `Dialog`) before this — `SettingsType`/`SettingsBar.tsx`/`GlobalSearchResults.tsx` are back to their pre-task state. `frontend` `pnpm run build` clean, `test:run` **121/121**.
- **Prev:** report 427 — **Downloads page: print-agent "cloud version" badge** (ad-hoc): `/info/descarga` — the print-agent card now shows a pill badge "Necesario para la versión cloud" / "Required for the cloud version" (new i18n key `dl.agent.badge`, +1/+1). `descarga.astro` wraps the card `<h2>` in a flex row + conditional `<span>`; Hub entry gets `badge: null` for a uniform shape. `landing` `pnpm run build` clean (**28 pages**).
- **Prev:** report 426 — **Downloads hosting live + `v0.2.3` deploy** (ad-hoc): `gs://ember-downloads-prod` (public) + `downloads.ember.vanter.net` (proxied A → VM, Caddy block PR #111, Origin cert re-scoped to `api.` + `downloads.` no wildcard); `EmberAgentSetup-latest.exe` published; landing copy scrubbed of "Java"/`.exe` (PR #112, parity 467/467); tagged `v0.2.3` → deployed, V10 applied clean 9→10, "Generar código" + "Limpiar pendientes" verified in prod UI. Follow-ups: build/publish `EmberHubSetup`, purge CF cache per release, optional FE PR to hide the raw API key.
- **Prev:** report 425 — **Landing `/info/descarga`** (ad-hoc): downloads page in the `/info` section, below Seguridad. New `info/descarga.astro` (+ `en/` re-export) on `InfoLayout` — `sm:grid-cols-2` card grid for the two Windows installers (print agent, Ember Hub), each with a "Descargar .exe" button. `constants.ts` += `AGENT_DOWNLOAD_URL` / `HUB_DOWNLOAD_URL` (`import.meta.env.PUBLIC_{AGENT,HUB}_DOWNLOAD_URL` ?? `https://downloads.ember.vanter.net/EmberAgentSetup-latest.exe` / `EmberHubSetup-latest.exe`). `InfoLayout` sidebar + `info/index` overview card += downloads entry. `ui.ts` +22 keys/locale (`dl.*`, `info.nav.downloads`, `info.card.downloads.*`). `landing` `pnpm run build` clean (**28 pages**, +2), ES/EN parity **467/467**. **Links 404 until the `.exe`s are hosted at `downloads.ember.vanter.net` — hosting + `PUBLIC_*_DOWNLOAD_URL` config is the pending next step. `EmberHubSetup` not built/published yet.**
- **Prev:** report 424 — **PRINT-JOB-CANCEL** (ad-hoc, not an EMB-PRINT-AGENT milestone): stop stuck `PENDING` print jobs. `PrintJobStatus` += `CANCELED`; `PrintDispatchService.cancel(tenantId, jobId)` (only `PENDING`/`ERROR`, else `IllegalStateException`→409; tenant mismatch→404) + `cancelAllPending(tenantId)` (bulk, returns count); `PrintJobRepository.findByTenantIdAndStatus`; `POST /printing/jobs/{id}/cancel` + `/printing/jobs/cancel-pending` (ADMIN+WAITER, mirrors `retry`). `flushPendingFor` untouched — it scans `findByStatus(PENDING)` so `CANCELED` is auto-excluded from the reconnect replay. Frontend: `PrintingSettings` "Trabajos recientes" — per-row **Cancelar** for `PENDING`/`ERROR`, header **Limpiar pendientes** when any `PENDING`, `CANCELED` rows greyed; `api.ts` `cancelJob`/`cancelPendingJobs`; `backend-types.ts` enum hand-patched; i18n ES/EN +3. No migration (string enum). Backend **1216/1216** (+8), frontend `build` clean, `test:run` **121/121** (+1), `lint` 0 err.
- **Prev:** report 423 — **EMB-PRINT-AGENT T7 (verification checklist)**: `printing-agent/VERIFY.md` — 10-point pass/fail checklist (clean install/no-Java + embedded runtime, pair-by-code → `credential.bin` binary, printer `<select>` + inkjet→DRIVER, test page, real job, auto-start, update keeps `credential.bin`, uninstall keep/wipe, no inbound firewall) + header table + Go/No-Go. **UPDATE (report 424 session):** `EmberAgentSetup-0.1.0.exe` **now built** — Inno Setup 6.7.3 installed per-user (`%LOCALAPPDATA%\Programs\Inno Setup 6\`), `build-installer.ps1 -Stage installer` OK → `printing-agent/dist/EmberAgentSetup-0.1.0.exe` (48.5 MB, embedded JRE). Installed + launched on THIS PC; connected to `app.ember.vanter.net` via the **"Tengo una API key"** manual path (not the code-pairing flow — prod is `v0.2.2`, no T1/V10, so `/pair` 404s). VERIFY.md lines 1 (no-Java) / 2 (code pairing) / 6–10 still un-run; full checklist still needs T1 in prod + a Java-less VM. `printing-agent/build-installer.ps1` (cloned from `ember-hub/`, stripped of Postgres/MinIO/frontend/`build.env`-read/firewall) — `-Stage runtime` (jlink from `jlink-modules.txt`) / `appimage` (`mvn -f printing-agent/pom.xml package` → shaded jar → `jpackage --type app-image --name "Ember Agent" --main-class com.vanter.emberagent.Main`) / `installer` (`iscc /DAppVersion=<v> EmberAgent.iss`). `Get-AgentVersion` reads `printing-agent/pom.xml` (agent self-versions). New `installer/{EmberAgent.iss (`{commonstartup}` shortcut → `--tray`, `%ProgramData%\EmberAgent` dirs, uninstall prompts before wiping data, NO `[Run]` firewall — agent is outbound-only), Iniciar Ember Agent.cmd, make-icon.ps1, ember-agent.ico (committed 9.7 KB)}` + `build.env.example` (no vars). `.gitignore` += `dist/`/`build.env`. `lint.yml` += `build-print-agent` job (`windows-latest`, package + `-Stage appimage` + assert launcher; Inno `.exe` stays manual). `README.md` rewritten to the installer flow (Task Scheduler + `agent.properties` sections dropped/demoted). **Verified:** `-Stage appimage` on Windows/JDK17 → `dist/app-image/Ember Agent/{Ember Agent.exe, runtime\bin\java.exe, app\printing-agent.jar}` all present; `-Stage installer` not run here (no `iscc` — that's T7). `mvn -f printing-agent/pom.xml test` unchanged **41/41**.
- **Prev:** report 421 — **T5 (frontend admin)**: `CreateAgentModal` leads with the pairing code (API key → `<details>`); `PrintingSettings` "Nuevo código" Dialog + Emparejado/Sin emparejar badge (`agent.paired`) + `VITE_AGENT_DOWNLOAD_URL` `.exe` anchor; `AddPrinterModal` `windowsQueueName` → shadcn `<Select>` of `discoveredPrinters` + "Otra…" free-text, inkjet auto-`DRIVER`. `backend-types.ts` hand-patched (no full `pnpm run openapi` — ~1230 lines of unrelated drift). +10 i18n ES/EN, +2 tests. `build` clean, `lint` 0/16, `test:run` **120/120**.
- **Prev:** report 420 — **T4 (agent)**: `AgentRunner` extracted from `Main` (loop behaviour-identical + `AgentConfig.resolve`, `UNPAIRED` 3s re-poll, discovered-printers report on connect, `StatusHub` feed), `PrintJobHandler` nullable `StatusHub` ack-mirror, `Main` `--headless`/`--tray`, `AgentConfig.load` shim dropped. Swing `ui/{AgentDashboard,AgentTrayIcon,PairDialog}` + `DiagnosticsReport`. +4 tests (Swing = T7). Agent `mvn -f printing-agent/pom.xml test` **41/41**.
- **Prev (compressed, r401–419 — full detail in `reports/`, checklists in Task Queue Status below):** EMB-PRINT-AGENT T1–T3 (backend `V10` pairing/discovered-printers schema+endpoints r417; agent `CredentialStore`/DPAPI/`StatusHub` r418; agent `PairingClient`/`WindowsPrinterEnumerator` r419); SaaS favicon distinct from landing r415; Settings "Información" tab + logo polish r412–414; waiter cash-register/TopNav hotfixes r409–411; landing contact form (`ContactForm.tsx`+Turnstile+Resend — needs Cloudflare `TURNSTILE_SECRET_KEY`/`RESEND_API_KEY`/`PUBLIC_TURNSTILE_SITE_KEY`/`CONTACT_TO`) r404–408; Ember Local on-premise page r401–402.
- **Current Active Task:** none — **KDS-BULK-STATUS-UPDATE done** (all 4 tasks, reports 464–467: bulk service method, bulk endpoint, frontend API+i18n, `FocusedCard.tsx` UI). Plan `docs/superpowers/plans/2026-09-13-kds-bulk-status-update.md`. No live UI verification in a real browser yet (unit/component tests only) — worth a manual check next time the Hub/KDS is exercised live.
- **Prev active:** **EMBER-HUB-V2** — Tauri native shell for the Hub, same pattern as PRINT-AGENT-V2. **Code/doc-complete (reports 449–454, +post-completion fix rounds 455–463)**; only the clean-Windows-machine `VERIFY.md` ops run remains open (see Open/deferred).
- **Prev active:** **PRINT-AGENT-V2** — Tauri native shell for printer-agent. **Code-complete (reports 438–443)**; reports 444–448 are post-completion polish. Only the clean-Windows-machine manual `VERIFY.md` run remains open.
- **Prev active:** **EMB-EXPORT** — tenant data Excel export, **DONE** — merged PR #120, tagged + deployed backend `v0.2.4`. Plan `docs/superpowers/plans/2026-09-11-tenant-data-csv-export.md`.
- **Prev active:** **EMB-PRINT-AGENT** — plan `docs/superpowers/plans/2026-09-08-print-agent-installer.md`. All code T1–T7 complete (reports 417–423); only the clean-Windows-PC `VERIFY.md` manual run is pending (ops action — needs a Java-less Windows VM). `.exe` built + published at `https://downloads.ember.vanter.net/EmberAgentSetup-latest.exe`. Prod `v0.2.3` has T1/V10 live (code pairing + cancel-pending verified). T5 hand-patched `backend-types.ts`; a full `pnpm run openapi` regen is still owed. Report 415 (SaaS favicon distinct) on branch `fix/saas-favicon-distinct` — PR pendiente. Landing contact form LIVE and verified (2026-09-08).
- **Predecessor context:** guest-join PR #97 merged (`7dba69fe`, report 397); Live QA findings **Q1–Q6** merged (PRs #88/#89/#91–#95, reports 387–395); menu-responsive fix on `main` (report 396). Bug X + Bug Y confirmed fixed; Hub confirmed working on a real PC.
- **System Health:** backend `./mvnw test` **1244/1244** (r449, new: +11 — Hub `control` package: `DefaultHubOrchestratorTest`/`HubControlServerTest`; this line was stale at r424's 1216 in between — EMB-EXPORT r428-436 already brought it to 1233 unrecorded here); `ember-hub/ui` (r452) `npm test` **8/8**, `npm run build` clean; `ember-hub/src-tauri` `cargo tauri build`/`-Stage all` compiles + bundles; **r452's "blocked by Docker on 5432/9000" theory was wrong — corrected by r455**, which found and fixed the real causes (stale jar selection, `\\?\` path, stale jlink runtime missing `jdk.httpserver`) and verified a full real-install boot-to-dashboard end-to-end (screenshotted); print-agent `mvn -f printing-agent/pom.xml test` **50/50** (r438–439: `LocalControlServer` +9, Swing removal net-zero); `printing-agent/ui` (r448) `npm run test` **5/5**, `npm run build` clean; window `720×768` (r448, was `1024×768`); `printing-agent/src-tauri` (r441) `cargo tauri build --debug` compiles + bundles, manual smoke test verified (sidecar spawn, port discovery, dashboard render, close-to-tray); (r442, new) full pipeline `.\printing-agent\build-installer.ps1 -Stage all` verified live end-to-end → `printing-agent/dist/EmberAgentSetup-0.1.1.exe` (48.2 MB, NSIS via `cargo tauri build` release) produced on disk. **Rust toolchain (rustc/cargo/tauri-cli) + MSVC Build Tools installed on the dev machine** (r441) — required for any `src-tauri` build; PATH note: `cargo`/`rustc` live at `%USERPROFILE%\.cargo\bin`, not always on a fresh shell's PATH — prepend it if `cargo`/`Build-Installer` report "not found". `printing-agent/dist/app-image` + jlink runtime rebuilt against current source (were stale, pre-Task-2) purely to unblock r441's local verification — not part of a task deliverable. frontend `build` clean, `lint` 0 errors / 16 pre-existing warnings, `test:run` **121/121** (r424 +1; `backend-types.ts` still hand-patched, full `openapi` regen still owed); `landing` `pnpm run build` clean (**28 pages**, r425), ES/EN i18n parity **467/467**.
- **⚠ Prod Flyway is NOT baselined** — migrations run automatically on the next tagged backend release. Prod `flyway_schema_history` is at **v10** (V1–V10 applied; `v0.2.3` ran V10 clean 9→10). `V9`/`V10` are idempotent (`IF NOT EXISTS`); `V7`/`V8` are NOT (editing an applied migration breaks `validate-on-migrate`). Only the *local dev* DB is baselined at v15, so migrations ≤ 15 are skipped there → add new columns by hand for local dev. Never pre-run migration DDL on prod (a ~15 min outage on 2026-09-06 came from doing exactly that on `V7`). **TODO (ops, not code):** inspect prod `flyway_schema_history` from Cloud Shell and baseline properly.
- **Prod deploy:** tag `v*` on `main` → `backend-image.yml` builds `ember-backend:<tag-without-v>` → `./deploy/deploy.sh <tag-without-v>` from Cloud Shell (pure gcloud/IAP; the `v` is stripped — `deploy.sh 0.2.4`, not `v0.2.4`). Frontend + landing auto-deploy from `main` via Cloudflare. Backend on **`v0.2.4`** (tenant `.xlsx` export; PR #120, 2026-09-11). V1–V10 applied, no new migration this release.
- **Hub/Agent installers:** manual, no CI (`ember-hub/build-installer.ps1`, `printing-agent/build-installer.ps1`) → `deploy/publish-installer.sh <agent|hub> <version> <path>` from Cloud Shell (upload + `-latest.exe` + Cloudflare purge). Bucket `gs://ember-downloads-prod`: `EmberAgentSetup-latest.exe` live; Hub has only the stale `EmberHubSetup-0.0.1.exe` — `EmberHubSetup-0.2.4.exe` built (r437), **publish still pending**.

## Active Context & Recent Decisions
- Monolith at `ember/`: Java 17 + Spring Boot 3.5.14 / React 19 + TS + pnpm. Event bus is Spring `ApplicationEventPublisher`/`@EventListener` only — do **not** reintroduce Kafka (dependency vestigial).
- **Tenant isolation:** `@TenantId` discriminator on catalog/billing/settings/restaurant/kitchen entities. `User` and `Session` are deliberately excluded (untenanted lookups before a customer JWT is tenant-bound). Customers become tenant-bound only on table-join (`SessionService.bindResolvedTenant`). Every `@DataJpaTest` must `@Import(com.vanter.ember.config.TenantIdentifierResolver.class)`.
- **WebSocket multi-endpoint gotcha (reports 193-194):** all `WebSocketMessageBrokerConfigurer` beans merge onto one broker/channel; a 2nd `enableSimpleBroker(...)` silently replaces the 1st and its interceptor runs on every endpoint unless handshake-tagged. Only `WebSocketEndpointIsolationTest` catches it.
- **Digital payments are a STUB** (`gatewayRef = "STUB-..."`) — no real gateway. Biggest revenue gap; blocked on GATEWAY-01 (provider decision).
- **Commit/PR attribution:** zero Claude attribution in commit messages **or** PR descriptions (no `Co-Authored-By`, no `Claude-Session`, no "Generated with Claude Code" badge/URL). User is sole author — reaffirmed 2026-09-06 after stripping it from 16 PRs.
- Analytics money semantics: revenue = CONFIRMED `Payment` sums (never bill totals/PENDING); AOV = PAID-`Bill` avg; live counts ignore the query window.
- Missing shadcn primitives are built from the installed unified `radix-ui` export (`data-slot` convention), not a new dep.

## Task Queue Status

### KDS-BULK-STATUS-UPDATE — kitchen bulk item status — plan `docs/superpowers/plans/2026-09-13-kds-bulk-status-update.md`
Focused-ticket-only bulk select (checkbox per dish + select-all) + status dropdown; each dish walks every intermediate status one at a time (forward/backward) to the chosen target. New, separate backend method/endpoint — existing `updateItemStatus`/`isValidTransition` and `QueueCard.tsx` untouched. One task per context, `/clear` between.
- [x] Task 1 — `KitchenService.updateItemsStatus(orderId, itemIds, targetStatus)`: per-item ordinal walk (`Integer.signum` direction), one `save()`, `KitchenItemUpdated` per step, symmetric `active` recompute (handles reactivation on backward move off `DELIVERED`) + 8 unit tests — report 464
- [x] Task 2 — `UpdateItemsStatusRequest` DTO + `PATCH /kitchen/orders/{orderId}/items/status` (`hasRole('KITCHEN')`) + 5 controller tests (role coverage matching the existing endpoint) — report 465
- [x] Task 3 — `kitchenServices.updateItemsStatus` in `frontend/src/lib/api.ts` + 4 new i18n keys (`kdsSelectAll`/`kdsDeselectAll`/`kdsBulkStatusPlaceholder`/`kdsSelectItemAriaLabel`) in both `locales/{es,en}/kitchen.ts` — report 466
- [x] Task 4 — `FocusedCard.tsx`: per-item circular `Checkbox`, "select all" toggle, status `Select` dropdown (fires on change, no separate apply button), clears selection on success + new `FocusedCard.test.tsx` (4 tests) — report 467, **KDS-BULK-STATUS-UPDATE complete**

### PRINT-AGENT-V2 — Tauri native shell — spec `docs/superpowers/specs/2026-09-12-tauri-native-shells-design.md`, plan `docs/superpowers/plans/2026-09-12-printer-agent-v2-tauri-shell.md`
Replaces printer-agent's Swing UI (`AgentDashboard`/`AgentTrayIcon`/`PairDialog`) with a Tauri+WebView2 window (Astro+React+Tailwind); Java becomes a headless sidecar exposing a new loopback-only `LocalControlServer`. Fase 1 = parity with today's Swing (incl. test-print/logs/diagnostics, already existing — corrected in spec after initial miscategorization). No `AgentRunner`/`StatusHub`/pairing/printing logic changes. Branch `spec/tauri-native-shells` off `main`. Ember Hub v2 uses the same pattern but is a **separate plan**, done after this one ships. One task per context, `/clear` between.
- [x] Task 1 — `LocalControlServer` (JDK `HttpServer`, loopback-only: `/api/status`, `/api/pair`, `/api/printers`, `/api/test-print`, `/api/diagnostics`, `/api/paths`) — report 438
- [x] Task 2 — collapse `Main` to a single headless sidecar mode (prints `PORT=<n>`), delete Swing UI classes, `jlink-modules.txt` += `jdk.httpserver` — report 439
- [x] Task 3 — `printing-agent/ui/` (Astro+React+Tailwind window content, 4 sections + component tests) — report 440
- [x] Task 4 — Tauri shell (`src-tauri/`: sidecar spawn, port discovery, crash watcher, tray, close-to-tray, autostart) — report 441
- [x] Task 5 — installer pipeline: Tauri bundler replaces Inno Setup, `build-installer.ps1` + CI `build-print-agent` updated — report 442
- [x] Task 6a — `VERIFY.md` updated for the Tauri shell (+ sidecar crash/orphan checks 11–13) — report 443
- [ ] Task 6b — run the 13-item checklist on a clean Windows machine/VM (ops action, pending)

### EMB-EXPORT — Tenant data CSV export — spec `docs/superpowers/specs/2026-09-11-tenant-data-csv-export-design.md`, plan `docs/superpowers/plans/2026-09-11-tenant-data-csv-export.md`
Admin exports sales/billing + product-performance history as a `.zip` of CSVs from a new Settings tab. ADMIN-only, no new dependency, synchronous (data volume is small). Branch `feat/tenant-data-csv-export` off `main`. One task per context, `/clear` between.
- [x] Task 1 — `CsvWriter` (RFC 4180 CSV row writer, no dependency) — report 428
- [x] Task 2 — repo finders: `BillRepository.findByTenantIdAndCreatedAtBetweenAndStatusIn`, `PaymentRepository.findByBillIdIn` — report 429
- [x] Task 3 — `ExportService.buildTenantExportZip` (`ventas.csv` + `productos.csv`, reuses `AnalyticsService.getProducts`) — report 430
- [x] Task 4 — `ExportController` (`GET /admin/export`, ADMIN-only) — report 431
- [x] Task 5 — frontend `exportService` + `ExportSettings.tsx` (date range + download button) — report 432
- [x] Task 6 — wire "Exportar datos" tab into Settings (`uiStore`, `SettingsBar`, `GlobalSearchResults`, `Settings.tsx`) — report 433, **EMB-EXPORT complete**

### EMB-FEAT-HUB — Ember Hub: waiter-managed seats — DONE, report 398, merged in #98
- [x] Strip the customer flow from the Hub build (`isHubBuild` = `import.meta.env.BASE_URL !== '/'`); waiter opens a table with N name-only seats (`Participant.userId == null`, auto-named "Asiento N"), renames/adds/removes them, adds items per seat via the existing `waiter-items` path, split billing works unchanged. No DB migration. Branch `spec/hub-waiter-seats` (off `main` post-#97).
  - [x] T1 seat seeding (`CreateSessionRequest.seatNames` + `createSession` 4-arg; position-based auto-name w/ collision bump)
  - [x] T2 null-safety sweep (8 `Objects.equals` sites + controller) + `LoyaltyAccountJoinListener` null-`userId` guard
  - [x] T3 `ParticipantRenamed` event → session + waiter WS topics
  - [x] T4 `POST /sessions/{id}/participants` (add seat, lowest-free "Asiento N")
  - [x] T5 `PATCH` rename (bill-blocked 409, cascades item/activity names) + `DELETE …/{name}` remove (leave semantics) + `HubSeatFlowIntegrationTest`
  - [x] T6 `lib/isHubBuild.ts`, `App.tsx` lazy customer pages + `!isHubBuild` route gate, `Login.tsx` → shared module
  - [x] T7 `ParticipantsQrModal` Hub branch (seat-name inputs, no QR) + `createSession` 3rd arg + navigate to `/waiter/tables/{id}`
  - [x] T8 `SeatFormModal` (add/rename/remove-confirm) + `TableInformation` seat controls + API + `participant.name` key + WS invalidation
  - [x] T9 hide loyalty admin UI in the Hub build (`SettingsBar`, `Settings.renderContent`, `GlobalSearchResults`)
  - [x] T10 i18n ES/EN, report 398, PROGRESS, squash, PR
- Known: `HubSeatFlowIntegrationTest` skips the bill-blocks-rename 409 (billing precondition not worth satisfying in that test); covered by `renameSeat_billExists_*` unit/slice tests.

### Post-audit hardening — DONE, report 399, merged in #99
- [x] 1 `@Transactional` on `addSeat`/`renameSeat`/`removeSeat` endpoints
- [x] 2 `App.hubBuild.test.tsx` de-flaked (mock `isHubBuild`, no `resetModules`/dynamic `import('./App')`); frontend suite 79s→43s
- [x] 3 `addSeat` 409s at capacity, no silent `maxParticipants` bump; unit + `HubSeatFlowIntegrationTest` updated (calls `PATCH /capacity` first)
- [x] 4 cruft: `to_delete/` (license blob) + `docs/~$CHITECTURE.docx` removed; `.gitignore` += `*.key` / `.claude/skills/` / `~$*`
- [x] 5 public guest code-join — new `/join` page `JoinByCode` (Hub-build-gated) → existing `/sessions/join-as-guest` `{joinCode,name}`; i18n ES/EN

### Landing — Ember Local (on-premise) — DONE, r401 (merged #100) + r402 (PR #101 open)
- [x] `EmberLocal.astro` home band (after `<Compare/>`) + `LocalPlan.astro` on `/planes` (`#local`) + `/funcionalidades` callout (report 401)
- [x] `local.*` i18n in `es`/`en`; billing = anual **o** semestral + install cotizado; CTA → `/contacto`
- [x] Dedicated `/info/local` (+ `/en/…`) page: cómo funciona / offline / datos / soporte / **Requisitos** table; added to `/info` sidebar + card (report 402)
- [x] Home band + funcionalidades callout CTAs → `/info/local`; callout lists 2 cloud-vs-Local diffs; `LocalPlan` secondary link → `/info/local`
- [x] r402 shipped as its own PR #101 (PR #100 with r401 was merged mid-work)

### Landing nice-to-have batch — DONE r403–r407, branch `feat/landing-ember-local` → PR #101
- [x] r403 remove non-functional cookie consent banner (`CookieBanner.tsx` + `cookie.*` keys); Plausible is cookieless/PROD-only
- [x] r404 `/info/seguridad` (+ `/en/…`): multi-tenant / JWT+BCrypt / TLS / backups; claims verified vs code + `RUNBOOK.md`; `/info` sidebar + card
- [x] r405 `/contacto` form + `ContactForm.tsx` island + `/gracias` (+ `/en/…`); r406 full-width layout + drop Turnstile test-key
- [x] r407 endpoint moved off Pages Functions → `landing/worker/index.ts` (deploy target is a **Worker + static assets**, not Pages); `wrangler.jsonc` `main` + `assets.binding`; Turnstile site key `0x4AAAAAAEsaAHQ6XDMni_IM` hard-coded (public)
- [ ] **Config owed (owner, on the `ember` Worker → Settings → Variables and Secrets):** `TURNSTILE_SECRET_KEY` + `RESEND_API_KEY` (Secret), `CONTACT_TO` (Text) — `POST /api/contact` returns 500 until set
- [x] `pnpm build` clean (26 pages), ES/EN i18n parity 445/445, `wrangler deploy --dry-run` OK

### Hub v2 prep: residual hardening — DONE, report 400, merged in #99
- [x] 1 `OptimisticLockingFailureException` → 409 (`code:"CONCURRENT_MODIFICATION"`) in `GlobalExceptionHandler` + slice test
- [x] 2 `isHubBuild` → function (`VITE_HUB_BUILD` canonical, `BASE_URL` fallback); ~10 call sites; `SettingsBar` → `buildSettingsNav()`; `.env.hub` + `build:hub --mode hub`; `vite-env.d.ts`; 4 mock tests updated; CI job `build-hub` in `lint.yml`
- [x] 3 `V9` → `ADD COLUMN IF NOT EXISTS` (unreleased; `V7`/`V8` left alone)
- [ ] Ops (not code, not a Hub-v2 blocker): baseline prod Flyway — inspect `flyway_schema_history` from Cloud Shell first

### Guest table-join (Cloud) — DONE, report 397, PR #97 merged
- [x] T1 `GuestNameGenerator` · [x] T2 `V9` + `User.guest` + `GuestUserService` · [x] T3 `ParticipantJoined.guest` + loyalty guards · [x] T4 `POST /sessions/join-as-guest` · [x] T5 frontend "Entrar como invitado" on `/menu/join` · [x] T6 report + squash + PR.
- [x] Follow-up (report 399): public **code-entry-as-guest** page — `/join` (`JoinByCode`), 5-char code + optional name, no account, no QR.

### EMB-PRINT-AGENT — Print agent: desktop app + Windows installer — plan `docs/superpowers/plans/2026-09-08-print-agent-installer.md`
Cloud agent only (spec §4.1 Hub-local detection = separate future plan). One task per context, `/clear` between.
- [x] T1 backend — `V10` (`pairing_codes` + `discovered_printers` jsonb), `createPairingCode`/`redeemPairingCode`/`saveDiscoveredPrinters`, `POST /printing/admin/agents/{id}/pairing-code` + `/printing/agents/pair` (rate-limited) + `/printing/agents/me/discovered-printers`, `PrintAgentResponse` += `paired`/`discoveredPrinters`, `SecurityAuditTest`. Report 417. **Types regen deferred to T5** (openapi diff was ~1230 lines of pre-existing drift — out of scope for a backend commit).
- [x] T2 agent — `StatusHub` observable, `credential/{CredentialStore,DpapiCredentialStore,PlaintextCredentialStore,CredentialStores}` (JNA Crypt32, machine scope) + `jna-platform` dep, `AgentPaths` (`%ProgramData%\EmberAgent`), `AgentConfig.resolve` (credential first, `agent.properties` fallback, empty otherwise; `load(Path)` shim kept for `Main` until T4). **Closes F-24.** Report 418.
- [x] T3 agent — `DiscoveredPrinter`/`PairingException`/`PairingClient.redeem`, `WindowsPrinterEnumerator` (`Get-Printer` + `parse` + `looksLikeInkjet`), `DiscoveredPrintersClient` (best-effort). +9 tests. Report 419. Runtime wiring deferred to T4.
- [x] T4 agent — `AgentRunner` extracted from `Main` (loop unchanged + `resolve`/`UNPAIRED` re-poll + discovered-printers report on connect + `StatusHub` feed), `PrintJobHandler` nullable `StatusHub` ack-mirror, `Main` `--headless`/`--tray`, `AgentConfig.load` shim dropped; Swing `ui/{AgentDashboard,AgentTrayIcon,PairDialog}` + `DiagnosticsReport`. +4 tests (Swing = T7). Report 420.
- [x] T5 frontend — `CreateAgentModal` shows pairing code (key folded), `PrintingSettings` "Nuevo código" + Emparejado/Sin emparejar badge + `.exe` download link, `AddPrinterModal` `windowsQueueName` → `<select>` from `discoveredPrinters` + auto-`DRIVER` for inkjet, i18n ES/EN. Report 421. `backend-types.ts` hand-patched (no full `openapi` regen — ~1230 lines of unrelated drift).
- [x] T6 installer — `printing-agent/build-installer.ps1` + `installer/EmberAgent.iss` + `jlink-modules.txt` + `ember-agent.ico` (clone `ember-hub/`), `{commonstartup}` shortcut → `--tray`, `%ProgramData%\EmberAgent\`, CI `build-print-agent` job (fat jar + `jpackage` app-image; Inno `.exe` manual). Report 422. `-Stage appimage` verified on Windows/JDK17.
- [~] T7 manual verification — `printing-agent/VERIFY.md` 10-point checklist written (report 423); the clean-Windows-PC run + go/no-go is still pending (`iscc`/`.exe` build + a Java-less Windows VM — owner/ops action)

### Next up (spec/plan pending)
- [ ] **F-21 — hardcoded credentials** in `PortableDatabaseBootstrap`/`PortableMinioBootstrap`: generate random creds at first boot, persist in `hub.env`, read from env. Own small task.

### PILOT-READINESS — gate before onboarding the first paying customer (~1 week, not more)
Rationale: architecture is sound but prod infra is early (single VM, no HA, manual deploy). These four close the gap between "demo-able" and "safe to run a real restaurant's money on". Framing to customers stays **early access / founding restaurant**, no uptime SLA, no "diner pays from phone" (gateway is a stub — EMB-GATEWAY).
- [ ] **1 — Lower the RPO.** Nightly `pg_dump` = up to 24 h of lost orders/sales/cash-shift data on a VM failure. Minimum: hourly dumps to `gs://ember-backups-ember-prod-vanter`. Ideal: WAL archiving / PITR. Pairs with **HPD-21** (restore test).
- [ ] **2 — Rehearse rollback once.** `./deploy/deploy.sh <previous-tag>` documented in `deploy/RUNBOOK.md` and actually run once against prod (or a clone), not improvised during an incident.
- [ ] **3 — Bus factor = 1, acknowledged.** Founder reachable + able to fix fast; a support/status channel for pilots and a basic "prod is down" response checklist.
- [ ] **4 — Privacy/Terms lawyer pass.** `landing/src/pages/privacy.astro` is thin; taking payment + handling restaurant (and possibly diner) data needs a real review. Not a pilot blocker, is a scale blocker.

### Open / deferred
- [ ] **EMB-GATEWAY** — real payment gateway. Blocked on GATEWAY-01 (certified provider for Nicaragua). GATEWAY-02..06 open.
- [ ] **HUB-03 T10** — manual Windows verification (clean install, LAN 2nd PC, license picker, upgrade-in-place, uninstall-keep, boot errors) → `reports/382-…`. Spec/plan `…/2026-09-05-hub-installer*`.
- [ ] **EMBER-HUB-V2 / PRINT-AGENT-V2 clean-machine `VERIFY.md` runs** — both Tauri shells are code-complete; only the manual clean-Windows-machine/VM checklist runs (ops action) remain open for each.
- [ ] **HPD-21** restore test (GCS dump → throwaway `pg_restore`, document in `deploy/RUNBOOK.md`); **HPD-22** E2E prod walkthrough (2 devices).
- [ ] **Bug Y** — live retest with two diners now that `v0.2.1` cleared the deploy skew (marked fixed by the user, no formal retest logged).
- [ ] **LSEO** — content (blog / keyword articles / case studies), conversion (contact/demo form, WhatsApp, hero video, retargeting), off-page, minor JSON-LD. LSEO-04 Google Business Profile.
- [ ] **Login: no "recuperar contraseña" (forgot password)** — flagged live by the user (report 461), explicitly deferred by them ("puede ser después"); no plan yet.
- [ ] **Security debt:** F-15 (Hub activation returns `adminPasswordHash` — redesign contract + migration path for installed Hubs); ~~F-24 (print-agent key plaintext on disk)~~ — **FIXED, report 418** (DPAPI machine-scope credential store); F-10/E-23 (PIN-login enumeration oracle — accepted, needs a product decision); F-22 (secret in git history — accepted, rotate if revisited).

### Done (collapsed)
- [x] **KDS focused-ticket cleanup + manual reprint (r468)** — removed dead "Cliente"/"Anular" UI, `KitchenTicketPrintService`/`POST /printing/kitchen-orders/{orderId}/ticket` (KITCHEN) wires "Imprimir" as a reprint independent of auto-print.
- [x] **PRINT-JOB-CANCEL (r424)** — `PrintJobStatus.CANCELED` + `cancel`/`cancelAllPending` in `PrintDispatchService`, `POST /printing/jobs/{id}/cancel` + `/cancel-pending` (ADMIN+WAITER); `PrintingSettings` per-row **Cancelar** + header **Limpiar pendientes**. Stops `flushPendingFor` replaying stuck `PENDING` tickets onto an out-of-paper printer. No migration. Backend-only + frontend, not pushed to prod (awaits a tagged backend release).
- [x] **`/cash-shifts/current` empty 200 (r410)** — no open shift → 200 + empty body instead of 404, so the cash-register poll stops logging a console error. `findCurrentOpenShift(): Optional` + controller `orElse(null)`; `api.ts` `return data || null`. Backend change awaits a tagged release for prod.
- [x] **Frontend UX batch (r409 + r411 hotfix)** — `/admin` lands on analytics not inventory; section tours are request-only (no auto-start); arqueo difference no longer flashes on shift close (`CloseShiftDialog` unmounted with `cashShiftCurrent`). r409 wrongly removed the waiter "Registrar movimiento" button — **r411 restored it** and instead removed the dead TopNav "Nuevo registro" button (rendered with `actionType = null` on `/admin/cash-register`, `openModal(null)` fired nothing).
- [x] Core platform, EMB-PRINT, EMB-MOD, EMB-INV, restaurant onboarding, Ember Hub HUB-01, license heartbeat, EMBER-FIX, HPD-01..20, Landing SEO technical base — reports 1–329.
- [x] **FIX-QA** (22/23 live findings) — reports 361-364 (E-23 deferred).
- [x] **Platform console improvements A–D** — `updateRole` guard, retire/soft-delete tenants, Hub liveness/heartbeats in the console, console redesign + `GET /platform/stats`. Reports 383–385, PRs #80/#81/#82.
- [x] **Real-time bill bug cluster** — Bug X (EQUAL_PARTS divide-by-zero) + `@Transactional` handler. Reports 387-388, PRs #85/#86.
- [x] **Live QA findings Q1–Q6** — Q1a stale digital intent, Q1b loyalty reversal on refund, Q2 customer tax breakdown, Q3 `/menu/join` page, Q4 in-app QR scanner, Q5 print-agent setup docs, Q6 delete buttons. Reports 389–395, PRs #88/#89/#91–#95.
- [x] **Menu responsive** — report 396.
- [x] **Guest table-join (Cloud)** — report 397, PR #97.
- [x] **EMB-FEAT-HUB — Hub waiter-managed seats** — name-only seats + 3 WAITER endpoints + `ParticipantRenamed` + null sweep + loyalty guard; frontend `isHubBuild` fork (customer subsystem stripped, seat-name assign modal, seat controls, loyalty UI hidden). Report 398, merged in #98.
- [x] **Post-audit hardening** — seat-endpoint `@Transactional`, `App.hubBuild` de-flake, `addSeat` capacity 409, cruft cleanup, public `/join` guest code page. Report 399, merged in #99.
- [x] **Hub v2 prep** — optimistic-lock 409, `isHubBuild()` on `VITE_HUB_BUILD` + CI `build-hub` job, `V9` idempotent. Report 400, merged in #99.
- [x] Prod backend deployed to `v0.2.1` (Q1–Q6 backend + platform console). Reports 387/388, deploy incident recovered.
 
