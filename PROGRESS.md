# PROGRESS.md — Active Execution State

## Current Execution State
- **Last Completed Task:** feature-session-activity-log — `Session.activityLog` (new embedded `SessionActivity` list, `ITEM_SENT`/`ITEM_DELETED`) persists waiter-facing history independent of current items; `TableInformation`'s Actividad card reads it instead of deriving from live items. No backfill — history starts fresh at deploy for already-open tables. Report 110.
- **Current Active Task:** none — awaiting next task selection. `backend-types.ts` was hand-patched for `SessionActivityDto` (no live backend to run `pnpm run openapi`) — regenerate for real next time the backend is up. Repo-wide `pnpm run lint` still fails on ~17 PRE-EXISTING errors in files untouched by any recent task (`NewCategoryModal.tsx`/`NewMenuModal.tsx`/`Menu.tsx`/`ParticipantsPopUp.tsx`/`OrdersDisplay.tsx`/`TableInformation.tsx`/`sessionStore.tsx`/`settingStore.ts`/`uiStore.ts`/`websocket.ts`) — not caused by reports 106–110, flagged for future cleanup. Pending follow-ups from user (unrelated, deferred): (1) `/landing` mobile `MobileNavDrawer`/`StickyMobileCTA` z-index overlap on open — root cause identified, fix not applied. (2) Hero.astro mobile overlap report — no defect found, awaiting screenshot. (3) New `/landing` dark-mode toggle using `#8c1717` accent — not started.

## Active Context & Recent Decisions
- Monolith root at `ember/`; Java 17 + Spring Boot 3.5.14 / React 19 + TS + pnpm. Product: multi-tenant restaurant platform (collaborative cart, KDS, floor/waiter management, admin analytics). task-3.2 deleted `spring-kafka` — Spring `ApplicationEventPublisher` is the only event bus, do not reintroduce a broker.
- task-3.3: `GlobalExceptionHandler` extends `ResponseEntityExceptionHandler` (mandatory — a bare `Exception.class` catch-all would outrank `DefaultHandlerExceptionResolver` and degrade 400s to 500); RFC 7807 via `problem(status, detail, path)`, 500s log ERROR with a `traceId`, never echo `ex.getMessage()`. task-2.15's **Flyway** (`baseline-on-migrate`, migrations from V2, disabled in tests/H2 `create-drop`): every schema change goes in `db/migration/` AND the entity mapping; `ddl-auto: update` silently can't add `NOT NULL` columns without a default to a non-empty table — check boot logs for `CommandAcceptanceException`, don't trust a clean boot.
- Mongo has no replica set/`MongoTransactionManager` — fail-fast validation-before-mutation instead of `@Transactional`. `Session`/`KitchenOrder` carry `tenantId` (task-2.17, no Mongo `@TenantId`, every finder is tenant-first; route session reads through `SessionService.findById`). Hibernate JPA side uses `DISCRIMINATOR` multi-tenancy (task-2.14): `@TenantId` on `Category`/`MenuItem`/`Bill`/`BillSplit`/`Payment`/`DiningTables`/`RestaurantSettings` (`User` deliberately excluded, looked up by email pre-tenant-bind); isolation repo tests extend `config/AbstractTenantIsolationTest`. `config/MongoTenantBackfill` (task-2.18) and `config/KitchenOrderActiveBackfill` (bugfix, report 52 — retires `KitchenOrder.active` on `SessionClosed`, backs `GET /kitchen/display`'s live-only filter, `GET /kitchen/orders` stays unfiltered) are both idempotent boot-time `ApplicationRunner`s that resolve missing/legacy Mongo fields; that's the pattern to follow for any future Mongo field backfill.
- task-3.4: `config/CorsProperties` (`ember.cors.*`) is the ONE origin policy (tenant subdomains → `allowed-origin-patterns`, never `allowed-origins`); task-3.5's `AuthRateLimiterFilter` buckets `(tenant, clientIp)`, applies servlet-wide via `RateLimitProperties.paths`, not `SecurityConfig`. task-3.1: `.env` (gitignored) holds all secrets, no fallback/fail-fast boot, add new ones to `.env.example` too; old secrets remain in git history, rotation pending. task-4.3's `GET /public/restaurants/{slug}/branding` (permitAll) and the JWT filters are the ONLY places allowed to touch `TenantContextHolder`. task-4.4 made `Restaurant.plan`/`status` load-bearing (403 when `status != ACTIVE`).
- task-3.6–4.2: `SettingsPayload.paymentGateway`/`businessHours`/`billing.taxRules` now fully wired frontend-side (task-5.4–5.8); `GET /kitchen/orders`/`/catalog/items` return `Page<T>` (hand-written in `api.ts`). Remaining task-5.x gaps: `api.ts`'s interceptor only special-cases 401 (not 403); `backend-types.ts` predates task-4.3/4.4 additions. Plan/subscription self-service is real on the backend (`RestaurantAdminController`) but has NO frontend home — belongs on a future customer/billing portal, not the admin app; `api.ts`'s `restaurantAdminService` stays unused until then (report 44) — don't re-add a Plan settings tab. `/admin/analytics` (task-5.17–5.21, `FloatingNav.tsx`'s `BarChart3` link) is now COMPLETE: `SummaryCards`/`SalesChart`/`ProductPerformance`/`TableAnalytics` all consume `analyticsService` in `api.ts`, whose response types are `components['schemas'][...]` aliases (refactor-analytics-types-schema, report 66) — every field on them is OPTIONAL, and this project's build has `strictNullChecks` effectively ON despite no `strict`/`strictNullChecks` key in either tsconfig (verify with `npx tsc -b` if unsure), so every numeric/array field needs a `?? 0`/`?? []` default at the call site, not just at declaration. `SalesChart` uses a plain-CSS bar chart (NO charting library in `package.json` — keep following that pattern for future analytics charts unless a real need for one arises). STILL no shared date-range picker UI anywhere on the page — any future analytics work adding one should make it shared, not per-widget.
- task-5.10/5.11: KDS subscribes to `/topic/kitchen/{tenantId}` (`KitchenLayout` + `websocket.ts`'s `subscribeToKitchen`), just calling `queryClient.invalidateQueries(['kitchenOrders'])` on any message rather than reconciling payloads client-side; backend `KitchenWebSocketListener` broadcasts, `KitchenItemUpdated` gained `tenantId`. No STOMP SUBSCRIBE-destination ACL exists for this topic (or `/topic/session/{id}`) — pre-existing, accepted gap. Connection-state `Badge` reading `useWebsocketStore.isConnected` lives in `OrdersDisplays`, not `KitchenLayout`.
- CUSTOMERS ARE NOT TENANT-BOUND (reports 55–56): `User.restaurantId` is null for them by design (staff keep it, still required); a customer's tenant is only known once they JOIN A TABLE, so login/register mint a JWT with NO `rid` and `jwtAuthFilter` waves tenant-less CUSTOMERs through (staff still gated). `joinSessionCode` looks the code up untenanted (`findByJoinCodeAndStatus`, rejects a code open at 2 restaurants), `joinSession` reads the tenant off the signed QR token; both bind it via `SessionService.bindResolvedTenant` (the one non-filter place allowed to touch `TenantContextHolder`, and it re-checks `ACTIVE` since the filter no longer can) and return `JoinSessionResponse{session,token}` — the client MUST swap to that token (`JoinTableModal`), as menu/items/confirm all need `rid`. `issueTenantScopedToken` refuses to re-scope staff. `/t/:slug` (`TenantLanding`) is still UNREACHABLE — no inbound link anywhere; don't build flows through it without wiring an entry point first.
- task-5.12: `analytics` module (`controller`/`service`/`dto`) owns NO persistence — it only aggregates over `billing`/`session`/`catalog`. `AnalyticsController` carries a CLASS-LEVEL `@PreAuthorize("hasRole('ADMIN')")` so task-5.13–5.16's handlers inherit the gate; tenant always via `TenantContextHolder.requireTenantId()`, never a request param. Analytics repo queries name the tenant explicitly (`where b.tenantId = :tenantId`) ON TOP of the `@TenantId` filter — a context slip here returns plausible numbers instead of throwing. Read shapes are JPQL constructor-expression records living in the OWNING module's repo package (`billing.repository.BillActivityWindow`), not interface projections and not analytics DTOs. Seed endpoint `GET /admin/analytics/range` (billing window for task-5.19's date pickers) — add new routes to `SecurityAuditTest`'s 401 matrix.
- task-5.13 (`GET /admin/analytics/summary?from&to`) fixes the metric semantics task-5.14–5.16 must reuse: revenue = CONFIRMED `Payment` sums (never PENDING, never bill totals); AOV = PAID-`Bill` `sum/count` (2dp HALF_UP); `activeSessions` is a LIVE `countByTenantIdAndStatus(OPEN)` that deliberately IGNORES the window. `from`/`to` are optional and INCLUSIVE, defaulted in the service (`EPOCH_FLOOR` = 1970-01-01, never `LocalDateTime.MIN` — no SQL `timestamp` holds it — and `now()`), echoed back in the response; an inverted window throws `IllegalArgumentException` (409) rather than reading as "no sales". Null aggregates normalise to `0`/`0.00`. task-5.14 (`/sales`) reuses that window helper and those metrics: DB groups only by `year()/month()/day()` (NOT `date_trunc` — spells differently on H2 vs Postgres), week/month/year rolled up in Java by `SalesGranularity` (ISO Monday weeks); series is gap-free (zero-filled) and, with no `from`, starts at the first ACTIVE bucket, not `EPOCH_FLOOR` (which would emit ~20k empty daily buckets); unknown granularity → `IllegalArgumentException` (409), never a silent DAY fallback. task-5.15 (`/products`) is the one analytics read that CROSSES STORES: `BillRepository.findPaidSessionIds` (Postgres) → `SessionRepository.findByTenantIdAndIdIn` (Mongo line items, skip `DRAFT`) → `MenuItemRepository.findByTenantIdAndIdInWithCategory` (`join fetch` category) — same "a sale is a settled bill" rule, but its `totalRevenue` is ITEM money and deliberately ≠ `/summary`'s payment revenue (tax/tips live on the bill). Shares/`cumulativeShare` are computed over the FULL set before `limit` truncates; deleted menu items keep the name they were sold under (null category), renamed ones report the current catalogue name. task-5.16 (`/tables`) ALSO crosses stores: `Bill` has no `tableId` of its own, so `BillRepository.findPaidBillActivity` (sessionId+total+createdAt) joins back to `Session.tableId`/`createdAt` in the service, same as products. `Session` has no closed/settled timestamp, so session duration is approximated as `bill.createdAt - session.createdAt`, in minutes; `activeTableCount`/`averageTurnoverRate` are LIVE like `/summary`'s `activeSessions`, ignoring the window; a deleted table keeps the revenue it earned (null `tableNumber`, same pattern as a deleted menu item).
- EMB-PC backlog (brainstormed 2026-08-13): `PlatformOperator`/`PlatformAuditLog` have NO FK to `Restaurant`/`User`, signed with a separate `platform.jwt.secret`, own `SecurityFilterChain` on `/platform/**` that never touches `TenantContextHolder` — mutual exclusion comes from different signing keys, not a claim check. Tenant "owner info" = `User` rows where `role = ADMIN`, looked up untenanted like `PublicRestaurantController`. EMB-PC-06/12's "paginated list" should reuse `components/PaginationControls.tsx` (report 68) rather than re-inventing page controls. EMB-PC-01 (report 70) landed `platform/model/PlatformOperator` + `platform/repository/PlatformOperatorRepository` + `V4__platform_operators.sql` (seed: `platform-admin@ember.local` / `ChangeMe123!`, rotate via EMB-PC-05 once it exists) — **gotcha for any future migration:** `spring.flyway.enabled=false` in the H2 test profile means `./mvnw test` NEVER runs `db/migration/*.sql` — a migration can only be verified by pointing `./mvnw spring-boot:run` (or `psql` directly) at the real `ember-postgres-1` docker container and checking `flyway_schema_history`/the table by hand; a green test suite says nothing about the SQL file's correctness. A bare `@DataJpaTest` on a non-tenant-scoped repository also needs `@Import(TenantIdentifierResolver.class)` just to boot (multi-tenant `SessionFactory` blocks ALL repository beans in the slice otherwise), even for an entity that never touches tenant context. EMB-PC-02 (report 71) landed `platform/model/PlatformAuditLog` + `platform/repository/PlatformAuditLogRepository` (bare `JpaRepository`, no custom finders yet) + `V5__platform_audit_log.sql` (`operatorId`/`operatorEmail` are a write-time snapshot, not a live FK; `restaurantId` nullable + indexed for EMB-PC-09's future filter; `oldValue`/`newValue` are nullable `text` columns) — EMB-PC-07/08/09 are the next consumers of this table. EMB-PC-03 (report 72) landed `platform/service/PlatformJwtService` + `platform/service/PlatformOperatorDetailsService` (concrete-type injection required — `EmberUserDetailsService` is `@Primary`). EMB-PC-04 (report 73) landed `platform/config/PlatformSecurityConfig`: `/platform/**` `SecurityFilterChain` (`@Order(1)`, `AntPathRequestMatcher` not the `String` overload — see System Health note) + inline `platformJwtAuthFilter` (mirrors the tenant filter's validate→load→`SecurityContextHolder` shape, zero `TenantContextHolder` contact) + its own 401 entry point; `SecurityConfig.filterChain` now carries `@Order(2)`. `PlatformAuthIsolationTest` (`config` package) proves cross-rejection using REAL (non-mocked) `JwtService`/`PlatformJwtService` beans — every other controller test `@MockitoBean`s `JwtService`, but that would defeat a test whose entire point is genuine signature verification. EMB-PC-05 (report 74) landed `platform/service/PlatformAuthService` + `platform/controller/PlatformAuthController`: `POST /platform/auth/login` (now the chain's one `permitAll` carve-out, matching the tenant chain's `/auth/**` pattern but scoped to just this path — `/platform/auth/password` stays behind `authenticated()`) and `PATCH /platform/auth/password` (self-service — target email always comes from `Authentication.getName()`, never a request param). Both reuse the single global `PasswordEncoder` bean from `SecurityConfig`. `RateLimitProperties.paths` default now also includes `/platform/auth/login`; `AuthRateLimiterFilter` is a servlet-wide `@Component` independent of either `SecurityFilterChain`, so that was the only wiring needed. EMB-PC-06 (report 75) landed `platform/controller/PlatformRestaurantController` + `platform/service/PlatformRestaurantService`: `GET /platform/restaurants` (paginated `Page<PlatformRestaurantSummaryResponse>` via plain `RestaurantRepository.findAll(Pageable)`) + `GET /platform/restaurants/{id}` (`PlatformRestaurantDetailResponse`, 404s via `ResourceNotFoundException`, embeds `admins` from the new `UserRepository.findByRestaurantId_IdAndRole`). Confirmed `Restaurant`/`User` carry no `@TenantId` so both reads need zero `TenantContextHolder` involvement, unlike `PublicRestaurantController`'s branding lookup. No new security wiring — `/platform/**`'s blanket `anyRequest().authenticated()` already covers it, and `PlatformAuthIsolationTest` already exercised this exact path. EMB-PC-07 (report 76) landed `PATCH /platform/restaurants/{id}/status`: `PlatformRestaurantService.updateStatus` resolves the calling `PlatformOperator` by `Authentication.getName()` (email, same pattern as `PlatformAuthService.changePassword`; `BadCredentialsException` if the operator row is gone despite a valid token), delegates the actual mutation to the now-wired `RestaurantService.updateStatus`, and writes one `PlatformAuditLog` row (`action=RESTAURANT_STATUS_UPDATED`, `oldValue`/`newValue` = enum names) in the SAME `@Transactional` — first task that actually inserts into `platform_audit_log`. Returns the updated `PlatformRestaurantSummaryResponse` (not 204) so a future frontend button can update in place without a refetch. EMB-PC-08 (report 77) landed `POST /platform/restaurants`: `PlatformRestaurantService.create` resolves the operator the same way as `updateStatus`, guards `existsBySlug`/`existsByEmail` (`IllegalArgumentException` → 409 via the existing `GlobalExceptionHandler` mapping, same as `AuthService.register`'s duplicate-email path), saves the `Restaurant` (plan/status/timezone/currency all take entity defaults — no fields for them on the request DTO) then the `User` (`role=ADMIN`, `restaurantId` set, `passwordHash` via the shared `PasswordEncoder`), then one `PlatformAuditLog` row (`RESTAURANT_CREATED`), all in one `@Transactional` — mirrors EMB-PC-07's operator-resolve + audit-in-same-transaction shape. Password strategy decided: the operator supplies the initial ADMIN password directly on the request (`PlatformRestaurantCreateRequest`, same complexity regex as `RegisterRequest`) — no server-generated password or invite/reset-token flow, since no email/invite infrastructure exists in this codebase. This was the first restaurant-creation code path in the app; no prior onboarding/signup logic existed to reuse. EMB-PC-09 (report 78) landed `GET /platform/audit-log`: new `platform/service/PlatformAuditLogService` + `platform/controller/PlatformAuditLogController` (kept separate from `PlatformRestaurantService`/`Controller` since the log has no restaurant FK and may grow other filters later), `PlatformAuditLogRepository.findByRestaurantId(UUID, Pageable)` used only when the optional `restaurantId` query param is present (else plain `findAll`), `@PageableDefault(sort="createdAt", direction=DESC)` so it reads newest-first with no client-supplied sort. No new security wiring — same blanket `/platform/**` `anyRequest().authenticated()` covers it. EMB-PC-10 (report 79) landed the `/console/*` scaffold itself: `usePlatformAuthStore` (persisted `ember-platform-auth-storage`, shape mirrors `useAuthStore` but zero state sharing), `platformApi`/`platformAuthService` (`lib/platformApi.ts` — separate Axios instance, own 401 interceptor logging out only the platform store; hand-typed `PlatformLoginRequest`/`PlatformAuthResponse`/`PlatformPasswordChangeRequest` since `backend-types.ts` has no `platform/**` schemas — regenerating it needs a live backend via `pnpm run openapi`, out of scope here), and `ConsolePlaceholder` mounted at `/console/*` via `React.lazy`/`Suspense` in `App.tsx`, outside every `ProtectedRoute` tree. Verified code-split in the `vite build` output (its own `ConsolePlaceholder-*.js` chunk). EMB-PC-11 (report 80) landed the guard/layout/login: `PlatformProtectedRoute` (token-only check, no role dimension — single operator identity) redirects (relative `Navigate to="login"`) rather than absolute, since it's mounted inside `ConsoleApp`'s own nested `<Routes>` under `/console/*`; `PlatformLayout` is a from-scratch header (imports no `TopNav`/`FloatingNav`) with logout wired to `usePlatformAuthStore().logout`. Rather than statically importing the new guard/layout/pages into `App.tsx` (which would leak them into the tenant main bundle), EMB-PC-10's single `React.lazy` mount point now loads `pages/console/ConsoleApp.tsx`, which itself defines the `login`/guarded-index nested route tree — keeps the whole console subtree, including all future EMB-PC-12–14 pages, inside that one chunk boundary. `ConsolePlaceholder.tsx` is gone; `ConsoleDashboard.tsx` is its authenticated-index replacement. EMB-PC-12 (report 81) landed `pages/console/ConsoleRestaurants.tsx` as a sibling guarded route (`restaurants`) rather than replacing the dashboard index — `ConsoleDashboard` now links to it. `platformApi.ts` deliberately keeps its OWN `Page<T>`/`PlatformRestaurantSummary` types rather than importing `api.ts`'s (the two API layers — tenant vs platform — stay fully independent, same reasoning as EMB-PC-10's separate Axios instance/auth store). EMB-PC-13 (report 82) landed `pages/console/ConsoleRestaurantDetail.tsx` at guarded sibling route `restaurants/:id`: two independent `useQuery`s (detail; audit log, own page state) rather than one combined fetch, since the audit log paginates separately. Status toggle is a `useMutation` calling `platformRestaurantService.updateStatus` then invalidating `['platformRestaurant', id]`, `['platformAuditLog', id]`, AND `['platformRestaurants']` (the list page's key) so a status change is visible everywhere without a manual refetch — that 3-key invalidation is the pattern to follow for any future console mutation that affects both a detail view and a list. `PlatformRestaurantStatusUpdateRequest` only exposes `SUSPENDED`/`ACTIVE`/`INACTIVE`; the toggle button only ever flips between `SUSPENDED`/`ACTIVE` (never sets `INACTIVE` — no UI path to it yet). **Next up, EMB-PC-14:** create-restaurant form wired to `POST /platform/restaurants` + self password-change UI wired to `PATCH /platform/auth/password`.
- feature-catalog-pagination (report 68, ad hoc): `GET /catalog/categories` now takes `Pageable` and returns `Page<CategoryResponse>` (was a bare `List`) — any other future consumer of `categoryService.getAll` must read `.content`, not treat the result as an array. `components/PaginationControls.tsx` is the shared Prev/Next control (page 0-indexed, hides itself when `totalPages <= 1`); reuse it for any future paginated admin list instead of duplicating page-control JSX. feature-pagination-floating-style (report 69) then restyled it to a SELF-POSITIONED fixed `bottom-8 right-8` floating pill matching `FloatingNav`'s look — callers render it as a bare sibling with no wrapper div/margin, it needs no layout slot.
- EMB-LP backlog (brainstormed 2026-08-13, spec `docs/superpowers/specs/2026-08-13-ember-landing-page-design.md`): `ember/landing/` is a FULLY STANDALONE Astro 7.2.2 package — own `pnpm-lock.yaml`/`pnpm-workspace.yaml`, zero coupling to `frontend`'s workspace or API, static-build only, deployed separately (Vercel/Netlify/Cloudflare Pages). EMB-LP-01 (report 84) landed the scaffold: `@astrojs/react` (islands only, for EMB-LP-05/11/12), Tailwind CSS 4 via `@tailwindcss/vite` (the Vite-plugin path, NOT the legacy `@astrojs/tailwind` integration — stay on that path for any future Tailwind config), `src/styles/global.css` (`@import "tailwindcss"`), dev server pinned to port 5174 (`frontend` owns 5173). Brutalist theme tokens (palette/borders/shadows/type) are EMB-LP-04's job, not yet applied. `astro build` (not `pnpm run lint`/`./mvnw test`) is this package's own verification command — it has no test suite. EMB-LP-02 (report 85) set `site: 'https://ember.vanter.com'` in `astro.config.mjs` (the marketing/landing host — confirmed by user; tenant app lives at `slug.ember.vanter.com`, a separate, unrelated subdomain scheme) and wired `@astrojs/sitemap`; the emitted file is `sitemap-index.xml` (pointing at a nested `sitemap-0.xml`), NOT a bare `sitemap.xml` — `robots.txt`'s `Sitemap:` line and any future reference must use that exact filename. EMB-LP-03 (report 86) landed `Layout.astro`/`SEO.astro`: every future page must render through `Layout` (props `title`/`description`/optional `image`), never hand-roll its own `<head>` — that's how the OG/Twitter/canonical tags and favicon links stay consistent site-wide. `apple-touch-icon.png`/`og-image.png` are PLACEHOLDER solid-fill PNGs (brand red / near-black) with no real branded art yet — swap them for real assets before launch, don't mistake their presence for "done." EMB-LP-06 (report 89) landed `Hero.astro` + `src/assets/product-screenshot.png` (also a placeholder, same caveat) and added `sharp` as a `landing` devDependency (Astro's `<Image/>` build pipeline hard-requires it, no pure-JS fallback) — any future component needing `astro:assets`' `<Image/>` on a local file already has it available, don't re-add.

## Task Queue Status
- [x] **Completed (Milestones 1–5, task-5.1–5.21 + bugfix-kds-stale-orders + bugfix-customer-login-500 + refactor-analytics-types-schema):** Frontend/backend stability, WebSocket/error-boundary hardening, backend multi-tenancy & transactional billing, security/config hardening, frontend testing & tenant lifecycle, global 403 handling, `backend-types.ts` regen, ADMIN Plan/Estado UI (reverted, kept for a future billing portal), `MENU`/`BILLING`/`HARDWARE`/"Horario" settings tabs, KDS status-transition controls + stale-order fix + WS topic + connection badge (task-5.9–5.11), CUSTOMER login 500 fixed + tenant-binding moved to table-join (reports 55–56), analytics backend (`/range`/`/summary`/`/sales`/`/products`/`/tables`, task-5.12–5.16, reports 57–61) and its full `/admin/analytics` frontend (page scaffold + Summary Cards + Sales chart + Product Performance + Table Analytics, task-5.17–5.21, reports 62–65+67) with a types refactor to `components['schemas']` in between (report 66). Server-side pagination added to Categories and Menu Items admin views (feature-catalog-pagination, report 68) with its controls restyled to match `FloatingNav` (feature-pagination-floating-style, report 69). `PlatformOperator` entity/repository/seed migration (EMB-PC-01, report 70). `PlatformAuditLog` entity/repository/migration (EMB-PC-02, report 71). `PlatformJwtService`/`PlatformOperatorDetailsService` (EMB-PC-03, report 72). `/platform/**` `SecurityFilterChain` + `PlatformJwtAuthFilter` + cross-rejection test (EMB-PC-04, report 73). `PlatformAuthController` login + self-service password change (EMB-PC-05, report 74). `PlatformRestaurantController` list + detail (EMB-PC-06, report 75). `PATCH /platform/restaurants/{id}/status` + audit log write (EMB-PC-07, report 76). `POST /platform/restaurants` tenant onboarding + audit log write (EMB-PC-08, report 77). `GET /platform/audit-log` paginated + `restaurantId`-filterable audit trail (EMB-PC-09, report 78). Frontend `/console/*` scaffold (EMB-PC-10, report 79). `PlatformProtectedRoute`/`PlatformLayout`/`/console/login` (EMB-PC-11, report 80). Console restaurant list page (EMB-PC-12, report 81). Console restaurant detail page + status toggle + audit history (EMB-PC-13, report 82). Console create-restaurant form + self password-change UI (EMB-PC-14, report 83) — EMB-PC backlog complete. Reports 01–83.
- [x] **EMB-PC-05:** `PlatformAuthController` — `POST /platform/auth/login` + `PATCH /platform/auth/password` (self-service only); `/platform/auth/login` added to `RateLimitProperties.paths`. Report 74.
- [x] **EMB-PC-06:** `GET /platform/restaurants` (paginated list: name, slug, plan, status, createdAt) + `GET /platform/restaurants/{id}` (detail, incl. its `ADMIN` user(s) name/email via an untenanted `UserRepository` query). Report 75.
- [x] **EMB-PC-07:** `PATCH /platform/restaurants/{id}/status` — wires `RestaurantService.updateStatus` (previously unreachable), writes a `PlatformAuditLog` entry in the same `@Transactional`. Report 76.
- [x] **EMB-PC-08:** `POST /platform/restaurants` — operator-driven tenant onboarding: creates the `Restaurant` + its initial `ADMIN` `User` in one request, writes an audit entry. Report 77.
- [x] **EMB-PC-09:** `GET /platform/audit-log` — paginated, filterable by `restaurantId`. Report 78.
- [x] **EMB-PC-10:** Frontend — scaffold `/console` route tree in `App.tsx` via `React.lazy`/`Suspense` (code-split, never bundled for tenant users); `usePlatformAuthStore` (separate token slot from `useAuthStore`); `platformApi` Axios instance. Report 79.
- [x] **EMB-PC-11:** Frontend — `PlatformProtectedRoute` guard + `PlatformLayout` shell (own nav, no shared tenant business components) + `/console/login` wired to `POST /platform/auth/login`. Report 80.
- [x] **EMB-PC-12:** Frontend — restaurant list page consuming `GET /platform/restaurants` (paginated table). Report 81.
- [x] **EMB-PC-13:** Frontend — restaurant detail page (info + owner + enable/disable button wired to `PATCH .../status` + per-tenant audit history from `GET /platform/audit-log?restaurantId=`). Report 82.
- [x] **EMB-PC-14:** Frontend — create-restaurant form wired to `POST /platform/restaurants`, and self password-change UI wired to `PATCH /platform/auth/password`. Report 83.
- [x] **EMB-LP-01:** Scaffold standalone `ember/landing/` Astro project (own `package.json`/lockfile, not a `frontend` workspace member) + Tailwind CSS 4 via Astro's Vite integration + `@astrojs/react`; `astro.config.mjs` dev server on port 5174. Report 84.
- [x] **EMB-LP-02:** Add `@astrojs/sitemap` integration + `public/robots.txt` (allow-all, references `sitemap.xml`) — checklist #7/#8. Report 85.
- [x] **EMB-LP-03:** `src/layouts/Layout.astro` + `src/components/SEO.astro` — per-page `<title>`/`<meta description>` props, OG/Twitter tags → `/public/og-image.png`, favicon set (`favicon.ico`/`.svg`, `apple-touch-icon.png`, `site.webmanifest`) — #3/#4/#5/#6. Report 86.
- [x] **EMB-LP-04:** Brutalist Tailwind 4 theme tokens — palette (`#0a0a0a`/`#f5f5f0`/`#8c1717`), 3–4px borders, `6px 6px 0 #000` hard shadows, zero `border-radius`, Inter/Geist + monospace stack. Report 87.
- [x] **EMB-LP-05:** `src/components/Nav.astro` + `MobileNavDrawer.tsx` island — wordmark, anchors, "Iniciar sesión"/"Registrarme" CTAs → `frontend`, hamburger drawer. Report 88.
- [x] **EMB-LP-06:** `src/components/Hero.astro` — headline/subheadline, above-the-fold primary/secondary CTAs, product screenshot via `<Image/>` with explicit width/height + alt text — #2/#9/#20. Report 89.
- [x] **EMB-LP-07:** `src/components/Features.astro` — 3–6 card grid (collaborative cart, KDS, floor/waiter mgmt, admin analytics). Report 90.
- [x] **EMB-LP-08:** `src/components/Pricing.astro` — four static cards matching `RestaurantPlan` (FREE/STARTER/PRO/ENTERPRISE), ENTERPRISE CTA → `mailto:`/contact form → thank-you redirect. Report 91.
- [x] **EMB-LP-09:** `src/components/CTASection.astro` — high-contrast full-width closing band before the footer. Report 92.
- [x] **EMB-LP-10:** `src/components/Footer.astro` — wordmark, copyright, real physical address/business entity, links to privacy/terms/contact — #19. Report 93.
- [x] **EMB-LP-11:** `src/components/StickyMobileCTA.tsx` island — persistent bottom bar (Register/Iniciar Sesión), mobile-only, appears once scrolled past hero — #11. Report 94.
- [x] **EMB-LP-12:** `src/components/CookieBanner.tsx` island — non-intrusive brutalist cookie consent — #17. Report 95.
- [x] **EMB-LP-13:** Assemble `src/pages/index.astro` wiring Nav/Hero/Features/Pricing/CTASection/Footer/StickyMobileCTA/CookieBanner. Report 96.
- [x] **EMB-LP-14:** `src/pages/404.astro` custom error page — #1. Report 97.
- [x] **EMB-LP-15:** `src/pages/privacy.astro` + `src/pages/terms.astro` legal pages — #15/#16. Report 98.
- [x] **EMB-LP-16:** Contact/lead form island + `src/pages/thank-you.astro` confirmation page, with loading spinner/skeleton + form error states — #12/#13/#14. Report 99.
- [x] **EMB-LP-17:** Privacy-first analytics script (Vercel Analytics/Plausible/Cloudflare, zero tracking cookies by default) wired into `SEO.astro`/`Layout.astro` head — #18. Report 100.
- [x] **EMB-LP-18:** A11y/perf pass — WCAG AAA contrast audit, visible focus indicators, alt-text audit on every `<img>`/`<Image/>`, LCP fix on hero image, final `astro build` verification. Report 101. EMB-LP backlog complete.
- [x] **docs-architecture-diagram:** Root `ARCHITECTURE.md` — system connection diagram + order-lifecycle sequence diagram (Mermaid), brainstormed with user. Report 104.
- [x] **docs-architecture-word:** `docs/ARCHITECTURE.docx` — full class-by-class backend/frontend inventory, tech stack, E2E workflow; supersedes/replaces `ARCHITECTURE.md`. Report 105.
- [x] **bugfix-comanda-resend:** `ItemSent` gained `type`/`sessionItems`, broadcast via `SessionWebSocketListener`; `websocket.ts` handles `ITEMS_CONFIRMED` to sync item status, disabling re-send. Report 106.
- [x] **bugfix-kitchen-orphan-item:** new `KitchenItemRemoved` event; `KitchenService.handleItemDeleted` strips the item from `KitchenOrder` on `DeleteItem`, broadcasts on `/topic/kitchen/{tenantId}`. Report 107.
- [x] **bugfix-kitchen-empty-order:** empty-after-delete `KitchenOrder` retires instead of showing a blank card; reused order on a later confirm reactivates. Report 108.
- [x] **feature-comanda-historial:** `ComandaView` current-order/Historial split, whole-table running total; deleted dead `ItemStatusUpdated` customer status sync. Report 109.
- [x] **feature-session-activity-log:** persisted `Session.activityLog` (`ITEM_SENT`/`ITEM_DELETED`); waiter Actividad card reads it, no longer derived from live items. Report 110.

