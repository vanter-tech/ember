# PROGRESS.md — Active Execution State

## Current Execution State
- **Last Completed Task:** task-5.14 — `GET /admin/analytics/sales?granularity=day|week|month|year&from&to` (gap-free revenue/orders series) — report 59. System health: backend `./mvnw test` 488/488 passing; frontend `pnpm run build` PASSING (0 TS errors, untouched this task); `pnpm run lint` has 18 PRE-EXISTING errors in unrelated files (not introduced here, not yet triaged).
- **Current Active Task:** none — task-5.15 next in the frontend/backend gap-analysis backlog (task-5.15–5.21 remain; EMB-PC-01–14 Platform/Super-Admin Console and EMB-LP-01–18 Landing Page also queued), awaiting task selection/approval

## Active Context & Recent Decisions
- Monolith root at `ember/`; Java 17 + Spring Boot 3.5.14 / React 19 + TS + pnpm. Product: multi-tenant restaurant platform (collaborative cart, KDS, floor/waiter management, admin analytics). task-3.2 deleted `spring-kafka` — Spring `ApplicationEventPublisher` is the only event bus, do not reintroduce a broker.
- task-3.3: `GlobalExceptionHandler` extends `ResponseEntityExceptionHandler` (mandatory — a bare `Exception.class` catch-all would outrank `DefaultHandlerExceptionResolver` and degrade 400s to 500); RFC 7807 via `problem(status, detail, path)`, 500s log ERROR with a `traceId`, never echo `ex.getMessage()`. task-2.15's **Flyway** (`baseline-on-migrate`, migrations from V2, disabled in tests/H2 `create-drop`): every schema change goes in `db/migration/` AND the entity mapping; `ddl-auto: update` silently can't add `NOT NULL` columns without a default to a non-empty table — check boot logs for `CommandAcceptanceException`, don't trust a clean boot.
- Mongo has no replica set/`MongoTransactionManager` — fail-fast validation-before-mutation instead of `@Transactional`. `Session`/`KitchenOrder` carry `tenantId` (task-2.17, no Mongo `@TenantId`, every finder is tenant-first; route session reads through `SessionService.findById`). Hibernate JPA side uses `DISCRIMINATOR` multi-tenancy (task-2.14): `@TenantId` on `Category`/`MenuItem`/`Bill`/`BillSplit`/`Payment`/`DiningTables`/`RestaurantSettings` (`User` deliberately excluded, looked up by email pre-tenant-bind); isolation repo tests extend `config/AbstractTenantIsolationTest`. `config/MongoTenantBackfill` (task-2.18) and `config/KitchenOrderActiveBackfill` (bugfix, report 52 — retires `KitchenOrder.active` on `SessionClosed`, backs `GET /kitchen/display`'s live-only filter, `GET /kitchen/orders` stays unfiltered) are both idempotent boot-time `ApplicationRunner`s that resolve missing/legacy Mongo fields; that's the pattern to follow for any future Mongo field backfill.
- task-3.4: `config/CorsProperties` (`ember.cors.*`) is the ONE origin policy (tenant subdomains → `allowed-origin-patterns`, never `allowed-origins`); task-3.5's `AuthRateLimiterFilter` buckets `(tenant, clientIp)`, applies servlet-wide via `RateLimitProperties.paths`, not `SecurityConfig`. task-3.1: `.env` (gitignored) holds all secrets, no fallback/fail-fast boot, add new ones to `.env.example` too; old secrets remain in git history, rotation pending. task-4.3's `GET /public/restaurants/{slug}/branding` (permitAll) and the JWT filters are the ONLY places allowed to touch `TenantContextHolder`. task-4.4 made `Restaurant.plan`/`status` load-bearing (403 when `status != ACTIVE`).
- task-3.6–4.2: `SettingsPayload.paymentGateway`/`businessHours`/`billing.taxRules` now fully wired frontend-side (task-5.4–5.8); `GET /kitchen/orders`/`/catalog/items` return `Page<T>` (hand-written in `api.ts`). Remaining task-5.x gaps: `api.ts`'s interceptor only special-cases 401 (not 403); `backend-types.ts` predates task-4.3/4.4 additions; `FloatingNav.tsx`'s `/admin/reports` link is dead; no analytics backend exists beyond `/dashboard/status`. Plan/subscription self-service is real on the backend (`RestaurantAdminController`) but has NO frontend home — belongs on a future customer/billing portal, not the admin app; `api.ts`'s `restaurantAdminService` stays unused until then (report 44) — don't re-add a Plan settings tab.
- task-5.10/5.11: KDS subscribes to `/topic/kitchen/{tenantId}` (`KitchenLayout` + `websocket.ts`'s `subscribeToKitchen`), just calling `queryClient.invalidateQueries(['kitchenOrders'])` on any message rather than reconciling payloads client-side; backend `KitchenWebSocketListener` broadcasts, `KitchenItemUpdated` gained `tenantId`. No STOMP SUBSCRIBE-destination ACL exists for this topic (or `/topic/session/{id}`) — pre-existing, accepted gap. Connection-state `Badge` reading `useWebsocketStore.isConnected` lives in `OrdersDisplays`, not `KitchenLayout`.
- CUSTOMERS ARE NOT TENANT-BOUND (reports 55–56): `User.restaurantId` is null for them by design (staff keep it, still required); a customer's tenant is only known once they JOIN A TABLE, so login/register mint a JWT with NO `rid` and `jwtAuthFilter` waves tenant-less CUSTOMERs through (staff still gated). `joinSessionCode` looks the code up untenanted (`findByJoinCodeAndStatus`, rejects a code open at 2 restaurants), `joinSession` reads the tenant off the signed QR token; both bind it via `SessionService.bindResolvedTenant` (the one non-filter place allowed to touch `TenantContextHolder`, and it re-checks `ACTIVE` since the filter no longer can) and return `JoinSessionResponse{session,token}` — the client MUST swap to that token (`JoinTableModal`), as menu/items/confirm all need `rid`. `issueTenantScopedToken` refuses to re-scope staff. `/t/:slug` (`TenantLanding`) is still UNREACHABLE — no inbound link anywhere; don't build flows through it without wiring an entry point first.
- task-5.12: `analytics` module (`controller`/`service`/`dto`) owns NO persistence — it only aggregates over `billing`/`session`/`catalog`. `AnalyticsController` carries a CLASS-LEVEL `@PreAuthorize("hasRole('ADMIN')")` so task-5.13–5.16's handlers inherit the gate; tenant always via `TenantContextHolder.requireTenantId()`, never a request param. Analytics repo queries name the tenant explicitly (`where b.tenantId = :tenantId`) ON TOP of the `@TenantId` filter — a context slip here returns plausible numbers instead of throwing. Read shapes are JPQL constructor-expression records living in the OWNING module's repo package (`billing.repository.BillActivityWindow`), not interface projections and not analytics DTOs. Seed endpoint `GET /admin/analytics/range` (billing window for task-5.19's date pickers) — add new routes to `SecurityAuditTest`'s 401 matrix.
- task-5.13 (`GET /admin/analytics/summary?from&to`) fixes the metric semantics task-5.14–5.16 must reuse: revenue = CONFIRMED `Payment` sums (never PENDING, never bill totals); AOV = PAID-`Bill` `sum/count` (2dp HALF_UP); `activeSessions` is a LIVE `countByTenantIdAndStatus(OPEN)` that deliberately IGNORES the window. `from`/`to` are optional and INCLUSIVE, defaulted in the service (`EPOCH_FLOOR` = 1970-01-01, never `LocalDateTime.MIN` — no SQL `timestamp` holds it — and `now()`), echoed back in the response; an inverted window throws `IllegalArgumentException` (409) rather than reading as "no sales". Null aggregates normalise to `0`/`0.00`. task-5.14 (`/sales`) reuses that window helper and those metrics: DB groups only by `year()/month()/day()` (NOT `date_trunc` — spells differently on H2 vs Postgres), week/month/year rolled up in Java by `SalesGranularity` (ISO Monday weeks); series is gap-free (zero-filled) and, with no `from`, starts at the first ACTIVE bucket, not `EPOCH_FLOOR` (which would emit ~20k empty daily buckets); unknown granularity → `IllegalArgumentException` (409), never a silent DAY fallback.
- EMB-PC backlog (brainstormed 2026-08-13): `PlatformOperator`/`PlatformAuditLog` have NO FK to `Restaurant`/`User`, signed with a separate `platform.jwt.secret`, own `SecurityFilterChain` on `/platform/**` that never touches `TenantContextHolder` — mutual exclusion comes from different signing keys, not a claim check. Tenant "owner info" = `User` rows where `role = ADMIN`, looked up untenanted like `PublicRestaurantController`.

## Task Queue Status
- [x] **Completed (Milestones 1–4, task-5.1–5.14 + bugfix-kds-stale-orders + bugfix-customer-login-500):** Frontend/backend stability, WebSocket/error-boundary hardening, backend multi-tenancy & transactional billing, security/config hardening, frontend testing & tenant lifecycle, global 403 handling, `backend-types.ts` regen, ADMIN Plan/Estado UI (reverted, kept for a future billing portal), `MENU`/`BILLING`/`HARDWARE`/"Horario" settings tabs wired to `SettingsPayload`, `PaymentGatewaySettings`, KDS status-transition controls (task-5.9), KDS stale-order display bug fixed (`KitchenOrder.active`, report 52), tenant-scoped kitchen WS topic (task-5.10, report 53), KDS connection-state badge (task-5.11, report 54), CUSTOMER login 500 fixed + customers made non-tenant-bound (report 55) then tenant resolution moved to table-join (report 56), `analytics` module scaffold + `GET /admin/analytics/range` (task-5.12, report 57), analytics summary-cards endpoint `GET /admin/analytics/summary` (task-5.13, report 58), temporal sales endpoint `GET /admin/analytics/sales` (task-5.14, report 59). Reports 01–59.
- [ ] **task-5.15:** Backend — product performance endpoint (`GET /admin/analytics/products`): % of total sales per menu item/category (Pareto/top-selling), derived from order line items joined to `MenuItem`/`Category`.
- [ ] **task-5.16:** Backend — table analytics endpoint (`GET /admin/analytics/tables`): average session duration, turnover rate, revenue per table, derived from `Session`/`DiningTables`/`Bill`.
- [ ] **task-5.17:** Frontend — scaffold `pages/admin/analytics/` + `/admin/analytics` route in `App.tsx`, and repoint `FloatingNav.tsx`'s existing but dead `/admin/reports` link (renders `NotFound` today) to the new route.
- [ ] **task-5.18:** Frontend — Summary Cards widget (Total Revenue/Active Sessions/Average Order Value) consuming task-5.13.
- [ ] **task-5.19:** Frontend — Daily/Temporal Sales chart (day/week/month/custom range) consuming task-5.14.
- [ ] **task-5.20:** Frontend — Product Performance / Pareto view (top-selling dishes) consuming task-5.15.
- [ ] **task-5.21:** Frontend — Table Analytics view (session duration, turnover, revenue per table) consuming task-5.16.
- [ ] **EMB-PC-01:** `PlatformOperator` JPA entity + repository + Flyway migration (`platform_operators` table, no FK to `Restaurant`/`User`), seeded with the initial operator row (bcrypt hash committed in the migration).
- [ ] **EMB-PC-02:** `PlatformAuditLog` JPA entity + repository + Flyway migration (`platform_audit_log`: operatorId, operatorEmail snapshot, restaurantId, action, oldValue/newValue, createdAt — deliberately no `@TenantId`).
- [ ] **EMB-PC-03:** `PlatformJwtService` keyed to a new `platform.jwt.secret` (`.env`/`.env.example`, fail-fast like `jwt.secret`) + `PlatformOperatorDetailsService` (`UserDetailsService` over `PlatformOperatorRepository`).
- [ ] **EMB-PC-04:** New `SecurityFilterChain` matched to `/platform/**` (`@Order` before the tenant chain) + `PlatformJwtAuthFilter` — never touches `TenantContextHolder`. Add a security test proving a tenant token is rejected on `/platform/**` and a platform token is rejected on tenant routes.
- [ ] **EMB-PC-05:** `PlatformAuthController` — `POST /platform/auth/login` + `PATCH /platform/auth/password` (self-service only); add `/platform/auth/login` to `RateLimitProperties.paths`.
- [ ] **EMB-PC-06:** `GET /platform/restaurants` (paginated list: name, slug, plan, status, createdAt) + `GET /platform/restaurants/{id}` (detail, incl. its `ADMIN` user(s) name/email via an untenanted `UserRepository` query).
- [ ] **EMB-PC-07:** `PATCH /platform/restaurants/{id}/status` — wires `RestaurantService.updateStatus` (previously unreachable), writes a `PlatformAuditLog` entry in the same `@Transactional`.
- [ ] **EMB-PC-08:** `POST /platform/restaurants` — operator-driven tenant onboarding: creates the `Restaurant` + its initial `ADMIN` `User` in one request, writes an audit entry.
- [ ] **EMB-PC-09:** `GET /platform/audit-log` — paginated, filterable by `restaurantId`.
- [ ] **EMB-PC-10:** Frontend — scaffold `/console` route tree in `App.tsx` via `React.lazy`/`Suspense` (code-split, never bundled for tenant users); `usePlatformAuthStore` (separate token slot from `useAuthStore`); `platformApi` Axios instance.
- [ ] **EMB-PC-11:** Frontend — `PlatformProtectedRoute` guard + `PlatformLayout` shell (own nav, no shared tenant business components) + `/console/login` wired to `POST /platform/auth/login`.
- [ ] **EMB-PC-12:** Frontend — restaurant list page consuming `GET /platform/restaurants` (paginated table).
- [ ] **EMB-PC-13:** Frontend — restaurant detail page (info + owner + enable/disable button wired to `PATCH .../status` + per-tenant audit history from `GET /platform/audit-log?restaurantId=`).
- [ ] **EMB-PC-14:** Frontend — create-restaurant form wired to `POST /platform/restaurants`, and self password-change UI wired to `PATCH /platform/auth/password`.
- [ ] **EMB-LP-01:** Scaffold standalone `ember/landing/` Astro project (own `package.json`/lockfile, not a `frontend` workspace member) + Tailwind CSS 4 via Astro's Vite integration + `@astrojs/react`; `astro.config.mjs` dev server on port 5174.
- [ ] **EMB-LP-02:** Add `@astrojs/sitemap` integration + `public/robots.txt` (allow-all, references `sitemap.xml`) — checklist #7/#8.
- [ ] **EMB-LP-03:** `src/layouts/Layout.astro` + `src/components/SEO.astro` — per-page `<title>`/`<meta description>` props, OG/Twitter tags → `/public/og-image.png`, favicon set (`favicon.ico`/`.svg`, `apple-touch-icon.png`, `site.webmanifest`) — #3/#4/#5/#6.
- [ ] **EMB-LP-04:** Brutalist Tailwind 4 theme tokens — palette (`#0a0a0a`/`#f5f5f0`/`#8c1717`), 3–4px borders, `6px 6px 0 #000` hard shadows, zero `border-radius`, Inter/Geist + monospace stack.
- [ ] **EMB-LP-05:** `src/components/Nav.astro` + `MobileNavDrawer.tsx` island — wordmark, anchors, "Iniciar sesión"/"Registrarme" CTAs → `frontend`, hamburger drawer.
- [ ] **EMB-LP-06:** `src/components/Hero.astro` — headline/subheadline, above-the-fold primary/secondary CTAs, product screenshot via `<Image/>` with explicit width/height + alt text — #2/#9/#20.
- [ ] **EMB-LP-07:** `src/components/Features.astro` — 3–6 card grid (collaborative cart, KDS, floor/waiter mgmt, admin analytics).
- [ ] **EMB-LP-08:** `src/components/Pricing.astro` — four static cards matching `RestaurantPlan` (FREE/STARTER/PRO/ENTERPRISE), ENTERPRISE CTA → `mailto:`/contact form → thank-you redirect.
- [ ] **EMB-LP-09:** `src/components/CTASection.astro` — high-contrast full-width closing band before the footer.
- [ ] **EMB-LP-10:** `src/components/Footer.astro` — wordmark, copyright, real physical address/business entity, links to privacy/terms/contact — #19.
- [ ] **EMB-LP-11:** `src/components/StickyMobileCTA.tsx` island — persistent bottom bar (Register/Iniciar Sesión), mobile-only, appears once scrolled past hero — #11.
- [ ] **EMB-LP-12:** `src/components/CookieBanner.tsx` island — non-intrusive brutalist cookie consent — #17.
- [ ] **EMB-LP-13:** Assemble `src/pages/index.astro` wiring Nav/Hero/Features/Pricing/CTASection/Footer/StickyMobileCTA/CookieBanner.
- [ ] **EMB-LP-14:** `src/pages/404.astro` custom error page — #1.
- [ ] **EMB-LP-15:** `src/pages/privacy.astro` + `src/pages/terms.astro` legal pages — #15/#16.
- [ ] **EMB-LP-16:** Contact/lead form island + `src/pages/thank-you.astro` confirmation page, with loading spinner/skeleton + form error states — #12/#13/#14.
- [ ] **EMB-LP-17:** Privacy-first analytics script (Vercel Analytics/Plausible/Cloudflare, zero tracking cookies by default) wired into `SEO.astro`/`Layout.astro` head — #18.
- [ ] **EMB-LP-18:** A11y/perf pass — WCAG AAA contrast audit, visible focus indicators, alt-text audit on every `<img>`/`<Image/>`, Lighthouse/axe check across all pages, final `astro build` verification.
